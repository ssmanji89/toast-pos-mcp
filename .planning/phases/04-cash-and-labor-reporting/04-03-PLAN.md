---
phase: 04-cash-and-labor-reporting
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/report-tools.ts
  - test/fixtures/stdio-report-server.ts
  - test/report-tools-e2e.test.ts
  - docs/architecture/standard-report-tools.md
  - docs/architecture/orders-normalization-contract.md
  - docs/research/toast-api-reporting-landscape.md
autonomous: true
requirements:
  - T4-001
  - T4-002
must_haves:
  truths:
    - "An MCP client discovers and invokes `toast_cash_summary` and `toast_labor_summary` through the production stdio runtime."
    - "The registered cash and labor tools expose read-only structured results with correct complete, incomplete, and denied semantics, provenance, restaurant context, and bounded text content."
    - "The exact combined candidate has authentic Node 20.20.2 and 22.22.2 validation, package proof, independent exact-head review evidence, and explicit remaining external release gates."
  artifacts:
    - path: src/report-tools.ts
      provides: "Production registration and output mapping for both T4 report tools."
    - path: test/fixtures/stdio-report-server.ts
      provides: "Restaurant-scoped synthetic upstream scenarios for both report tools."
    - path: test/report-tools-e2e.test.ts
      provides: "Official-client child-process stdio proof for cash and labor success, denial, incomplete, cancellation, provenance, and privacy."
    - path: docs/architecture/standard-report-tools.md
      provides: "Durable cash/labor report source, formula, completeness, and proof contract."
  key_links:
    - from: src/report-tools.ts
      to: src/cash-report.ts
      via: "registered toast_cash_summary handler passes ctx.mcpReq.signal"
      pattern: "toast_cash_summary|buildCashSummaryReport"
    - from: src/report-tools.ts
      to: src/labor-report.ts
      via: "registered toast_labor_summary handler passes ctx.mcpReq.signal"
      pattern: "toast_labor_summary|buildLaborSummaryReport"
    - from: test/report-tools-e2e.test.ts
      to: test/fixtures/stdio-report-server.ts
      via: "official MCP client runs compiled child-process fixture scenarios"
      pattern: "stdio-report-server"
---

<objective>
Reconcile both independent T4 report slices at the shared MCP boundary and produce exact-head implementation evidence.

Purpose: The cash and labor calculators become a product feature only when the real stdio runtime registers them, their shared output state is truthful, and the combined head has authentic validation and independent review.
Output: Production tool registrations, synthetic child-process coverage, durable contracts, and a PR-ready exact-head evidence record.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cash-and-labor-reporting/04-RESEARCH.md
@.planning/phases/04-cash-and-labor-reporting/04-PATTERNS.md
@.planning/phases/04-cash-and-labor-reporting/04-VALIDATION.md
@.planning/phases/04-cash-and-labor-reporting/04-01-SUMMARY.md
@.planning/phases/04-cash-and-labor-reporting/04-02-SUMMARY.md

<interfaces>
From src/report-tools.ts: `registerStandardReportTools(server, runtime)` is the sole production registration boundary. `reportInputSchema` accepts a valid Toast `businessDate` and optional restaurant GUID. `toolResult()` currently maps `complete | denied`, and this plan must deliberately extend the shared output schema and mapper for labor `incomplete`.

From src/cash-report.ts: `buildCashSummaryReport(runtime, input, { signal })` returns the Standard cash report union from Plan 04-01.

From src/labor-report.ts: `buildLaborSummaryReport(runtime, input, { signal })` returns the Standard labor report union from Plan 04-02, including validated active-entry `incomplete` results.

From test/fixtures/stdio-report-server.ts: create the runtime with synthetic config, synthetic token scopes, and `syntheticToastFetch`; every restaurant-scoped route asserts the Toast restaurant header. Start it with the official local `startStdioServer` path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register both report tools and prove the shared production stdio chain</name>
  <files>src/report-tools.ts, test/fixtures/stdio-report-server.ts, test/report-tools-e2e.test.ts</files>
  <read_first>src/report-tools.ts; src/cash-report.ts; src/labor-report.ts; test/fixtures/stdio-report-server.ts; test/report-tools-e2e.test.ts; src/server.ts; src/stdio.ts</read_first>
  <behavior>
    - The official MCP client lists `toast_cash_summary` and `toast_labor_summary`, and each registration has the same read-only annotations as existing Standard tools.
    - Each handler accepts the standard report input, passes `ctx.mcpReq.signal`, and returns a schema-versioned structured result through the production `ApplicationRuntime`.
    - Cash success, labor complete, labor revised/archived current-snapshot behavior, labor active-entry incomplete, missing-scope denial, malformed required source denial, cancellation during a source read, and rate-limit wait behavior are observable through the compiled stdio child process.
    - Every synthetic cash/labor/config/Orders route receives the selected restaurant header. A scope-denied route records no business-data request.
    - Serialized tool output contains no secret, token, raw source body, employee/contact/external identifier, guest, or card marker.
  </behavior>
  <action>Modify only the shared reconciliation files after both Plan 04-01 and Plan 04-02 are present on the same exact base. In `src/report-tools.ts`, import and register `toast_cash_summary` and `toast_labor_summary` through `registerStandardReportTools`. Reuse `reportInputSchema`, `readOnlyAnnotations()`, and the runtime/signal path. Extend the complete-output report union for `cash_summary` and `labor_summary`. Extend `toolResult()` and its output schema deliberately so a labor `incomplete` result has truthful structured content and bounded text, is not called completed, and does not set the error flag reserved for denied requests. Keep denied results as errors and retain existing tool behavior unchanged.

Extend `test/fixtures/stdio-report-server.ts` with independently invented source data and explicit scenarios for cash success, labor success, revised/archived labor snapshots, active labor entry, missing cash scope, missing labor/order scope, malformed entry/deposit/time-entry/configuration/Orders bodies, cancellation during each report source plan, and rate-limit waiting. Bind cash entries/deposits, labor/configuration, and Orders routes to the fixture restaurant header. Make fixture assertions prove denied scenarios stop before business reads. Do not add real Merchant Data or reusable credential material.

Extend `test/report-tools-e2e.test.ts` using the official MCP client and compiled child process. Prove discovery, annotations, source endpoint traversal, selected restaurant context, business-date/timezone/currency context, bounded provenance and freshness, revised/archived labor current-snapshot behavior, complete/incomplete/denied state, no fabricated totals in denials, cancellation, rate-limit behavior, and privacy serialization for both tools. Keep the current nonzero-ID cancellation proof, and describe first-request cancellation only as the retained external T6-003 release gate. Enumerate and locally mutate every new registration, schema-state mapping, fixture route/header, revision/freshness, cancellation, denial, and privacy assertion guard. Record each caught mutation.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/report-tools-e2e.test.js</automated>
  </verify>
  <done>Both report tools are reachable only through the production stdio dependency chain. The compiled E2E test demonstrates truthful states and safety controls with synthetic fixtures, without claiming live endpoint compatibility or first-request cancellation resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Update durable report contracts and record exact-head validation evidence</name>
  <files>docs/architecture/standard-report-tools.md, docs/architecture/orders-normalization-contract.md, docs/research/toast-api-reporting-landscape.md</files>
  <read_first>docs/architecture/standard-report-tools.md; docs/architecture/orders-normalization-contract.md; docs/research/toast-api-reporting-landscape.md; AGENTS.md; LOOP.md; .planning/phases/04-cash-and-labor-reporting/04-VALIDATION.md</read_first>
  <action>Update `docs/architecture/standard-report-tools.md` with the two tool contracts: input, Standard API source family, exact required scopes, restaurant/business-date/currency/timezone/provenance fields, required cash drawer/no-sale-reason/payout-reason configuration sources, cash source distinctions, labor current-source-snapshot revision policy, aggregate/privacy boundary, and complete/incomplete/denied state semantics. State that cash entries are not guest cash payments; state that labor sales/tips come from Orders and that no overtime wage is calculated without a source multiplier. Update `docs/architecture/orders-normalization-contract.md` with the identifier-only server attribution field, its source-to-normalized path, its in-memory labor-join purpose, and the prohibition on display/contact/raw employee expansion. Update `docs/research/toast-api-reporting-landscape.md` to record the implemented source map and formula boundaries. Apply the DOX check and record `DOX: updated` in the T4 integration PR evidence.

On the single reconciled candidate head, record the immutable candidate SHA immediately before and immediately after each runtime gate, and require the two SHA values to match. The executor must use each selected runtime's paired npm. Run these independently executable authentic gates: `nvm use 20.20.2 && test "$(node --version)" = "v20.20.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/report-tools-e2e.test.js && npm pack --dry-run --json`; then `nvm use 22.22.2 && test "$(node --version)" = "v22.22.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/report-tools-e2e.test.js && npm pack --dry-run --json`. Record in the owning integration PR comment: immutable candidate SHA before and after each gate; each command and exit result; discovered test count and total test count; package file count; focused mutation matrix; production-wiring evidence; DOX result; and the exact source/report/test ownership that made Plans 04-01 and 04-02 safe to build separately. Request an independent reviewer for that exact SHA. A builder must not self-approve; any head change invalidates the review and requires a new exact-head review.

Keep external gates explicit in the same PR evidence: issue #28 requires owner-authorized live Standard credentials and documented Merchant consent before real data reaches an AI tool or third party; #4/T6-003 still requires first-tool-request cancellation proof; and T6 terms/branding review, signing, publish credentials, artifact install smoke, and public publication remain release gates. Do not call synthetic fixtures, package dry-run, or independent code review live Toast, signing, or publish proof.</action>
  <verify>
    <automated>nvm use 20.20.2 && test "$(node --version)" = "v20.20.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/report-tools-e2e.test.js && npm pack --dry-run --json</automated>
    <automated>nvm use 22.22.2 && test "$(node --version)" = "v22.22.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/report-tools-e2e.test.js && npm pack --dry-run --json</automated>
  </verify>
  <done>Durable contracts match the shipped combined behavior. The integration PR has matching before-and-after immutable SHA evidence for independent Node 20.20.2 and 22.22.2 gates, paired-npm package proof, mutation results, DOX result, and an independent-review request. Open external gates remain clearly unresolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP stdio client → registered handler | Tool names, inputs, cancellation, and result-state presentation cross the public local MCP boundary. |
| Fixture runtime → report sources | Synthetic upstream routes stand in only for implementation proof and must preserve the same restaurant/scope controls. |
| Source report union → MCP structured output | Complete, incomplete, and denied report values become client-visible text and structured content. |
| Candidate head → package/release process | The repository artifact crosses into dependency restore, review, signing, and future publication gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T4-CHAIN-01 | Elevation of privilege | `src/report-tools.ts` | mitigate | Register both tools only through the process-owned runtime and preserve report-builder capability preflight. |
| T4-CHAIN-02 | Information disclosure | MCP result mapping | mitigate | Test aggregate-only structured output and bounded text; exclude raw sources, credentials, employee/contact, guest, and card data. |
| T4-CHAIN-03 | Tampering | incomplete/denied mapping | mitigate | Give active validated labor facts an explicit incomplete state; keep missing/malformed/failed sources as errors with no totals. |
| T4-CHAIN-04 | Repudiation | stdio and candidate evidence | mitigate | Child-process official-client test, provenance assertions, immutable SHA, command outputs, and independent exact-head review evidence. |
| T4-CHAIN-05 | Denial of service | cancellation/rate-limit boundary | mitigate | Pass MCP abort signals, test source-read and rate-limit behavior, and retain #4/T6-003 for the unresolved first-request case. |
| T4-CHAIN-SC | Tampering | npm dependency and package artifact | mitigate | Use authentic `npm ci` and package dry-run on both required Node versions; do not use copied caches or reconstructed results. |
</threat_model>

<source_audit>
SOURCE   | ID      | Feature or requirement | Plan | Status | Notes
GOAL     | Phase 4 | Deterministic cash and labor reports with business-date, revision, deletion, wage, tip, break, and completeness handling | 04-01, 04-02, 04-03 | COVERED | Cash and labor are source/report slices; integration proves the production chain.
REQ      | T4-001  | Cash entries/deposits, reversals, no-sales, deposits, drawer/reason context, no false zero | 04-01, 04-03 | COVERED | Integration owns shared MCP proof.
REQ      | T4-002  | Time entries, breaks, jobs, wages, Orders sales/tips, aggregate privacy, salaried/overtime semantics | 04-02, 04-03 | COVERED | Integration owns shared MCP proof.
RESEARCH | Cash    | `cashmgmt:read` plus `config:read`; cash entries, deposits, drawers, no-sale reasons, payout reasons; strict source/provenance; observed reversals | 04-01, 04-03 | COVERED | No expected-deposit or guest cash-payment formula is in Phase 4 scope.
RESEARCH | Labor   | `labor:read`, `config:read`, `orders:read`; current-source snapshot, revised/archived/missed-break time entries, jobs, break types, tip withholding, Orders attribution, active incomplete | 04-02, 04-03 | COVERED | No individual employee report, persisted reconciliation state, or silent revision backfill is in the approved tool contract.
RESEARCH | Proof   | Real stdio path, mutation checks, Node 20/22 authentic checks, package, live gates retained | 04-03 | COVERED | Synthetic evidence remains synthetic.
CONTEXT  | none    | No CONTEXT.md or locked D-NN decision exists | 04-01, 04-02, 04-03 | COVERED | Autonomous documented GSD defaults apply.
</source_audit>

<verification>
1. Run the child-process stdio test after Task 1.
2. Run focused cash, labor, and normalization tests after reconciling both source slices.
3. Run the authentic full check and package dry run on Node 20.20.2 and Node 22.22.2 on one immutable candidate head.
4. Obtain independent exact-head review evidence before merge.
5. Keep #4/T6-003, #28, and T6 terms/signing/publish gates open until their authorized evidence exists.
</verification>

<acceptance_criteria>
- Both Phase 4 tools are production-wired through the same runtime, location, capability, transport, cancellation, provenance, and MCP stdio boundaries as T3-002.
- Test, mutation, package, and review evidence distinguish implemented, validated, reviewed, wired, and externally proven claims.
- No result or document makes accounting, tax, payroll-filing, GAAP, Toast certification, live compatibility, signing, or publication claims.
</acceptance_criteria>

<success_criteria>
The integration plan reconciles every shared-file surface after both independent source/report slices, produces production-chain evidence, and preserves all remaining external release gates without fabricating proof.
</success_criteria>

<output>
Create `.planning/phases/04-cash-and-labor-reporting/04-03-SUMMARY.md` when done.
</output>
