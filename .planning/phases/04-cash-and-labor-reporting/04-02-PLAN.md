---
phase: 04-cash-and-labor-reporting
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/orders-normalization-source.ts
  - src/orders-normalization-traversal.ts
  - src/orders-normalization-types.ts
  - src/labor-report-source.ts
  - src/labor-report.ts
  - test/labor-report.test.ts
  - test/orders-normalization.test.ts
autonomous: true
requirements:
  - T4-002
must_haves:
  truths:
    - "A labor report uses restaurant-local business-date bounds, a current-source snapshot with retained revised, archived, deleted, and active time-entry states, validated labor context, and Orders-derived employee sales/tips."
    - "The report returns aggregate-only output, retains salaried and overtime-hour semantics, and labels active validated shifts as incomplete."
    - "Missing required scopes or sources are denied before a final labor result; unknown labor enums and unresolved references survive safely."
  artifacts:
    - path: src/labor-report-source.ts
      provides: "Strict transient labor source schemas and normalized report facts."
      exports: ["laborTimeEntryArraySchema", "laborJobsSchema", "laborBreakTypesSchema", "laborTipWithholdingSchema"]
    - path: src/labor-report.ts
      provides: "Pure labor fold and ApplicationRuntime-backed labor-summary builder."
      exports: ["buildLaborSummaryReport"]
    - path: src/orders-normalization-types.ts
      provides: "Identifier-only normalized server attribution for the existing Orders pipeline."
    - path: test/labor-report.test.ts
      provides: "Synthetic lifecycle, formula, attribution, denial, privacy, and mutation tests."
  key_links:
    - from: src/labor-report.ts
      to: src/orders-normalization-traversal.ts
      via: "bounded Orders-page normalization retains only server attribution required for labor sales/tips"
      pattern: "normalizeOrdersPages"
    - from: src/labor-report.ts
      to: src/rate-limited-client.ts
      via: "restaurant-scoped cancellable time-entry and Orders page-fold requests"
      pattern: "getJsonDetailedCancellable|foldOrdersBulkPagesCancellable"
    - from: src/labor-report.ts
      to: src/capabilities.ts
      via: "labor:read, config:read, and orders:read preflight"
      pattern: "requiredScopes.*labor:read"
---

<objective>
Deliver the independent T4-002 labor source, Orders-attribution, and deterministic report builder.

Purpose: Labor summaries need complete lifecycle handling and current Orders attribution while retaining the product privacy, capability, and fail-closed contracts.
Output: Tested labor and identifier-only Orders contracts, plus a report builder ready for shared MCP registration.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cash-and-labor-reporting/04-RESEARCH.md
@.planning/phases/04-cash-and-labor-reporting/04-PATTERNS.md
@.planning/phases/04-cash-and-labor-reporting/04-VALIDATION.md

<interfaces>
From src/orders-normalization-source.ts, src/orders-normalization-traversal.ts, and src/orders-normalization-types.ts: extend the existing strict source → immutable normalized-fact pipeline in all three files together. `normalizeOrdersPages({ location, query, pages })` is the boundary; only a validated identifier may survive as server attribution.

From src/sales-report.ts: `foldOrdersBulkPagesCancellable` accepts `{ restaurantGuid, query: { businessDate }, pageSize }`, a bounded state, a page fold callback, and `{ signal }`. It supplies the existing sequential bounded Orders path.

From src/report-tools.ts: current tool output accepts `complete | denied`; the later integration plan extends it for labor `incomplete`. This plan must not modify the shared file.

From src/report-core.ts: use the existing provenance, integer-money, and stable denial helpers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict labor facts and identifier-only Orders server attribution</name>
  <files>src/orders-normalization-source.ts, src/orders-normalization-traversal.ts, src/orders-normalization-types.ts, src/labor-report-source.ts, test/orders-normalization.test.ts, test/labor-report.test.ts</files>
  <read_first>src/orders-normalization-source.ts; src/orders-normalization-traversal.ts; src/orders-normalization-types.ts; src/orders-normalization-helpers.ts; test/orders-normalization.test.ts; docs/architecture/orders-normalization-contract.md</read_first>
  <behavior>
    - A valid Orders server reference becomes an immutable identifier-only normalized field that labor can join in memory; display, contact, and free-text employee data do not survive.
    - Strict labor source schemas preserve revised and archived time-entry lifecycle, job, break, and tip-withholding facts plus open-string values and unresolved references.
    - A returned time entry with an invalid date or a source `businessDate` different from the requested date is rejected.
    - Revised or archived records change only the current invocation snapshot; deleted records remain represented as exclusions; active entries are retained as an incomplete finality fact; a null hourly wage remains salaried context.
    - Synthetic output cannot contain employee names, employee GUIDs, external IDs, guest values, card values, raw source arrays, tokens, or contact markers.
  </behavior>
  <action>Extend `src/orders-normalization-source.ts`, `src/orders-normalization-traversal.ts`, and `src/orders-normalization-types.ts` as one atomic source-to-normalized change. Add the smallest validated server reference needed to attribute Orders sales and payment tips during the labor fold. Reuse `normalizeReference()` and `Object.freeze()` conventions. Keep the reference identifier-only; do not widen normalized orders/checks/payments with human-readable employee fields, guest fields, card data, or raw upstream payloads.

Create `src/labor-report-source.ts` with strict transient Zod schemas for time entries, the one bounded jobs array, break types, and the one tip-withholding object. Retain only report-required identifiers, time bounds, source business date, revised/archived/deleted and active state, regular/overtime hours, nullable wage basis, break and missed-break facts, and open-string job/break/state values. Validate zoned timestamps and business dates. Preserve unknown strings and unresolved references rather than discarding valid records.

Create the initial `test/labor-report.test.ts` and extend `test/orders-normalization.test.ts` before completing the transformations. Use invented synthetic records. Prove identifier-only attribution, schema rejection, source business-date mismatch rejection, unknown enum survival, revised/archived/deleted/active/salaried state retention, current-invocation-only revision semantics, source snapshot/freshness provenance, and serialization privacy. Enumerate and locally mutate every new schema, mapper, immutable-field, revision/freshness, and privacy guard; record each caught mutation in slice evidence.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/orders-normalization.test.js dist-test/test/labor-report.test.js</automated>
  </verify>
  <done>The existing Orders pipeline exposes only a validated immutable attribution identifier, and labor source facts contain all required lifecycle data without exposing personal or raw upstream payload data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the capability-gated labor report with explicit finality</name>
  <files>src/labor-report.ts, test/labor-report.test.ts</files>
  <read_first>src/sales-report.ts; src/item-sales-report.ts; src/runtime.ts; src/capabilities.ts; src/rate-limited-client.ts; src/labor-report-source.ts; src/orders-normalization-traversal.ts; src/report-core.ts</read_first>
  <behavior>
    - `buildLaborSummaryReport` accepts one business date and optional restaurant GUID, constructs restaurant-local closeout bounds, and uses each returned time entry's source business date as the inclusion fact.
    - It requires `labor:read`, `config:read`, and `orders:read` before all labor/config/Orders reads; missing scope performs no business-data request.
    - It uses the selected restaurant GUID and caller signal on `GET /labor/v1/timeEntries` with bounded `startDate`/`endDate`, `includeArchived=true`, and `includeMissedBreaks=true`; it reads `GET /labor/v1/jobs` as one bounded array, `GET /config/v2/breakTypes` through page-token traversal with `config-break-types`, and `GET /config/v2/tipWithholding` as one strict object with `config-tip-withholding`.
    - It folds `/ordersBulk` pages with the shared bounded page consumer and derives employee sales/tips from Orders, never TimeEntry monetary fields.
    - Active validated entries yield `status: incomplete` with explicit counts and finality warnings; deleted entries are explicit exclusions; hourly wage calculates regular wages only; overtime hours are reported without an overtime-wage amount.
  </behavior>
  <action>Create `src/labor-report.ts` with a pure labor fold and `buildLaborSummaryReport`. Resolve the selected location and closeout/timezone context. Compute a bounded restaurant-local start/end timestamp window for the requested Toast business date; use time-entry source `businessDate` as final inclusion validation. Query `GET /labor/v1/timeEntries` with `includeArchived=true` and `includeMissedBreaks=true`. Query `GET /labor/v1/jobs` as one bounded strict array with no pagination. Use the existing `getConfigurationPagesDetailedCancellable` page-token traversal for `GET /config/v2/breakTypes` with rate-limit key `config-break-types`. Query `GET /config/v2/tipWithholding` as one strict object with rate-limit key `config-tip-withholding`. Bind every labor, configuration, and Orders request to the selected restaurant GUID and caller abort signal. Use detailed provenance for every successful source. A malformed, inaccessible, cancelled, or incomplete required source returns a structured denial. Do not call employee endpoints for the aggregate output. Do not request `/labor/v1/shifts`, because this tool input does not expose the optional scheduled-versus-actual comparison contract described in issue #20.

Run one preflight capability decision for exactly `labor:read`, `config:read`, and `orders:read` before every business-data request. Use the shared bounded `foldOrdersBulkPagesCancellable` flow to normalize Orders and join only identifier-level server attribution in memory. Derive sales and tips from those Orders facts, never from TimeEntry monetary fields. Apply current-source-snapshot revision semantics: a revised or archived source record returned by this restaurant-local window changes only this invocation; deleted records are explicit exclusions; do not persist reconciliation state or run a silent `modifiedStartDate`/`modifiedEndDate` repair scan. Aggregate output must contain counts and buckets only. Apply explicit state rules: deleted entries contribute declared exclusions, active entries make the result `incomplete`, null hourly wage records salaried context, regular wages use exact minor-unit arithmetic when a valid hourly wage exists, and overtime is an hours-only total with a formula note that no multiplier source exists. Required source failures, malformed data, location failures, aborts, incomplete configuration traversal, and Orders page failures become `denied`; they cannot become `incomplete` or a complete zero summary.

Extend `test/labor-report.test.ts` with synthetic builder doubles and formula cases for time bounds, scope ordering, location headers and abort signals, revised/archived/deleted entries, current-invocation-only snapshot changes, source snapshot/freshness provenance, active entries, missed breaks, bounded jobs array, break-type traversal, tip-withholding object, regular versus salaried wage semantics, overtime-hour-only policy, unknown enums, Orders-only sales/tips, cancellation, malformed/inaccessible/incomplete required sources, and no-leak serialization. Mutate each state transition, endpoint query option, scope gate, rate-limit key, Orders-attribution choice, revision policy, money calculation, and denial boundary. The focused compiled tests must fail for every mutation before the implementation is restored.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/labor-report.test.js</automated>
  </verify>
  <done>The labor builder has one capability-first, restaurant-scoped source plan. It delivers only aggregate deterministic output, accurately distinguishes incomplete active facts from denied failures, and uses Orders rather than stale TimeEntry monetary fields for sales/tips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP input → labor builder | An untrusted business date and optional restaurant GUID select a time-bounded report. |
| Runtime authority → labor/config/Orders sources | Restaurant-scoped employee-related business data is read after capability preflight. |
| Source records → aggregate result | Time-entry, configuration, and Orders facts are minimized and combined into an operator-visible report. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T4-LABOR-01 | Information disclosure | `src/labor-report.ts` | mitigate | Bind every source read to the selected restaurant GUID and return aggregate output only. |
| T4-LABOR-02 | Elevation of privilege | capability decision | mitigate | Require intersection-backed `labor:read`, `config:read`, and `orders:read` before business reads. |
| T4-LABOR-03 | Tampering | lifecycle/finality fold | mitigate | Strict schemas, source-date validation, active/deleted state handling, and denial on malformed required data prevent fabricated final totals. |
| T4-LABOR-04 | Information disclosure | Orders normalization extension | mitigate | Retain server attribution as identifier-only, use it transiently for joins, and prove serialised output has no personal/guest/payment markers. |
| T4-LABOR-05 | Repudiation | report provenance | mitigate | Add every successful detailed source result to the shared bounded provenance collector. |
| T4-LABOR-SC | Tampering | dependency supply chain | accept | This plan installs no package; the shared candidate package gate is in Plan 04-03. |
</threat_model>

<verification>
Run the focused compiled labor and normalization tests after each task commit. Plan 04-03 owns shared stdio integration, authentic Node 20.20.2/22.22.2 restore/check/package proof, and independent review of the reconciled integration head.
</verification>

<acceptance_criteria>
- The T4-002 builder is independent from T4-001 and has no edit to the shared MCP registration, fixture, E2E, or documentation surfaces.
- Labor results distinguish `complete`, validated-fact `incomplete`, and source/capability `denied` states without changing the existing shared tool-result contract before Plan 04-03.
- Synthetic tests are implementation evidence. They do not establish live Toast scope/payload compatibility, first-tool-request cancellation proof, signing, or publication.
</acceptance_criteria>

<success_criteria>
The independent labor source/report/test slice is executable from merged T3-002 with separate source/report/test ownership and no dependency on the cash slice.
</success_criteria>

<output>
Create `.planning/phases/04-cash-and-labor-reporting/04-02-SUMMARY.md` when done.
</output>
