---
phase: 05-source-distinct-analytics-adapter-and-tools
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/auth.ts
  - src/capabilities.ts
  - src/analytics-access.ts
  - src/runtime.ts
  - test/analytics-config.test.ts
  - test/analytics-capabilities.test.ts
  - test/analytics-access-adapter.test.ts
  - scripts/verify-t5-001-analytics-guard-mutations.mjs
autonomous: true
requirements:
  - T5-001
must_haves:
  truths:
    - "An operator can omit Analytics credentials and retain the current Standard runtime configuration unchanged."
    - "A separately configured Analytics identity must prove enterprise-metrics:read before the adapter makes its sole business-data request."
    - "The adapter can retain only a validated, immutable management-group restaurant registry and reject an invalid or inaccessible selected set."
    - "T5-001 remains an internal runtime boundary: it registers no MCP tool and makes no live Toast compatibility claim."
  artifacts:
    - path: src/analytics-access.ts
      provides: "Closed Analytics capability, credential, request, registry, selection, cancellation, and per-identity limiter boundary."
      exports: ["AnalyticsAccessAdapter", "createAnalyticsAccessAdapter", "validateAnalyticsRestaurantSelection"]
    - path: test/analytics-access-adapter.test.ts
      provides: "Synthetic endpoint, header, scope, schema, cancellation, isolation, and serialization proofs."
    - path: scripts/verify-t5-001-analytics-guard-mutations.mjs
      provides: "Enumerated mutation execution that records a focused test failure for every Analytics safety guard."
  key_links:
    - from: src/config.ts
      to: src/auth.ts
      via: "separate non-serializable Analytics credentials feed a distinct OAuth token manager"
      pattern: "getAnalytics.*Credentials|createOAuthTokenManager"
    - from: src/analytics-access.ts
      to: src/capabilities.ts
      via: "Analytics preflight requires the Analytics scope decision before the restaurant-information GET"
      pattern: "decideAnalyticsCapability|enterprise-metrics:read"
    - from: src/runtime.ts
      to: src/analytics-access.ts
      via: "optional process-owned Analytics adapter is composed without entering the Standard location registry"
      pattern: "analyticsAccess|AnalyticsAccessAdapter"
---

<objective>
Build and prove T5-001's separate Analytics authority and management-group restaurant adapter.

Purpose: The Analytics API has separate credentials, scope, location authority, request boundary, and rate-limit state. This slice must prove those controls before any report-job or MCP presentation work begins.
Output: Test-first Analytics config, capability, adapter, immutable selected-set contract, and explicit mutation harness.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-CONTEXT.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-RESEARCH.md
@src/config.ts
@src/auth.ts
@src/capabilities.ts
@src/locations.ts
@src/runtime.ts
@src/transport.ts
@test/config.test.ts
@test/capabilities.test.ts
@test/locations.test.ts
@test/support/synthetic-runtime-env.ts
@scripts/verify-pr34-normalization-mutations.mjs

<interfaces>
From `src/config.ts`: `loadRuntimeConfig(source)` returns a frozen `RuntimeConfig`; `getRuntimeConfigCredentials(config)` reads credentials from a private identity-keyed store. Preserve the same non-serializable credential rule for Analytics.

From `src/auth.ts`: `createOAuthTokenManager(config, options)` owns in-memory OAuth token refresh, and `OAuthTokenManager` implements `getProvisionedScopes()`. Refactor only as needed to accept a separate credential configuration without exposing a bearer token or changing Standard behavior.

From `src/capabilities.ts`: `ProvisionedScopeProvider` supplies token scopes. Standard `CapabilityContext` uses a restaurant and connection scopes, so Analytics needs a separate context and decision rather than a Standard location or connection-scope intersection.

From `src/locations.ts`: registry replacement validates all records before atomically publishing frozen records. Use its strict schema, duplicate rejection, selected identity, and no-raw-body patterns, but do not use `ToastLocation` or `ToastLocationRegistry` for Analytics.

From `src/transport.ts`: `ToastApiFamily` permits only Standard requests and its private no-header path is a Standard Partners exception. Keep Analytics in a new closed adapter; do not widen this transport union or its headerless primitive.

From `src/runtime.ts`: `createApplicationRuntime()` creates the process-owned Standard runtime. It may expose the optional Analytics adapter as a distinct internal property, but this slice must not change `src/server.ts`, `src/report-tools.ts`, or stdio fixtures.
</interfaces>
</context>

<feature>
  <name>Separate Analytics authority and management-group access adapter</name>
  <files>src/config.ts, src/auth.ts, src/capabilities.ts, src/analytics-access.ts, src/runtime.ts, test/analytics-config.test.ts, test/analytics-capabilities.test.ts, test/analytics-access-adapter.test.ts, scripts/verify-t5-001-analytics-guard-mutations.mjs</files>
  <behavior>
    - Optional Analytics configuration accepts one complete Analytics-only hostname, access type, client ID, and client secret set or no Analytics configuration. A partial set fails closed, and neither config rendering nor errors expose either Standard or Analytics synthetic credential marker.
    - The Analytics scope context accepts only a separately managed token identity with `enterprise-metrics:read`. Missing or malformed scope data returns an immutable Analytics denial before the fetch double observes a business-data request.
    - The sole adapter method issues one GET to `/era/v1/restaurants-information`, sends its Analytics bearer token and cancellation signal, sends no Standard restaurant header, and uses only Analytics identity-scoped rate-limit state for the documented five-per-second and thirty-per-minute source limit.
    - The adapter validates only restaurant GUID, name, active, test-mode, and archived status; it atomically publishes frozen records; it rejects duplicate or malformed GUIDs and selected sets that are empty, duplicated, malformed, or outside its registry.
    - Two synthetic Analytics identities cannot share token, registry, selection, or limiter state. Serialized configs, contexts, registries, errors, and denials never contain invented raw body, guest, card, contact, token, or credential markers.
    - The compiled test suite contains no MCP tool invocation for this slice. The test proves the internal runtime composition boundary only.
  </behavior>
  <implementation>Use existing TypeScript, Zod, in-memory OAuth, cancellation, immutable-publication, and secret-safe error conventions. Add a closed Analytics request model and a separate rate-limit namespace. Retain source status fields without choosing a dataset eligibility policy. Keep job lifecycle, report calculations, tool registration, stdio fixture routing, and live compatibility outside this slice, per D-08 and D-09.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Analytics authority and guard tests</name>
  <files>test/analytics-config.test.ts, test/analytics-capabilities.test.ts, test/analytics-access-adapter.test.ts, scripts/verify-t5-001-analytics-guard-mutations.mjs</files>
  <read_first>test/config.test.ts; test/capabilities.test.ts; test/locations.test.ts; test/support/synthetic-runtime-env.ts; scripts/verify-pr34-normalization-mutations.mjs; src/config.ts; src/capabilities.ts; src/locations.ts</read_first>
  <behavior>
    - The config test distinguishes absent, complete, partial, malformed, and serializable Analytics configuration while preserving the existing Standard test environment result.
    - The capability test proves accepted, missing-scope, malformed-scope, frozen, and cross-identity decisions without consulting Standard connection scopes.
    - The adapter test proves preflight order, exact method and path, absent restaurant header, token separation, abort propagation, source-specific limiter state, strict source validation, atomic registry publication, selected-set validation, and privacy serialization using independently invented fixtures.
    - The mutation script enumerates every config, scope, route, header, schema, registry, selection, limiter, isolation, cancellation, and privacy guard. It applies each mutation one at a time in a temporary work area, requires the focused compiled test to fail, restores the candidate, and reports no survivor.
  </behavior>
  <action>Create the three test files before source implementation. Use new synthetic Analytics environment entries and distinctive invented markers that differ from the existing Standard markers. Make `npm run build:test` fail because the named Analytics exports do not exist yet; record that RED result in the task evidence.

In `test/analytics-config.test.ts`, define the selected local Analytics environment-name contract and test all-or-nothing optional credentials, optional startup behavior, immutability, and non-serialization. In `test/analytics-capabilities.test.ts`, require only the Analytics scope authority described in D-02 and D-03. In `test/analytics-access-adapter.test.ts`, use fetch, clock, and wait doubles to record preflight-before-read ordering, one literal read operation per D-04 and D-06, no Standard header, cancellation, response minimization, failures that leave the prior registry generation intact, canonical selected-set validation per D-07, and independent state for two Analytics identities.

Create the mutation script with an explicit guard matrix. Follow the existing temporary-copy and restoration pattern. The script must run the focused compiled Analytics tests for each mutation and stop with a nonzero result when a guard survives. It must not contact Toast, read real environment credentials, capture source bodies, or create a package lockfile.</action>
  <verify>
    <automated>npm run build:test; test $? -ne 0</automated>
  </verify>
  <done>Each new test has a precise safety behavior and the test build fails only because the planned Analytics contracts are absent. The mutation harness has one named mutation for every listed guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the closed Analytics adapter and make every guard green</name>
  <files>src/config.ts, src/auth.ts, src/capabilities.ts, src/analytics-access.ts, src/runtime.ts, test/analytics-config.test.ts, test/analytics-capabilities.test.ts, test/analytics-access-adapter.test.ts, scripts/verify-t5-001-analytics-guard-mutations.mjs</files>
  <read_first>src/config.ts; src/auth.ts; src/capabilities.ts; src/locations.ts; src/runtime.ts; src/transport.ts; test/analytics-config.test.ts; test/analytics-capabilities.test.ts; test/analytics-access-adapter.test.ts</read_first>
  <action>Implement a separate optional Analytics configuration and private credential lookup per D-01. Use the resolved package-local environment contract `TOAST_ANALYTICS_API_HOSTNAME`, `TOAST_ANALYTICS_ACCESS_TYPE`, `TOAST_ANALYTICS_CLIENT_ID`, and `TOAST_ANALYTICS_CLIENT_SECRET`. Treat the four-value set as all-or-nothing and preserve the established Standard `RuntimeConfig` API and its output when the set is absent. Validate the Analytics hostname and access type independently from Standard values. Refactor the OAuth token-manager input behind a narrow internal credential contract only if both Standard and Analytics managers retain independent identity-keyed credentials and token state.

Add `AnalyticsCapabilityContext` and `decideAnalyticsCapability()` in `src/capabilities.ts`. The decision must normalize and freeze trusted open-string scopes, require `enterprise-metrics:read`, expose a stable Analytics-specific denial, and avoid `ToastLocation`, `connectionScopes`, and Standard exclusions per D-02 and D-03.

Create `src/analytics-access.ts` as the only Analytics request surface. Its public operation must be limited by the type system and implementation to the documented management-group restaurant information GET in D-04 and D-06. Obtain an Analytics token, preflight before the fetch, pass a caller signal, omit the Standard restaurant header, validate and minimize the response, publish a frozen registry only after the whole response validates, and retain all documented status flags without guessing report eligibility. Use a distinct private credential-identity plus canonical selected-set key for its registry and limiter state. Enforce the documented endpoint limits with Analytics-only state. Reject guest-linked operation, field, fixture, and data-flow expansion per D-05. Add immutable canonical selected-set validation per D-07.

Compose the optional adapter into `ApplicationRuntime` as an internal boundary. Keep its lifecycle separate from the Standard location discovery, client, scope context, registry, rate-limit state, report calculators, and response objects. Do not edit MCP registration, report tool, stdio server, or fixture files, per D-09. Finish the GREEN cycle by compiling and running all focused Analytics tests, then run the guard-mutation script. Restore every mutation before creating the implementation commit.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-config.test.js dist-test/test/analytics-capabilities.test.js dist-test/test/analytics-access-adapter.test.js && node scripts/verify-t5-001-analytics-guard-mutations.mjs</automated>
  </verify>
  <done>The adapter has one separate authority path and one allowlisted GET. All named tests and every enumerated mutation pass after restoration. Standard configuration and source state remain separate, and the process has no new MCP tool path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator environment → Analytics configuration | Optional Analytics secrets become a private in-memory authority identity. |
| Analytics token scope → adapter request | Token-derived scope authority decides whether the only business-data GET may start. |
| Analytics restaurant response → internal registry | Untrusted management-group source records become minimized internal location authority. |
| Internal runtime → later presentation layers | This slice exposes an adapter boundary but no MCP registration or report output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5-001-01 | Elevation of privilege | config, auth, capabilities | mitigate | Separate optional credentials, distinct token manager, Analytics-only scope context, and preflight before the source GET implement D-01 through D-03. |
| T5-001-02 | Information disclosure | analytics adapter | mitigate | The closed request type permits only the D-04 restaurant-information GET and excludes guest-linked routes and fields per D-05 and D-06. |
| T5-001-03 | Tampering | response registry and selected set | mitigate | Strict schemas, duplicate rejection, atomic immutable publication, canonical UUID set validation, and focused mutations implement D-07. |
| T5-001-04 | Information disclosure | identity-scoped state | mitigate | Private Analytics credential identity plus canonical selected set isolates token, registry, and limiter state across accounts. |
| T5-001-05 | Denial of service | Analytics GET limiter | mitigate | Apply endpoint-specific five-per-second and thirty-per-minute state only to this GET; retain job endpoint/time-range policy for D-08. |
| T5-001-SC | Tampering | npm dependency supply chain | accept | This plan adds no package. Plan 05-02 requires authentic locked-dependency Node and package validation. |
</threat_model>

<verification>
Run the focused compiled Analytics tests after each RED and GREEN commit. Run the enumerated mutation harness after each change to a safety guard. Plan 05-02 owns authentic Node 20.20.2/22.22.2, package, PR evidence, and independent exact-head review.
</verification>

<success_criteria>
- T5-001 has a deterministic internal path from optional Analytics config to scoped, cancelled, rate-limited, minimized management-group discovery.
- Every input or source failure is a specific denial or error, never a fabricated empty authority set.
- Synthetic tests and mutations prove implementation behavior only. They do not establish live Toast compatibility, report-job behavior, tool wiring, package publication, or release readiness.
</success_criteria>

<output>
Create `.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-01-SUMMARY.md` when done.
</output>
