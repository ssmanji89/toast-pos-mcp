---
phase: 05-source-distinct-analytics-adapter-and-tools
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - test/analytics-config.test.ts
  - test/analytics-capabilities.test.ts
  - test/analytics-access-adapter.test.ts
  - scripts/verify-t5-001-analytics-guard-mutations.mjs
  - docs/verification/t5-001-analytics-adapter-guard-matrix.md
autonomous: false
gap_closure: true
requirements:
  - T5-001
must_haves:
  truths:
    - "Each of the 21 matrix guards has a named focused mutation proof, including all 12 guards that were previously blocked."
    - "Every one of the 22 mutation entries changes a production guard, makes a focused compiled test fail, and restores the final candidate source before the next entry."
    - "One unchanged candidate SHA has authentic Node 20.20.2 and Node 22.22.2 clean-install, package, mutation, and independent-review evidence in the existing T5-001 pull request."
    - "The evidence keeps T5-001 internal, read-only, non-guest, synthetic-only, and distinct from T5-002 and T5-003 work."
  artifacts:
    - path: "scripts/verify-t5-001-analytics-guard-mutations.mjs"
      provides: "Unique named mutation executions for all 22 T5-001 guard proofs."
    - path: "docs/verification/t5-001-analytics-adapter-guard-matrix.md"
      provides: "Pre-candidate matrix with no Missing mutation identifier."
    - path: "test/analytics-access-adapter.test.ts"
      provides: "Focused behavior proofs for Analytics request, registry, selection, state, cancellation, limiter, and runtime boundaries."
  key_links:
    - from: "docs/verification/t5-001-analytics-adapter-guard-matrix.md"
      to: "scripts/verify-t5-001-analytics-guard-mutations.mjs"
      via: "Every matrix identifier has one unique harness entry and all 22 entries are caught."
      pattern: "analytics-(config-optional-standard-compatibility|config-completeness|config-secret-serialization|standard-scope-substitution|cross-identity-state|guest-route|atomic-publication|selection-uuid|selection-canonicalization|selection-identity|limiter-isolation|runtime-tool-boundary)"
    - from: "scripts/verify-t5-001-analytics-guard-mutations.mjs"
      to: "test/analytics-*.test.ts"
      via: "Each temporary production mutation runs the focused compiled Analytics test command and must fail."
      pattern: "build:test.*analytics-config"
    - from: "T5-001 pull-request evidence"
      to: "final candidate SHA"
      via: "Before-and-after SHA checks surround both supported Node sequences and independent review names the same SHA."
      pattern: "git rev-parse HEAD"
---

<objective>
Close the T5-001 guard-matrix proof gap, then resume the blocked 05-02 final-candidate validation and review record.

Purpose: The committed matrix has 12 missing named mutations. A green ten-entry harness cannot establish every T5-001 safety guard or support candidate selection.
Output: A complete 22-entry mutation harness, focused regression tests where required, an updated static matrix, and exact-head evidence in the existing T5-001 pull request.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-CONTEXT.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-01-SUMMARY.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-02-PLAN.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-02-SUMMARY.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-VALIDATION.md
@docs/verification/t5-001-analytics-adapter-guard-matrix.md
@scripts/verify-t5-001-analytics-guard-mutations.mjs
@test/analytics-config.test.ts
@test/analytics-capabilities.test.ts
@test/analytics-access-adapter.test.ts

<interfaces>
The current mutation harness runs the three focused compiled Analytics tests after one temporary replacement in `src/config.ts`, `src/capabilities.ts`, `src/analytics-access.ts`, or `src/runtime.ts`, then restores the original source in `finally`. It has ten named entries and must become a unique 22-entry source-mutation list.

The matrix contains 21 guard rows. G05 requires two distinct mutations, so the complete proof set has 22 identifiers. The 12 missing identifiers are `analytics-config-optional-standard-compatibility`, `analytics-config-completeness`, `analytics-config-secret-serialization`, `analytics-standard-scope-substitution`, `analytics-cross-identity-state`, `analytics-guest-route`, `analytics-atomic-publication`, `analytics-selection-uuid`, `analytics-selection-canonicalization`, `analytics-selection-identity`, `analytics-limiter-isolation`, and `analytics-runtime-tool-boundary`.

Plan 05-02 Task 2 owns authentic final-candidate checks and pull-request evidence. Plan 05-02 Task 3 owns independent review. This gap plan supplies the missing proof prerequisite and resumes both tasks without adding a report job, MCP tool, stdio tool path, live Toast request, or guest-linked dataset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add and prove every missing Analytics guard mutation</name>
  <files>test/analytics-config.test.ts, test/analytics-capabilities.test.ts, test/analytics-access-adapter.test.ts, scripts/verify-t5-001-analytics-guard-mutations.mjs, docs/verification/t5-001-analytics-adapter-guard-matrix.md</files>
  <read_first>docs/verification/t5-001-analytics-adapter-guard-matrix.md; scripts/verify-t5-001-analytics-guard-mutations.mjs; test/analytics-config.test.ts; test/analytics-capabilities.test.ts; test/analytics-access-adapter.test.ts; src/config.ts; src/capabilities.ts; src/analytics-access.ts; src/runtime.ts</read_first>
  <behavior>
    - Analytics configuration tests reject a forced absence failure, a bypassed partial four-value check, and a credential field made serializable while preserving Standard-only startup behavior.
    - Analytics capability tests reject a Standard-scope-shaped context and prevent one Analytics context from using another identity's frozen scope set.
    - Adapter tests reject an expanded guest request route, a failed refresh that overwrites the last valid registry, invalid selected-set UUID input, unsorted normalized selected sets, selected sets accepted under another Analytics identity, and shared limiter state.
    - Runtime tests reject a composition change that exposes the internal adapter through MCP or Standard runtime state.
    - The harness rejects duplicate names, requires exactly the 22 matrix identifiers, applies each mutation to a production source guard, requires a nonzero focused-test result, and restores the source after every entry.
  </behavior>
  <action>Work test-first. For every missing identifier, first add or strengthen the smallest focused assertion that expresses the matrix invariant. Run the focused compiled Analytics tests against a temporary single mutation and confirm the test fails before registering the mutation in the harness. Do not use aggregate test success as the proof.

Extend the existing harness with exactly these twelve named mutations: `analytics-config-optional-standard-compatibility`, `analytics-config-completeness`, `analytics-config-secret-serialization`, `analytics-standard-scope-substitution`, `analytics-cross-identity-state`, `analytics-guest-route`, `analytics-atomic-publication`, `analytics-selection-uuid`, `analytics-selection-canonicalization`, `analytics-selection-identity`, `analytics-limiter-isolation`, and `analytics-runtime-tool-boundary`. Use a stable, single-occurrence production guard marker for each. Make each replacement compile so that the focused test failure proves behavior rather than a TypeScript syntax failure. Retain the ten existing names unchanged. Add harness self-validation that requires the 22-name set with no duplicate or omitted entry, and fail the harness before source mutation if that set is incomplete.

Use invented synthetic records and markers only. Keep separate Analytics credentials, token managers, capability contexts, registry identity, and limiter state per D-01 through D-03. Keep the request operation closed, read-only, and limited to its existing management-group discovery boundary per D-04 and D-06. Prove the request-layer privacy exclusion per D-05 without adding a guest-linked route, field, fixture, or source request. Preserve immutable publication and selected-set identity validation per D-07. Do not add Analytics job lifecycle, report endpoints, endpoint/time-range policy, report calculation, MCP registration, or stdio tool behavior; those boundaries remain owned by D-08 and D-09.

Update the committed guard matrix in the same pre-candidate change. Replace all twelve Missing entries with their exact identifiers and MAPPED status. Keep the matrix free of candidate SHA values, command outputs, package results, and review outcomes. Commit the test, harness, and matrix change before selecting a candidate. Run the focused compiled test command and the complete harness after restoration. Record `DOX: updated` in the pull-request evidence because the durable guard-proof contract changes.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-config.test.js dist-test/test/analytics-capabilities.test.js dist-test/test/analytics-access-adapter.test.js && node scripts/verify-t5-001-analytics-guard-mutations.mjs</automated>
  </verify>
  <done>The static matrix has 21 MAPPED guard rows with 22 unique mutation identifiers. The harness prints a caught result for every entry, restores all production files, and the focused compiled suite passes afterward.</done>
</task>

<task type="auto">
  <name>Task 2: Validate one immutable candidate and resume 05-02 pull-request evidence</name>
  <files>External T5-001 pull-request evidence only; leave the committed guard matrix unchanged after candidate selection</files>
  <action>After Task 1 is committed and `git status --short` is empty, set one full `CANDIDATE_SHA` from `git rev-parse HEAD`. This is the only final candidate for this execution. Record it in the existing T5-001 pull request. Do not amend the commit or select a new candidate while either supported-runtime sequence or review remains pending. If `git rev-parse HEAD` differs from `CANDIDATE_SHA`, if tracked source differs, or if a mutation restoration leaves a diff, invalidate all candidate evidence, commit the required correction, set one new candidate SHA, and repeat every validation and review step.

For Node 20.20.2, use its paired npm and require the runtime version before `npm ci --no-audit --no-fund`. Record the candidate SHA immediately before and after this complete sequence: `npm run check`, `npm run build:test`, the three focused compiled Analytics test files, the 22-entry guard harness, and `npm pack --dry-run --json`. Run the same clean-install and command sequence under Node 22.22.2 with its paired npm. Record each exit result, test-file discovery count, Node test total, package file count, mutation count of 22, caught count of 22, survivor count of zero, DOX result, and before-and-after SHA values in the existing T5-001 pull-request evidence only.

Use authentic registry-backed dependency restoration. Do not use copied caches, fabricated lockfiles, reconstructed results, real Merchant Data, live Toast credentials, or validation doubles. Keep the final PR evidence explicit: T5-001 has no MCP or stdio tool path under D-09; report job lifecycle and endpoint/time-range limits remain D-08/T5-002; report presentation remains T5-003; live Analytics compatibility, documented Merchant consent for AI processing, first-tool-request cancellation, signing, publication, and install smoke remain external gates.</action>
  <verify>
    <automated>nvm use 20.20.2 && test "$(node --version)" = "v20.20.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/analytics-config.test.js dist-test/test/analytics-capabilities.test.js dist-test/test/analytics-access-adapter.test.js && node scripts/verify-t5-001-analytics-guard-mutations.mjs && npm pack --dry-run --json && nvm use 22.22.2 && test "$(node --version)" = "v22.22.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/analytics-config.test.js dist-test/test/analytics-capabilities.test.js dist-test/test/analytics-access-adapter.test.js && node scripts/verify-t5-001-analytics-guard-mutations.mjs && npm pack --dry-run --json</automated>
  </verify>
  <done>The existing T5-001 pull request has authentic Node 20.20.2 and Node 22.22.2 evidence for one unchanged candidate SHA. Both runs show all 22 mutations caught and no survivor, and no post-candidate matrix change exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record an independent review of the final candidate</name>
  <files>External T5-001 pull-request review evidence only; do not alter the committed guard matrix</files>
  <what-built>Complete named mutation proof coverage and exact-head Node, package, and PR validation evidence for the internal T5-001 adapter.</what-built>
  <how-to-verify>
    1. Open the existing T5-001 pull request.
    2. Compare the candidate SHA in both Node evidence records with the reviewed SHA.
    3. Confirm all 22 named mutations report caught and the builder did not approve the change.
    4. Confirm the evidence retains the no-tool, no-live, no-guest, and later-slice boundaries.
  </how-to-verify>
  <action>Request findings from an independent reviewer for exactly `CANDIDATE_SHA`. The builder must not self-approve. If the reviewer sees another SHA or reports a required finding, invalidate the candidate evidence, correct the work in a new commit, select one new candidate, and repeat Task 2 before another review. Record the reviewer identity, exact reviewed SHA, result, and any findings in the existing pull request. Keep live compatibility and all external release gates open.</action>
  <verify>
    <automated>git status --short && git rev-parse --verify HEAD</automated>
  </verify>
  <resume-signal>Provide the independent review result and the exact reviewed SHA, or provide the required findings.</resume-signal>
  <done>An independent reviewer reports on the same single candidate SHA used by both Node validation records. The PR evidence distinguishes synthetic implementation validation, independent review, and unresolved external gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix guard → mutation harness | Each stated security guard requires a unique failing production mutation proof. |
| Temporary mutation → candidate source | The harness must restore the exact candidate after every focused test failure. |
| Candidate SHA → runtime/package evidence | Node 20 and Node 22 checks must execute against the one immutable committed candidate. |
| Builder → reviewer | A separate reviewer must evaluate the exact candidate without builder self-approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5-001-G-01 | Tampering | mutation harness | mitigate | Require all 22 unique identifiers, production-source markers, nonzero focused test failures, and `finally` restoration. |
| T5-001-G-02 | Information disclosure | Analytics guard tests | mitigate | Use invented fixtures only and retain request-layer guest exclusion, private credentials, and source minimization under D-01 through D-07. |
| T5-001-G-03 | Tampering | final candidate | mitigate | Capture one full candidate SHA and enforce matching before-and-after SHA values around both supported Node sequences. |
| T5-001-G-04 | Repudiation | pull-request evidence | mitigate | Record exact commands, exits, test counts, package count, 22 caught mutations, DOX, reviewer, and reviewed SHA. |
| T5-001-G-05 | Elevation of privilege | release claim | mitigate | Preserve D-08 and D-09 ownership and state the no-MCP, no-live, and external release gates in PR evidence. |
| T5-001-G-SC | Tampering | npm dependency supply chain | mitigate | Use authentic `npm ci --no-audit --no-fund` and package dry-run with each selected Node runtime. |
</threat_model>

<verification>
Run the focused compiled Analytics suite after every test or harness change. Run the 22-entry harness after every guard change and before the candidate commit. Select the candidate only after the matrix and code commit are clean. Run the full authentic Node 20.20.2 and Node 22.22.2 sequences on that one SHA. The independent-review checkpoint blocks merge evidence until the reviewer names that same SHA.
</verification>

<success_criteria>
- No `Missing:` mutation identifier remains in the T5-001 matrix.
- Every matrix guard has a focused compiled test and a caught named production mutation.
- The existing pull request records one exact candidate SHA, two supported-runtime clean-install/package results, a 22/22 mutation result, and independent review evidence.
- The slice remains an internal, read-only Analytics discovery boundary with no guest-linked data, no report jobs, no MCP tools, and no live compatibility claim.
</success_criteria>

<source_audit>
SOURCE | ID | Feature or requirement | Plan | Status | Notes
GOAL | — | Capability-gated Analytics transport without guest-payment data or accounting claims | 05-03 | COVERED | Restores the proof and exact-head evidence prerequisite; report tools remain later-slice work.
REQ | T5-001 | Separate Analytics capability and management-group location adapter | 05-03 | COVERED | Closes the matrix proof gap and resumes validation/review evidence.
RESEARCH | authority and isolation | Separate credentials, token, scope, registry, and identity state | 05-03 | COVERED | New config and capability mutations prove D-01 through D-03 guards.
RESEARCH | request privacy | Closed GET, no Standard header, guest exclusion, response minimization | 05-03 | COVERED | Existing and added route/privacy mutations retain D-04 through D-06 guards.
RESEARCH | selected set and limiter | Immutable publication, canonical selection, cancellation, isolated endpoint limiter | 05-03 | COVERED | Adds the missing publication, selection, and limiter proofs under D-07.
RESEARCH | evidence | Synthetic mutation tests, Node 20/22, package, exact-head review, no live claim | 05-03 | COVERED | Tasks 2 and 3 resume 05-02 evidence with a single-SHA rule.
CONTEXT | D-01 | Separate optional Analytics credentials and token manager | 05-03 | COVERED | Task 1 config proofs and Task 2 evidence.
CONTEXT | D-02 | Require Analytics scope before each request | 05-03 | COVERED | Task 1 Standard-scope and cross-identity proofs.
CONTEXT | D-03 | Separate Analytics location authority from Standard state | 05-03 | COVERED | Task 1 capability, registry, limiter, and runtime proofs.
CONTEXT | D-04 | Allow one literal no-header management-group GET | 05-03 | COVERED | Existing allowlist proofs remain required in Tasks 1 and 2.
CONTEXT | D-05 | Exclude guest-linked data at request layer | 05-03 | COVERED | Task 1 adds `analytics-guest-route` proof.
CONTEXT | D-06 | Keep Analytics operation type closed | 05-03 | COVERED | Task 1 route and runtime-boundary proofs.
CONTEXT | D-07 | Immutable registry and canonical selected-set validation | 05-03 | COVERED | Task 1 adds publication, UUID, canonicalization, and identity proofs.
CONTEXT | D-08 | Keep job lifecycle and policy with T5-002 | 05-03 | COVERED | Tasks 1 and 2 prohibit job lifecycle additions and retain ownership.
CONTEXT | D-09 | Keep MCP tools, reports, stdio wiring, and live compatibility outside T5-001 | 05-03 | COVERED | Task 1 adds the runtime-boundary mutation; Tasks 2 and 3 retain external gates.
</source_audit>

<output>
Create `.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-03-SUMMARY.md` when done.
</output>
