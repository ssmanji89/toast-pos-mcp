---
phase: 05-source-distinct-analytics-adapter-and-tools
plan: 08
type: tdd
wave: 8
depends_on: [05-07]
files_modified:
  - src/analytics-report-tools.ts
  - src/server.ts
  - test/analytics-report-tools-stdio.test.ts
  - test/fixtures/stdio-analytics-report-server.ts
  - test/support/analytics-report-tools-stdio-support.ts
  - scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs
  - docs/verification/t5-003-analytics-mcp-guard-matrix.md
autonomous: true
requirements: [T5-003]
must_haves:
  truths:
    - "A modern stdio MCP client discovers exactly one Analytics tool named toast_analytics_metrics_day."
    - "The tool accepts one Analytics-selected restaurant GUID and one explicit valid business date, then uses the process-owned Analytics runtime only."
    - "A missing capability or invalid Analytics selection produces a structured denied result before an Analytics report-job request starts."
    - "Every T5-002 terminal lifecycle outcome, including a retrieval HTTP 200, becomes a body-free structured incomplete or denied MCP result with safe Analytics provenance."
    - "The tool is read-only, source-labelled analytics_api, explicitly informational and non-GAAP, cancellable, and cannot request guest, payment, restaurant-name, grouping, or inactive-only data."
  artifacts:
    - path: src/analytics-report-tools.ts
      provides: "The closed toast_analytics_metrics_day input, lifecycle-to-MCP-envelope mapping, provenance projection, and safe denial/incomplete output schemas."
      exports: ["registerAnalyticsReportTools"]
    - path: src/server.ts
      provides: "Production MCP registration of the separate Analytics tool against the same ApplicationRuntime instance."
    - path: test/analytics-report-tools-stdio.test.ts
      provides: "Child-process stdio proof from MCP request through Analytics selection, capability, lifecycle, and safe result envelope."
    - path: scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs
      provides: "Compile-success-required semantic mutation proof for every T5-003 public-boundary guard."
  key_links:
    - from: src/server.ts
      to: src/analytics-report-tools.ts
      via: "createServer registers the distinct Analytics namespace with the supplied process-owned runtime"
      pattern: "registerAnalyticsReportTools"
    - from: src/analytics-report-tools.ts
      to: src/runtime.ts
      via: "handler reads only ApplicationRuntime.analyticsAccess and analyticsReportJobs, without Standard location or transport state"
      pattern: "analyticsAccess|analyticsReportJobs"
    - from: src/analytics-report-tools.ts
      to: src/analytics-report-jobs.ts
      via: "validated one-restaurant selection and fixed Metrics/day input call runReportJob with ctx.mcpReq.signal"
      pattern: "runReportJob|result_contract_unavailable"
    - from: test/analytics-report-tools-stdio.test.ts
      to: test/fixtures/stdio-analytics-report-server.ts
      via: "official MCP client invokes the compiled child process and asserts its recorded Analytics requests and response envelopes"
      pattern: "StdioClientTransport|toast_analytics_metrics_day"
---

<objective>
Expose one source-distinct Analytics Metrics-day MCP tool through the real stdio runtime while T5-003-G01 remains open.

Purpose: T5-003 owns the MCP presentation boundary in D-09. The current vendor result-shape conflict permits only safe capability, selection, lifecycle, and schema-gate denial or incomplete outcomes.
Output: One registered `toast_analytics_metrics_day` tool, synthetic stdio proof, an enumerated mutation harness, and a durable guard matrix. No successful Analytics result parser or complete report is part of this plan.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-CONTEXT.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-T5-002-CONTRACT-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-T5-003-CONTRACT-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-06-SUMMARY.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-07-PLAN.md
@src/analytics-access.ts
@src/analytics-report-jobs.ts
@src/runtime.ts
@src/server.ts
@src/report-tools.ts
@test/report-tools-e2e.test.ts
@test/fixtures/stdio-report-server.ts
@test/support/report-tools-e2e-support.ts

<interfaces>
From `src/runtime.ts`: `ApplicationRuntime` already exposes optional process-owned `analyticsAccess: AnalyticsAccessAdapter | undefined` and `analyticsReportJobs: AnalyticsReportJobAdapter | undefined`. The handler must use these exact objects. It must not recreate OAuth, configuration, Analytics identity, Standard location state, selection state, or a transport client.

From `src/analytics-access.ts`: `refreshManagementGroupRestaurants({ signal })` refreshes the separate Analytics registry, and `validateAnalyticsRestaurantSelection(access, restaurantGuids)` creates a private, canonical validated selection. T5-003 must retain the D-01 through D-07 separate authority model. Capability denial must occur before Analytics business-data access under D-02.

From `src/analytics-report-jobs.ts`: `runReportJob(selection, input, { signal })` accepts only a reviewed typed operation. A Metrics day call uses `operation: "metrics"`, `timeRange: "day"`, and equal start/end business-date tokens. Its current `AnalyticsReportJobLifecycleResult` is frozen, contains only body-free status/completeness/provenance data, and maps a retrieval HTTP 200 to `result_contract_unavailable`. Cancellation rejects with `AnalyticsReportJobError` instead of publishing a result.

From `src/server.ts`: `createServer({ runtime })` registers production report tools only when it receives the startup `ApplicationRuntime`. Add the Analytics registration to this path. Keep server construction with no runtime side-effect-free for protocol tests.

From `src/report-tools.ts`: Standard tools use Zod schemas, MCP `readOnlyHint` annotations, `ctx.mcpReq.signal`, and bounded text plus `structuredContent`. Implement an Analytics-specific registration module instead of widening the Standard report source/output schema.

From `test/support/report-tools-e2e-support.ts`: child-process integration tests use the official `@modelcontextprotocol/client` `StdioClientTransport`, the compiled fixture, and a modern 2026-07-28 client. Reuse this runtime proof pattern, but use a dedicated Analytics fixture so Standard source routes cannot become a test double for Analytics authority.
</interfaces>
</context>

<feature>
  <name>Body-free Analytics Metrics-day MCP lifecycle tool</name>
  <files>src/analytics-report-tools.ts, src/server.ts, test/analytics-report-tools-stdio.test.ts, test/fixtures/stdio-analytics-report-server.ts, test/support/analytics-report-tools-stdio-support.ts, scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs, docs/verification/t5-003-analytics-mcp-guard-matrix.md</files>
  <behavior>
    - A modern client discovers `toast_analytics_metrics_day` once. Its input requires one UUID restaurant GUID and a real calendar business date in numeric `YYYYMMDD` form. It accepts neither Standard default-location selection nor caller-selected operation, range, path, report identifier, grouping, or query flags.
    - The handler uses the startup runtime's separate Analytics adapter to refresh and validate the requested one-restaurant Analytics selection, then uses the same runtime's Analytics job adapter. Missing Analytics configuration, capability denial, an inaccessible selection, or a lifecycle denial returns a structured `denied` envelope before a prohibited job request can start.
    - The only job input has `metrics` plus `day` and identical start/end business-date tokens. The source fixture permits only the management-group discovery route followed by Metrics day POST and its matching retrieve GET. It rejects Standard headers, Standard routes, all guest or payment routes, restaurant-name retrieval, grouping, and inactive-only query construction.
    - Every lifecycle terminal state returns a structured body-free `incomplete` or `denied` envelope with `source: "analytics_api"`, `report: "analytics_metrics_day"`, the requested restaurant and business date, safe lifecycle provenance, fixed request-policy exclusions, and an informational/non-GAAP formula note. It never has `status: "complete"`, a row, a metric amount, a raw source body, a token, a raw error, or a report request GUID.
    - A completed retrieval is translated to `incomplete` with stable reason `analytics_result_schema_unverified`. This preserves T5-003-G01; the handler does not inspect, parse, cache, validate, or project the completed body.
    - Cancellation travels from the MCP request signal to the adapter and source fixture. The existing first-tool-request cancellation limitation remains T6-003 evidence work and this local nonzero-request test does not close it.
  </behavior>
  <implementation>Create a source-specific tool module and import it from `src/server.ts` per D-09. Preserve D-01 through D-03 by using only Analytics-owned runtime fields. Preserve D-04 through D-06 by retaining the allowlisted Analytics request path and by adding no generic path, no-header, guest, payment, or raw-body primitive. Obtain the existing canonical selection under D-07 and call the already-closed T5-002 lifecycle under D-08. Use the agent's discretion for exact Zod names and immutable envelope types, but retain the existing TypeScript, Zod, cancellation, secret-safe error, and MCP annotations patterns.

T5-003-G01 is binding. Do not add successful-result parsing, result-row types, exact-decimal conversion, source amount fields, a complete response schema, raw response retention, or a second Analytics tool. T5-002-G01 through G04 remain excluded or unresolved source concerns; this tool must not invoke payout-by-payment, inactive-only, disputed count, grouping, or restaurant-name behavior.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing public-envelope and stdio lifecycle tests</name>
  <files>test/analytics-report-tools-stdio.test.ts, test/fixtures/stdio-analytics-report-server.ts, test/support/analytics-report-tools-stdio-support.ts</files>
  <read_first>src/analytics-access.ts; src/analytics-report-jobs.ts; src/runtime.ts; src/server.ts; src/report-tools.ts; test/report-tools-e2e.test.ts; test/fixtures/stdio-report-server.ts; test/support/report-tools-e2e-support.ts; test/support/synthetic-runtime-env.ts</read_first>
  <behavior>
    - Test 1: A modern child-process client lists the one Analytics tool and rejects an omitted, malformed, multiple, or non-calendar restaurant/date request through the MCP schema before a fixture request exists.
    - Test 2: An absent Analytics runtime, missing `enterprise-metrics:read`, or an unknown Analytics restaurant returns a typed denied envelope. The fixture proves the missing-scope branch observes zero Analytics business-data requests, and the selection branch observes no Metrics job request.
    - Test 3: A valid synthetic selected restaurant invokes exactly the documented management-group discovery GET, `POST /era/v1/metrics/day`, and matching `GET /era/v1/metrics/{guid}`. The POST has one included GUID, an empty excluded list, equal date strings, and no group, aggregation, inactive, or restaurant-name setting.
    - Test 4: Synthetic pending exhaustion, expiry, replacement exhaustion, request failure, and retrieval HTTP 200 each produce only the documented denied or incomplete MCP envelopes. The 200 case asserts reason `analytics_result_schema_unverified` and proves no result field, raw body marker, report request identifier, token marker, guest marker, payment marker, name, grouping value, or amount enters content or structured output.
    - Test 5: The public envelope carries Analytics source/report labels, request restaurant/date, API-family lifecycle timestamps/counts/safe response request IDs, fixed request-policy exclusions, and an informational/non-GAAP note. It has no Standard source, timezone, closeout hour, currency, accounting, or tax claim.
    - Test 6: A nonzero modern MCP request cancellation aborts the synthetic Analytics source turn and rejects without publishing an MCP envelope. It records this as local handler-chain evidence only.
  </behavior>
  <action>Create the dedicated Analytics fixture, support helper, and focused child-process stdio test before source changes. Use the official test-only MCP client and compiled test fixture pattern. Use independently invented UUIDs, dates, JWT scope values, request IDs, and body markers.

The fixture must construct an Analytics-configured `ApplicationRuntime` through the production factory. It must use one separate Analytics identity and reject every route except D-04 management-group discovery and the fixed D-08 Metrics-day lifecycle routes. Record method, path, headers, query keys, and POST keys without logging token values or response bodies. Make the post/retrieve fixture return opaque test bodies; tests must prove that a retrieval HTTP 200 never needs inspection. Add exact fixture scenarios for absent Analytics configuration, missing scope, inaccessible selected GUID, terminal lifecycle states, result-contract gate, and cancellation.

Write expectations for a new exported `registerAnalyticsReportTools()` and the `toast_analytics_metrics_day` contract. Start with `npm run build:test` and record the RED result. Do not alter shared Standard fixtures, direct-report tests, T5-002 lifecycle behavior, or a source parser. This implements D-09 while preserving D-01 through D-08.</action>
  <verify>
    <automated>npm run build:test; test $? -ne 0</automated>
  </verify>
  <done>The focused stdio test names every public safe outcome and the compiled test build fails only because the planned Analytics registration module is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the fixed tool and map lifecycle outcomes without parsing results</name>
  <files>src/analytics-report-tools.ts, src/server.ts, test/analytics-report-tools-stdio.test.ts, test/fixtures/stdio-analytics-report-server.ts, test/support/analytics-report-tools-stdio-support.ts</files>
  <read_first>src/analytics-access.ts; src/analytics-report-jobs.ts; src/runtime.ts; src/server.ts; src/report-tools.ts; test/analytics-report-tools-stdio.test.ts; test/fixtures/stdio-analytics-report-server.ts</read_first>
  <action>Create `src/analytics-report-tools.ts` with `registerAnalyticsReportTools(server, runtime)`. Register exactly `toast_analytics_metrics_day`, use strict Zod input for one UUID and one real numeric `YYYYMMDD` date, mark it read-only/idempotent, and retain a separate Analytics output union for `denied` and `incomplete` only. The handler must check that both existing Analytics runtime fields exist, refresh the Analytics registry through its process-owned adapter, make a one-GUID canonical selection, and call `runReportJob()` with exactly Metrics/day and equal date tokens plus `ctx.mcpReq.signal`.

Map each existing lifecycle result to a frozen body-free MCP envelope. Keep selected restaurant and requested business date as local request identity. Copy only safe source/lifecycle provenance. Map `result_contract_unavailable` to `status: "incomplete"` and reason `analytics_result_schema_unverified`; map capability and selection outcomes to stable denied codes; map all other lifecycle terminal outcomes to explicit incomplete reasons. Include `source: "analytics_api"`, `report: "analytics_metrics_day"`, operation/time-range provenance, the fixed request-policy exclusions, and a single informational/non-GAAP statement. Keep cancellation as the existing sanitized rejection.

Update `src/server.ts` to register this module only for a supplied process-owned runtime. Keep Standard registration and no-runtime factory behavior unchanged. Do not edit `src/analytics-report-jobs.ts`, add a 200 parser, add result fields, use exact-decimal helpers, emit currency/timezone/closeout context, create a Standard fallback, register a second Analytics tool, or widen the closed Analytics request catalog. These limits preserve D-01 through D-09 and the binding T5-003-G01 gate.

Run the focused compiled stdio test until GREEN. Use the fixture log assertions to prove the complete production chain: stdio request, runtime identity, Analytics capability/selection, closed lifecycle, and structured MCP response.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-report-tools-stdio.test.js</automated>
  </verify>
  <done>A modern stdio client can call only the one fixed Analytics tool and receive safe denied or incomplete responses. No response is complete or contains source result data, raw data, credentials, guest/payment identifiers, or a report request GUID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove every public guard with semantic mutations and document the durable boundary</name>
  <files>scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs, docs/verification/t5-003-analytics-mcp-guard-matrix.md, test/analytics-report-tools-stdio.test.ts</files>
  <read_first>src/analytics-report-tools.ts; src/server.ts; test/analytics-report-tools-stdio.test.ts; test/fixtures/stdio-analytics-report-server.ts; scripts/verify-t5-002-analytics-job-guard-mutations.mjs; docs/verification/t5-002-analytics-job-guard-matrix.md</read_first>
  <action>Create an enumerated guard matrix and a temporary-work-area mutation script patterned after T5-002. For each mutation, require the modified source to compile and its named focused stdio test to fail. Restore the candidate after every mutation. The matrix must include one named guard for tool registration, required one-GUID/date input, runtime-only Analytics authority, preflight-before-business-data access, selection membership, closed Metrics/day input, equal business dates, route/method catalog, absence of Standard header, no grouping/inactive/name settings, source label, non-GAAP text, body-free provenance, 200 schema-gate mapping, no complete branch, guest/payment exclusion, report-GUID exclusion, and cancellation propagation.

Use semantic mutations that would otherwise compile, such as removing the capability branch, changing the fixed operation/range, accepting multiple IDs, exposing a result property, changing the schema-gate reason, or omitting the cancellation signal. Do not use a TypeScript error as mutation proof. The harness must use only invented fixture data, never real credentials or Toast access, and must leave no modified candidate after execution.

Update the nearest verification document with the public tool contract, G01 boundary, each guard-to-test-to-mutation link, exact focused commands, and `DOX: updated`. State all external gates precisely: T5-003-G01 requires a current corrected Toast OpenAPI or written confirmation of retrieval top-level shape/cardinality; T5-002-G01/G02/G03/G04 remain vendor contracts that this plan avoids; owner-authorized live Analytics compatibility requires documented Merchant consent and separate review; T6-003 first-tool-request cancellation remains a release gate; #28 and T6 signing/publication remain release gates but do not block this local Analytics implementation. Do not claim that synthetic evidence closes any external gate.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-report-tools-stdio.test.js && node scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs</automated>
  </verify>
  <done>Each named T5-003 safety property has a compiling semantic mutation and focused child-process proof. The durable guard matrix separates local synthetic implementation evidence from unresolved vendor, consent, cancellation, compatibility, signing, and publication gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → Analytics tool | Untrusted tool input becomes a single selected restaurant and business-date request. |
| Runtime → Analytics authority | The handler obtains only the existing private Analytics identity, registry, selection, token scope, and lifecycle adapter. |
| Analytics lifecycle → MCP envelope | Body-free internal terminal data becomes a source-labelled public denial or incomplete result. |
| MCP cancellation → Analytics source | Client cancellation crosses the handler and closed lifecycle to stop an in-flight source request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5-003-01 | Elevation of privilege | analytics report handler | mitigate | Use the runtime-owned separate Analytics authority and reassert capability before source turns per D-01 through D-03. |
| T5-003-02 | Tampering | tool input and selection | mitigate | Require one UUID and calendar business date, refresh the Analytics registry, and use canonical selected-set validation per D-07. |
| T5-003-03 | Information disclosure | lifecycle response | mitigate | Map only body-free lifecycle/provenance fields, omit result rows/report IDs/tokens/raw errors, and retain G01 incompleteness. |
| T5-003-04 | Information disclosure | source operation catalog | mitigate | Fix Metrics/day input and retain D-04 through D-06 closed routes; prohibit guest, payment, grouping, restaurant-name, and inactive-only paths. |
| T5-003-05 | Denial of service | cancelled lifecycle | mitigate | Pass `ctx.mcpReq.signal` through selection and job calls, then prove source abort with a modern stdio request. |
| T5-003-06 | Repudiation | public output | mitigate | Return immutable source/API/lifecycle provenance and fixed request-policy exclusions without fabricating completeness. |
| T5-003-SC | Tampering | npm dependency supply chain | accept | This plan adds no package. The exact-head clean-install and package gates remain delivery evidence. |
</threat_model>

<source_coverage_audit>

SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | Phase 5 | Capability-gated source-distinct Analytics reporting without guest-payment data or accounting claims | 05-08 | COVERED | One read-only Analytics namespace tool returns source-labelled non-GAAP denied/incomplete results.
REQ | T5-003 | Source-distinct Analytics reporting tools excluding guest-payment datasets | 05-08 | COVERED | One fixed Metrics-day tool only; all guest/payment data stays outside the request catalog.
RESEARCH | T5-003-G01 | Conflicting completed-result shape blocks parser and complete report | 05-08 | COVERED | Retrieval HTTP 200 maps to body-free incomplete schema-gate result.
RESEARCH | Fixed Metrics/day request | One selected restaurant, business date, no grouping/name/inactive query | 05-08 | COVERED | Handler and fixture assertions lock the request shape.
RESEARCH | Production wiring | stdio → runtime → selection/capability → lifecycle → MCP result | 05-08 | COVERED | Dedicated official-client child-process tests prove this path.
RESEARCH | External evidence | Live compatibility, consent, and first-request cancellation remain release evidence | 05-08 | COVERED | Guard matrix records owners and does not turn fixture tests into live claims.
CONTEXT | D-01 | Separate Analytics credentials and token manager | 05-08 | COVERED | Handler uses only runtime Analytics-owned objects.
CONTEXT | D-02 | Scope preflight before Analytics data | 05-08 | COVERED | Missing scope test proves zero business-data requests.
CONTEXT | D-03 | Separate management-group authority | 05-08 | COVERED | No Standard location/default fallback is accepted.
CONTEXT | D-04 | Literal management-group discovery GET | 05-08 | COVERED | Fixture allows and asserts only the established discovery route before selection.
CONTEXT | D-05 | Guest-linked data excluded | 05-08 | COVERED | Closed fixture, output checks, and mutations exclude guest/payment data.
CONTEXT | D-06 | Closed Analytics request type | 05-08 | COVERED | Tool exposes no caller-controlled route, method, or operation.
CONTEXT | D-07 | Immutable registry and canonical selection | 05-08 | COVERED | Existing selection validation is used and tested from stdio.
CONTEXT | D-08 | T5-002 lifecycle owns create/retrieve/poll/replacement/limits | 05-08 | COVERED | Tool delegates to runReportJob without changing lifecycle source code.
CONTEXT | D-09 | T5-003 owns MCP tools and stdio wiring | 05-08 | COVERED | Server registration and child-process tests are this plan's primary output.

</source_coverage_audit>

<verification>
Run the RED build before implementation. Then run the focused compiled child-process stdio suite after each source change. Run the semantic mutation harness after every public-boundary guard change. Before review, run authentic clean dependency restoration, `npm run check`, focused stdio suite, mutation harness, and `npm pack --dry-run --json` on Node 20.20.2 and Node 22.22.2 for one unchanged candidate SHA. Independent review must assess that exact SHA.
</verification>

<success_criteria>
- The production stdio path exposes exactly one `toast_analytics_metrics_day` tool on the supplied runtime.
- The tool invokes only existing separate Analytics authority and closed Metrics/day lifecycle code, then returns source-distinct safe envelopes.
- Retrieval HTTP 200 cannot produce a parser call, a public row, an amount, a complete status, or a report-request identifier while T5-003-G01 is open.
- Local tests and mutations prove wiring and safety guards. They do not prove live Toast compatibility, Merchant consent, external vendor contracts, first-request cancellation, signing, or publication.
</success_criteria>

<output>
Create `.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-08-SUMMARY.md` when done.
</output>
