---
phase: 05-source-distinct-analytics-adapter-and-tools
plan: 04
type: execute
wave: 4
depends_on: [05-03]
research_dependencies:
  - 05-T5-002-CONTRACT-RESEARCH.md
files_modified:
  - src/analytics-report-jobs.ts
  - test/analytics-report-jobs.test.ts
autonomous: true
requirements: [T5-002]
must_haves:
  truths:
    - "A selected Analytics restaurant set can create and retrieve only one of six explicit read-only Analytics report-job operations."
    - "An accepted create response retains a bounded opaque report request identifier without asserting an unresolved UUID format."
    - "A caller cannot construct an arbitrary Analytics path, method, query, time range, location set, or non-allowlisted report operation."
    - "No completed report body or source result shape is modeled, retained, or presented while G05 remains open."
  artifacts:
    - path: src/analytics-report-jobs.ts
      provides: "Closed six-operation Analytics request catalog, validated create-input contracts, opaque request-identifier contract, and body-free retrieval status classifier."
      exports: ["AnalyticsReportJobAdapter", "createAnalyticsReportJobAdapter", "AnalyticsReportJobError"]
    - path: test/analytics-report-jobs.test.ts
      provides: "Synthetic RED-to-GREEN tests for the six-operation allow-list, identity-bound selection, unresolved-contract gates, and rejected outbound request forms."
  key_links:
    - from: src/analytics-report-jobs.ts
      to: src/analytics-access.ts
      via: "The job adapter requires AnalyticsAccessAdapter.assertSelectionForCurrentIdentity before URL, body, or limiter-key construction."
      pattern: "assertSelectionForCurrentIdentity"
---

<objective>
Define and prove the closed outbound wire contracts for T5-002 Analytics report jobs.

Purpose: The lifecycle can create and retrieve only reviewed request forms while it preserves every unresolved upstream contract as an explicit gate.
Output: A six-operation catalog, typed create requests, opaque request identifiers, body-free status classification, and synthetic contract tests.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-CONTEXT.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-T5-002-CONTRACT-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-01-SUMMARY.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-03-PLAN.md
@src/analytics-access.ts
@src/runtime.ts
@test/analytics-access-adapter.test.ts
</context>

<interfaces>
`src/analytics-access.ts` exports `AnalyticsAccessAdapter`, `AnalyticsRestaurantSelection`, and `assertSelectionForCurrentIdentity(selection)`. The job adapter must call that assertion before it creates a request or computes a limiter key. The selected set is an opaque private-identity capability and must not be recreated from public JSON.

This plan depends on `05-T5-002-CONTRACT-RESEARCH.md`. Its G01 through G05 gates are binding: implement only the six documented outbound operations; omit unresolved request options and result fields; retain no completed response body; and do not claim a source-result schema is known.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify failing six-operation outbound contract tests</name>
  <files>test/analytics-report-jobs.test.ts, src/analytics-report-jobs.ts</files>
  <behavior>
    - Test 1: The catalog contains exactly metrics, check, labor, menu, payout-by-settled-date, and payout-by-sales-date operations; each has one literal POST create route, one literal GET route, and its documented time-range forms.
    - Test 2: Each request accepts only its documented selected restaurant set, dates, and range form. The request body always uses the canonical `restaurantIds` set and an empty `excludedRestaurantIds` list.
    - Test 3: A forged or foreign selection, caller-supplied route, alternate method, unsupported range, non-empty exclusion list, inactive-restaurant query, unsupported metrics aggregation query, and employee grouping fail before a source request starts.
    - Test 4: A create response accepts a bounded non-empty opaque string identifier. A 200 retrieval response is classified without parsing or retaining its body, and 202, 404, and 409 are classified body-free.
  </behavior>
  <action>Work test-first. Create `test/analytics-report-jobs.test.ts` with invented UUIDs, names, dates, values, and token markers. Write each test against imports that do not yet exist in `src/analytics-report-jobs.ts`; run the focused compiled test and record its RED failure before implementation. The test harness must use an adapter from the current Analytics identity, mint a canonical selection from its validated registry, and verify that a second identity and a forged public-shape selection are rejected through `assertSelectionForCurrentIdentity` per D-01, D-03, and D-07.

Cover exactly the six reviewed Analytics reporting families named in the behavior block. Treat the operation identifier, POST route, GET route, method pair, date fields, and allowed range form as a closed discriminated union. Do not add an option for inactive restaurants, metrics hourly aggregation, employee grouping, arbitrary query parameters, or a caller-defined body. Per G01 and D-05, do not add another report family, a payment-level route, a guest-linked field, fixture, schema, test value, or limiter entry. Per G02 and G04, do not infer a restaurant-status mapping or request/query behavior from unresolved documentation. Per G03, test an opaque documented string identifier rather than a UUID-only response rule. Per G05, do not create a completed-result schema, result-row fixture, field projection, or 200 compatibility assertion. Do not add a report tool, server registration, stdio fixture, report calculation, or presentation output; those remain T5-003 under D-09.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-report-jobs.test.js</automated>
  </verify>
  <done>The focused test file fails before implementation and specifies the six-operation allow-list, outbound validation, identity-bound selection, opaque create identifier, and body-free status behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the closed outbound catalog and gated retrieval contract</name>
  <files>src/analytics-report-jobs.ts, test/analytics-report-jobs.test.ts</files>
  <behavior>
    - Test 1: A valid typed request produces only its documented create and retrieve URLs and sends Analytics bearer authorization without a Standard restaurant header.
    - Test 2: A valid selected set and request create an immutable internal job descriptor with canonical location members, immutable request metadata, and no enumerable credential or identity field.
    - Test 3: Invalid create payloads and malformed create identifiers return a secret-safe typed error and leave no published job descriptor.
    - Test 4: Retrieval status classification retains no response body or result field while G05 is open.
  </behavior>
  <action>Create `src/analytics-report-jobs.ts` as the T5-002-owned closed adapter. Export typed outbound input and lifecycle-status types only where T5-003 will need them after the documented gates close. Keep route construction private. Use a discriminated operation catalog for the six reviewed reporting families, with literal endpoint templates and typed range unions. Each definition must include its validated create-input contract and no unreviewed query or grouping option.

Construct an `AnalyticsReportJobAdapter` with the existing separate Analytics access adapter, existing separate Analytics token manager, private Analytics config identity, injectable fetch/clock/sleep seams, and no Standard client or location input. Require `access.assertSelectionForCurrentIdentity(selection)` before any URL, body, or limiter data can exist. Send only bearer authorization for the Analytics token. Do not add a generic no-header request primitive, generic URL argument, arbitrary body, Standard restaurant header, credential persistence, raw-body retention, or error-message interpolation. Freeze the accepted job descriptor and expose no identity, token, hostname, or credential-derived value. This implements D-01 through D-08 while preserving D-09's no-MCP boundary.

Treat a create response identifier as a bounded opaque documented string and encode it only within the fixed retrieval template. Implement a named body-free retrieval-status classifier for 200, 202, 404, 409, and other responses. Per G05, a 200 must not parse, retain, project, or claim compatibility for a result body; Plan 05-05 must return an explicit result-contract-unavailable state. Per G03 and G04, do not add UUID-only response parsing or disputed metrics count fields. Keep the lifecycle policy for Plan 05-05; this task only establishes the gated contracts that it consumes.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-report-jobs.test.js</automated>
  </verify>
  <done>The adapter accepts only six reviewed outbound operations, binds requests to the private Analytics identity and canonical selected set, retains an opaque create identifier, and passes the focused compiled suite without modeling unresolved response contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| T5-003 caller → job adapter | Typed input can request only six reviewed read-only Analytics job operations. |
| selected restaurant set → job descriptor | The private Analytics identity must prove location-set authority before request construction. |
| Toast JSON → internal state | The adapter retains only a validated opaque create identifier and body-free status while G03-G05 remain open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5-002-04-01 | Elevation of privilege | operation catalog | mitigate | Closed six-operation union excludes caller-defined paths, methods, headers, query options, and non-allowlisted operations. |
| T5-002-04-02 | Information disclosure | response handling | mitigate | Retain no completed response body, raw error, credentials, tokens, or guest-linked field while G03-G05 are open. |
| T5-002-04-03 | Spoofing | selected set | mitigate | Require the current adapter's private identity assertion before request construction. |
| T5-002-04-04 | Tampering | unresolved vendor contracts | mitigate | Keep G01-G05 as explicit state gates; outbound contracts do not model unproven fields, shapes, options, or routes. |
| T5-002-04-SC | Tampering | npm dependency supply chain | mitigate | Use the existing lockfile and authentic `npm ci --no-audit --no-fund` during candidate validation. |
</threat_model>

<verification>
Run the focused compiled job test after the RED commit and after each catalog or create-contract change. Verify that every rejected input observes zero source requests and that no retrieval classifier retains a body.
</verification>

<success_criteria>
- The catalog contains only the six reviewed outbound operations and no caller-defined transport primitive.
- Every accepted create response retains a bounded opaque request identifier without an unsupported UUID claim.
- G01-G05 remain explicit gates; the adapter models no disputed option, field, top-level response shape, or completed result.
- A forged, foreign, empty, or non-canonical selected set cannot reach request construction.
</success_criteria>

<output>
Create `.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-04-SUMMARY.md` when done.
</output>
