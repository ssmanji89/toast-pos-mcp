---
phase: 05-source-distinct-analytics-adapter-and-tools
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified: []
autonomous: false
requirements: [T5-002]
must_haves:
  truths:
    - "One immutable non-main candidate has authentic Node 20.20.2 and Node 22.22.2 clean-install, test, mutation, and package evidence."
    - "An independent reviewer evaluates the same exact candidate SHA and does not self-approve builder work."
    - "The evidence distinguishes synthetic lifecycle validation from live Analytics compatibility, retains T5-003 MCP ownership, and records G01-G05 as unresolved gates."
  artifacts:
    - path: "External T5-002 pull request"
      provides: "Candidate SHA, supported-runtime evidence, mutation evidence, DOX result, independent review record, and G01-G05 gate status."
  key_links:
    - from: "candidate branch"
      to: "T5-002 pull request"
      via: "The pushed branch and pull request name the immutable SHA before validation or review."
      pattern: "CANDIDATE_SHA"
---

<objective>
Create exact-head T5-002 validation and independent-review evidence without extending scope.

Purpose: Synthetic job lifecycle code is not complete evidence until supported runtimes, package contents, mutations, and a separate reviewer inspect one immutable candidate.
Output: A pushed candidate branch, owning pull request evidence, and independent review result that preserves all unresolved contract gates.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-T5-002-CONTRACT-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-04-SUMMARY.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-05-SUMMARY.md
@docs/verification/t5-002-analytics-job-guard-matrix.md
@scripts/verify-t5-002-analytics-job-guard-mutations.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push one final candidate and record authentic supported-runtime evidence</name>
  <files>External T5-002 candidate branch and pull-request evidence only</files>
  <action>After Plan 05-05 is committed, require an empty `git status --short`, select `CANDIDATE_SHA` from the full `git rev-parse HEAD`, push the named non-main candidate branch, and create the owning T5-002 pull request to `main` before running validation or requesting review. The initial pull-request body must name the candidate branch and SHA, state the closed six-operation job-lifecycle scope, state that T5-003 retains MCP registration and presentation, and retain live Analytics compatibility, Merchant consent, first-tool-request cancellation, signing, publication, and install smoke as external gates. It must also list G01 through G05 as unresolved: no additional report family, inactive-status option, UUID-only source-response assertion, disputed metrics count, or completed-result top-level schema is validated by this slice.

Run authentic clean dependency restoration and the full validation sequence on that one candidate under Node 20.20.2 with its paired npm, then repeat under Node 22.22.2 with its paired npm. In each pull-request evidence record, capture the SHA immediately before and after: `npm ci --no-audit --no-fund`, `npm run check`, `npm run build:test`, the focused compiled job test, the T5-002 mutation harness, and `npm pack --dry-run --json`. Record Node version, npm version, test discovery count, test total, package file count, matrix guard count, caught mutation count, survivor count, and `DOX: updated`. Do not use package caches, fabricated lockfiles, reconstructed results, real Merchant Data, or live Toast credentials.

If the branch head, tracked source, or restoration diff changes after the candidate is selected, invalidate all evidence, commit the correction, select a new exact SHA, push it, update the owning pull request, and rerun both runtime sequences. Do not make post-candidate source changes while validation or review is pending.</action>
  <verify>
    <automated>test "$(git branch --show-current)" != main && git ls-remote --exit-code --heads origin "$(git branch --show-current)" && gh pr view --json url,headRefName,baseRefName && nvm use 20.20.2 && test "$(node --version)" = "v20.20.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/analytics-report-jobs.test.js && node scripts/verify-t5-002-analytics-job-guard-mutations.mjs && npm pack --dry-run --json && nvm use 22.22.2 && test "$(node --version)" = "v22.22.2" && npm ci --no-audit --no-fund && npm run check && npm run build:test && node --test dist-test/test/analytics-report-jobs.test.js && node scripts/verify-t5-002-analytics-job-guard-mutations.mjs && npm pack --dry-run --json</automated>
  </verify>
  <done>A pushed non-main candidate branch and T5-002 pull request exist before validation. Both supported Node runs prove the same unchanged SHA, all matrix mutations are caught, no source restoration diff remains, package validation succeeds, and G01-G05 remain explicitly open.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Record an independent exact-head review</name>
  <files>External T5-002 pull-request review evidence only</files>
  <what-built>A closed, bounded, cancellation-aware six-operation Analytics report-job lifecycle with supported-runtime, package, mutation, and unresolved-contract gate evidence for one candidate SHA.</what-built>
  <how-to-verify>
    1. Open the owning T5-002 pull request.
    2. Confirm the reviewed SHA equals the SHA in both Node evidence records.
    3. Confirm every named mutation is caught and source restoration is clean.
    4. Confirm G01-G05 remain open and the implementation has no non-allowlisted operation, MCP registration, stdio fixture, completed-result shape claim, or live-compatibility claim.
    5. Record the reviewer identity, reviewed SHA, and result in the pull request.
  </how-to-verify>
  <action>Request an independent review only after Task 1 has pushed the candidate branch, created the pull request, and recorded both exact-head validation sequences. The builder must not self-approve. The reviewer must inspect the six-operation catalog, selection-identity assertion, local 202 limits, invalid-or-expired 404 classification, local 409 replacement budget, limiter-key dimensions, cancellation points, body-free result handling, provenance/completeness, D-05 request-layer exclusion, G01-G05 gate retention, and the no-MCP T5-003 boundary. If the reviewed SHA differs or a required finding exists, invalidate candidate evidence, correct in a new commit, create one new candidate SHA, and repeat Task 1 before requesting a replacement review.</action>
  <verify>
    <automated>git status --short && git rev-parse --verify HEAD</automated>
  </verify>
  <resume-signal>Provide the independent review result and exact reviewed SHA, or provide the required findings.</resume-signal>
  <done>An independent reviewer records a result for the exact candidate SHA used by both supported Node validation runs. The pull request distinguishes synthetic validation, review, production wiring status, G01-G05 gate status, and unresolved external gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate source → validation evidence | Runtime, package, and mutation results must apply to one immutable commit. |
| builder → reviewer | A separate person or agent reviews the candidate without builder self-approval. |
| synthetic evidence → release claim | Local fixtures prove implementation behavior only and cannot prove authorized live Analytics compatibility or close G01-G05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5-002-06-01 | Tampering | candidate source | mitigate | Capture before-and-after SHA values on both supported runtime sequences and fail on source restoration diff. |
| T5-002-06-02 | Repudiation | pull-request evidence | mitigate | Push the branch and create the owning pull request before validation, then record commands, counts, SHA, DOX, gate status, and review. |
| T5-002-06-03 | Elevation of privilege | unresolved vendor contract | mitigate | Require evidence to list G01-G05 as open and reject any claim that validation proves a blocked source shape or option. |
| T5-002-06-04 | Elevation of privilege | release claim | mitigate | State T5-003 ownership and all live, consent, and release gates explicitly in evidence. |
| T5-002-06-SC | Tampering | dependency supply chain | mitigate | Use authentic registry-backed `npm ci --no-audit --no-fund` for Node 20 and Node 22. |
</threat_model>

<verification>
The final evidence requires two supported-runtime records, one unchanged candidate SHA, a complete mutation matrix with no survivor, a successful package dry run, a separate exact-head review, and a written G01-G05 open-gate record.
</verification>

<success_criteria>
- The candidate branch and owning pull request exist before runtime validation or review.
- Node 20.20.2 and Node 22.22.2 validate the same exact head.
- Mutation evidence proves every documented lifecycle and G01-G05 guard and restores source after each temporary change.
- No evidence claims an MCP tool, live Analytics request, T5-003 work, or unresolved source response contract.
</success_criteria>

<source_audit>
SOURCE | ID | Feature or requirement | Plan | Status | Notes
GOAL | — | Capability-gated Analytics job transport and source-distinct reporting | 05-04, 05-05, 05-06 | COVERED | T5-002 supplies only the internal lifecycle; T5-003 owns presentation.
REQ | T5-002 | Create/retrieve lifecycle, bounded polling, invalid-or-expired handling, replacement, and endpoint/time-range limiter policy | 05-04, 05-05, 05-06 | COVERED | Contracts, lifecycle controls, then exact-head evidence.
RESEARCH | closed catalog | Six documented outbound operation pairs | 05-04, 05-05 | COVERED | The catalog is exact and has no additional family or generic transport.
RESEARCH | status and local policy | 202 polling, invalid-or-expired 404, 409 replacement, and locally chosen safety budgets | 05-05 | COVERED | Bounds are labeled local product policy, not Toast facts.
RESEARCH | rate limits | Endpoint, method, and time-range limits remain Analytics-specific | 05-05 | COVERED | Per-operation limiter policy includes create and retrieve turns.
RESEARCH | privacy and authority | Separate identity/token/location state; request-layer guest exclusion | 05-04, 05-05 | COVERED | Closed catalog and private selected-set assertion enforce D-01 through D-07.
RESEARCH | G01 | Additional payment-level report operation | 05-04, 05-05, 05-06 | COVERED | Explicitly omitted and retained as an open external gate.
RESEARCH | G02 | Inactive-restaurant status mapping | 05-04, 05-05, 05-06 | COVERED | Option is absent and gate remains open.
RESEARCH | G03 | UUID-format source create response | 05-04, 05-05, 05-06 | COVERED | Identifier is opaque; UUID-only assertion remains gated.
RESEARCH | G04 | Metrics open/closed count naming and type | 05-04, 05-05, 05-06 | COVERED | Disputed fields are unmodeled and gate remains open.
RESEARCH | G05 | Retrieval top-level result shape | 05-04, 05-05, 05-06 | COVERED | 200 handling is body-free and no result schema is claimed.
RESEARCH | evidence | Synthetic only; Node 20/22, package, mutation, exact-head review; no live claim | 05-06 | COVERED | Candidate evidence remains external to source fixtures.
CONTEXT | D-01 | Separate Analytics credentials and token manager | 05-04, 05-05 | COVERED | Jobs reuse the Analytics-only token owner and private config identity.
CONTEXT | D-02 | Scope preflight before every Analytics request | 05-05 | COVERED | Every source turn uses the Analytics capability boundary.
CONTEXT | D-03 | Analytics authority remains separate from Standard locations/scopes | 05-04, 05-05 | COVERED | Selection and limiter keys never use Standard state.
CONTEXT | D-04 | Existing literal management-group GET remains limited | 05-05 | COVERED | Discovery remains separate from job operation routes.
CONTEXT | D-05 | Exclude guest-linked data at request layer | 05-04, 05-05, 05-06 | COVERED | The exact allow-list prevents non-reviewed source families and fields.
CONTEXT | D-06 | Keep Analytics operations closed | 05-04, 05-05 | COVERED | Discriminated catalog forbids arbitrary methods, paths, queries, and bodies.
CONTEXT | D-07 | Use immutable registry and canonical selected-set validation | 05-04, 05-05 | COVERED | Pre-job private identity assertion is mandatory.
CONTEXT | D-08 | T5-002 owns job lifecycle and limiter policy | 05-04, 05-05, 05-06 | COVERED | All lifecycle and evidence work is in this plan set.
CONTEXT | D-09 | T5-003 owns MCP tools, reports, and stdio wiring | 05-04, 05-05, 05-06 | COVERED | Plans prohibit registration and retain the ownership boundary.
</source_audit>

<output>
Create `.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-06-SUMMARY.md` when done.
</output>
