---
phase: 05-source-distinct-analytics-adapter-and-tools
plan: 07
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - src/analytics-report-jobs.ts
  - test/analytics-report-jobs.test.ts
  - scripts/verify-t5-002-analytics-job-guard-mutations.mjs
  - docs/verification/t5-002-analytics-job-guard-matrix.md
autonomous: false
requirements: [T5-002]
must_haves:
  truths:
    - "Every valid Analytics lifecycle invocation returns one immutable typed non-success envelope for capability denial or any source-request failure; cancellation remains a sanitized rejection."
    - "Each of the six closed Analytics operations enforces every documented concurrent rate-limit window for its method and range, and retries a 429 only within a finite local policy."
    - "Each named lifecycle, limiter, retry, privacy, and G01-G05 guard has a compiling semantic source mutation that fails its named focused behavioral test."
    - "One new T5-002 candidate SHA has fresh Node 20.20.2, Node 22.22.2, package, mutation, and independent-review evidence; the rejected SHA has no remaining evidentiary force."
  artifacts:
    - path: src/analytics-report-jobs.ts
      provides: "Typed lifecycle failure mapping, documented multi-window Analytics limits, and bounded 429 retry turns."
      exports: ["AnalyticsReportJobAdapter", "AnalyticsReportJobLifecycleResult"]
    - path: test/analytics-report-jobs.test.ts
      provides: "Focused behavioral proof for lifecycle envelopes, limiter windows, 429 retries, and closed Analytics boundaries."
    - path: scripts/verify-t5-002-analytics-job-guard-mutations.mjs
      provides: "Compile-success-required semantic mutation harness that invokes each named focused test."
    - path: docs/verification/t5-002-analytics-job-guard-matrix.md
      provides: "Guard-to-test-to-semantic-mutation evidence map and open G01-G05 record."
  key_links:
    - from: src/analytics-report-jobs.ts
      to: test/analytics-report-jobs.test.ts
      via: "Every capability/request terminal state and every rate-limit or 429 retry transition has a named behavioral test."
      pattern: "runReportJob|429|capability_denied|failed_or_incomplete"
    - from: scripts/verify-t5-002-analytics-job-guard-mutations.mjs
      to: test/analytics-report-jobs.test.ts
      via: "Each semantic mutation must compile and then make its matrix-named test fail."
      pattern: "test-name-pattern|build:test"
---

<objective>
Repair the T5-002 exact-head review findings without expanding the Analytics source surface.

Purpose: PR #50 rejected candidate `ab63141ef12658d94702e6fbf0314cdf1bd14343` because it did not consistently publish typed lifecycle failure envelopes, dropped documented limiter windows, did not retry 429 responses correctly, and used compile failures as mutation evidence.
Output: A repaired internal six-operation Analytics lifecycle, guard-specific behavioral mutation proof, and fresh exact-head candidate and review evidence for PR #50.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-CONTEXT.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-T5-002-CONTRACT-RESEARCH.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-05-SUMMARY.md
@.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-06-PLAN.md
@src/analytics-report-jobs.ts
@test/analytics-report-jobs.test.ts
@scripts/verify-t5-002-analytics-job-guard-mutations.mjs
@docs/verification/t5-002-analytics-job-guard-matrix.md
@https://github.com/ssmanji89/toast-pos-mcp/pull/50#issuecomment-5446735189
</context>

<interfaces>
`AnalyticsReportJobAdapter.runReportJob(selection, input, { signal })` is the internal T5-002 lifecycle boundary. Its accepted selection and typed input have passed the closed route and range validation. It must return a frozen `AnalyticsReportJobLifecycleResult` for capability denial and all source-request failures after that point. A preflight input or foreign-selection contract error remains a typed `AnalyticsReportJobError`, and an aborted invocation remains the existing sanitized cancellation rejection.

The rate contract is fixed by `05-T5-002-CONTRACT-RESEARCH.md`: retrieval is 5/second and 30/minute; metrics and menu custom/month/year creation is 10/hour; metrics and menu day/week creation is 10/minute and 60/hour; check creation is 5/minute and 60/day; labor and both payout creation routes use 10/hour for month and 10/minute plus 60/hour for day/week. Each source turn, including replacement POSTs, polling GETs, and a 429 retry, reasserts capability and consumes all applicable buckets for its private identity, operation, method, time range, and canonical selected restaurant set.

`T5-002` owns this internal lifecycle per D-08. The public result contract stays blocked by G05. T5-003 alone owns MCP tool registration, report presentation, and stdio proof per D-09.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair lifecycle failure envelopes, multi-window limits, and bounded 429 retries</name>
  <files>src/analytics-report-jobs.ts, test/analytics-report-jobs.test.ts</files>
  <behavior>
    - Test 1: After valid selection and input, an `enterprise-metrics:read` denial returns a frozen `capability_denied` lifecycle envelope with denied completeness, safe provenance, zero fetches, and no raw scope or token data.
    - Test 2: Token/capability acquisition failure, initial POST failure, poll GET failure, and replacement POST failure each return the same typed failed-or-incomplete lifecycle envelope with correct poll and replacement counts; no failure leaks a source body or raw error.
    - Test 3: A create operation cannot start when any applicable documented window is exhausted. A controlled clock proves 5/second plus 30/minute retrieval limits, 10/minute plus 60/hour day/week creation, 10/hour long-range/month creation, and 5/minute plus 60/day check creation independently.
    - Test 4: A 429 on initial POST or polling GET rechecks capability and all limiter buckets, waits by a bounded local policy that honors a valid retry header, falls back to bounded exponential backoff with jitter when the header is absent or unusable, and retries only within the named retry budget.
    - Test 5: Repeated 429s end as a typed failed-or-incomplete envelope. A 401, 403, validation failure, capability denial, cancellation, or exhausted limiter does not start a retry. A cancelled 429 wait starts no later source turn or envelope publication.
    - Test 6: The six reviewed operation pairs remain the only Analytics job routes. G01-G05 remain open, no guest-linked route or field enters the request or test fixture, and no server, report-tool, or stdio path is added.
  </behavior>
  <action>Work test-first in `test/analytics-report-jobs.test.ts`. Add deterministic controlled clock, sleep, and random seams to exercise every listed terminal transition before changing `src/analytics-report-jobs.ts`. Run the focused compiled test and record its RED result. Use invented identifiers, restaurant GUIDs, dates, response headers, and token markers only.

In `src/analytics-report-jobs.ts`, make the lifecycle mapping total after selection and input validation. Add a distinct internal capability-denied error or decision branch, then map it to the frozen denied envelope. Map every non-cancellation capability acquisition, token acquisition, initial create POST, polling GET, replacement POST, rate-limit wait, and retry-exhaustion failure to the existing body-free failed-or-incomplete envelope. Preserve the typed input and foreign-descriptor failures before lifecycle work. Preserve sanitized cancellation rejection and prevent later polling, replacement, retry, or envelope publication after abort. Every envelope must retain only the established API family, operation, range, canonical selected GUIDs, injected timestamps, safe request IDs, poll count, replacement count, and completeness reason.

Replace the single-window `rateLimitsFor` result with immutable per-operation budget arrays. Atomically check every applicable window before adding a turn to every bucket. Calculate the next wait from the maximum required bucket delay, retain private `WeakMap` identity isolation, and keep management-group discovery limiter state separate. Implement all documented budgets in the interface section, including both retrieval windows. Every replacement and retry is a new source turn that repeats selected-set assertion and D-02 capability preflight. Use only the separate Analytics identity, token manager, hostname, and limiter state per D-01 and D-03. Do not modify the literal discovery operation from D-04, the closed request catalog from D-06, or the identity-bound selection proof from D-07.

Add named local retry constants and document them as local safety policy, not Toast values. Retry only HTTP 429. Read a valid `Retry-After` value first; otherwise calculate a capped exponential backoff with injected jitter. Bound retry count and wait duration. Do not parse an error body, retain raw headers other than a safe request identifier, retry authorization or validation failures, or create a generic transport primitive. Preserve D-05 by keeping all guest-linked paths and fields absent. Preserve G01-G05 and D-09 by adding no report data model, MCP tool, server registration, stdio fixture, or result presentation.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-report-jobs.test.js && npm run check</automated>
  </verify>
  <done>Valid lifecycle calls return one typed envelope for every capability or request failure, cancellation remains sanitized and terminal, every documented simultaneous limiter window is enforced, 429 retries are finite and safe, and the six-operation internal-only boundary remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace compile-break mutations with guard-specific behavioral proofs</name>
  <files>test/analytics-report-jobs.test.ts, scripts/verify-t5-002-analytics-job-guard-mutations.mjs, docs/verification/t5-002-analytics-job-guard-matrix.md</files>
  <behavior>
    - Test 1: Each matrix row names one guard, one exact Node test name, one source marker, one compiling semantic mutant, and the expected failed assertion.
    - Test 2: Each lifecycle-envelope, documented-window, 429-retry, cancellation, provenance, closed-catalog, and runtime-boundary mutant compiles, then fails only its matrix-named behavioral test.
    - Test 3: G01-G05 mutations prove the rejected request, option, identifier rule, disputed metric field, or result schema cannot become reachable while the gate stays open.
    - Test 4: The harness refuses a duplicate or unmapped guard, a non-unique marker, a mutation that breaks the TypeScript build, a survivor, or a restoration/tree-diff failure.
  </behavior>
  <action>Expand the focused tests before changing the mutation harness. Give every guard a stable test name that asserts an observable lifecycle or boundary behavior. Include separate guards for capability-denied envelope mapping, each request-failure stage, each applicable rate-limit window, valid-header and fallback 429 retry behavior, retry exhaustion, no retry on non-429 statuses, cancelled retry wait, selection identity, all six operation routes, body-free result state, and each G01-G05 gate. Use the runtime composition test only for the internal-only boundary. Do not use a compiler error, deleted type, `never` annotation, or invalid syntax as proof.

Update `docs/verification/t5-002-analytics-job-guard-matrix.md` before candidate selection. Each row must state the guard ID, exact test name, single-occurrence source marker, compiling semantic source change, expected assertion failure, and whether it proves lifecycle behavior, isolation, or an open gate. Record the five source gates as open: excluded payment-level family, inactive-status option, UUID-only source parsing, disputed metrics counts, and completed-result schema. State that behavioral proof does not close any vendor gate. Record `DOX: updated` in the later PR evidence because this matrix changes the durable proof contract.

Rewrite `scripts/verify-t5-002-analytics-job-guard-mutations.mjs` so it checks the complete unique matrix identifier list before it edits a source file. For each entry, apply only its semantic replacement, require `npm run build:test` to exit zero, run `node --test --test-name-pattern <exact-test-name> dist-test/test/analytics-report-jobs.test.js`, and require that focused behavioral test to exit nonzero. Fail the harness when compilation fails, when the named test does not run, or when the mutant survives. Restore original bytes in `finally`, verify byte equality and `git diff --exit-code -- src/analytics-report-jobs.ts test/analytics-report-jobs.test.ts scripts/verify-t5-002-analytics-job-guard-mutations.mjs docs/verification/t5-002-analytics-job-guard-matrix.md`, then rebuild and run the complete focused job suite after restoration. Manually perform one temporary compiling semantic mutation for every entry before registering it. Do not accept aggregate test failure as a guard proof.</action>
  <verify>
    <automated>npm run build:test && node --test dist-test/test/analytics-report-jobs.test.js && node scripts/verify-t5-002-analytics-job-guard-mutations.mjs && git diff --exit-code -- src/analytics-report-jobs.ts test/analytics-report-jobs.test.ts scripts/verify-t5-002-analytics-job-guard-mutations.mjs docs/verification/t5-002-analytics-job-guard-matrix.md</automated>
  </verify>
  <done>Every matrix entry uses a compiling semantic source mutation and its exact behavioral test. The harness rejects compile-break evidence and source-restoration drift. The matrix retains six operations, G01-G05, and the no-tool boundary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Replace invalid candidate evidence and obtain a fresh exact-head review</name>
  <files>External PR #50 validation and independent-review evidence only</files>
  <what-built>PR #50 repair commits that restore lifecycle envelopes, documented multi-window Analytics limits, bounded 429 retries, and guard-specific behavioral mutation proof without adding an MCP tool.</what-built>
  <how-to-verify>
    1. Confirm PR #50 records `ab63141ef12658d94702e6fbf0314cdf1bd14343` as rejected and not valid evidence.
    2. Confirm the repair candidate SHA equals the PR head and both Node 20.20.2 and Node 22.22.2 evidence records.
    3. Confirm all documented limiter windows and each matrix mutation are named, compile, fail their matching test, and restore a clean candidate tree.
    4. Confirm the implementation exposes no MCP tool, no guest-linked operation, and no G01-G05 closure claim.
    5. Record a fresh independent review verdict and exact reviewed SHA on PR #50.
  </how-to-verify>
  <action>After Tasks 1 and 2 pass, commit all repair source, test, harness, and matrix work. Require an empty status and select a new full `CANDIDATE_SHA` from `git rev-parse HEAD`; this SHA must differ from `ab63141ef12658d94702e6fbf0314cdf1bd14343`. Push the repaired PR #50 branch before validation. Add a PR comment that invalidates the earlier Node evidence, names the new candidate SHA, records that T5-002 remains six non-guest internal operations, lists G01-G05 as open, and states that T5-003 alone owns MCP tools and stdio presentation.

For the new candidate only, run authentic registry-backed clean restores with separate empty temporary npm caches under Node 20.20.2 and Node 22.22.2. On each runtime, capture the exact SHA immediately before and after `npm ci --no-audit --no-fund --prefer-online`, `npm run check`, `npm run build:test`, `node --test dist-test/test/analytics-report-jobs.test.js`, `node scripts/verify-t5-002-analytics-job-guard-mutations.mjs`, and `npm pack --dry-run --json`. Record Node and npm versions, discovered-file and test totals, focused test total, matrix guard count, compiling caught mutant count, survivor count, package file count, and `DOX: updated`. The candidate tree must remain clean after every harness run. Do not use a copied package cache, reconstructed result, real Merchant Data, or live Toast credentials.

Request a fresh independent review only after both runtime records name the same unchanged candidate SHA. The reviewer must inspect typed capability/request envelopes, every documented limiter window, 429 retry bounds, non-429 retry denial, cancellation during retry wait, semantic mutation proof, six-operation closure, G01-G05, and no-MCP boundary. The builder must not self-approve. If a repair commit changes the head, invalidate all new evidence, select a new SHA, and repeat both runtime sequences before a replacement review.</action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" != ab63141ef12658d94702e6fbf0314cdf1bd14343 && git status --short && gh pr view 50 --json url,headRefOid,headRefName,baseRefName,state && git ls-remote --exit-code --heads origin "$(git branch --show-current)"</automated>
  </verify>
  <resume-signal>Provide the independent review verdict and exact reviewed candidate SHA, or provide the required findings.</resume-signal>
  <done>PR #50 has fresh Node 20.20.2, Node 22.22.2, focused, mutation, package, and independent-review evidence for one new unchanged SHA. The prior rejected SHA remains invalid, G01-G05 stay open, and no MCP tool is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → lifecycle | A valid internal request receives a typed non-success envelope for denial and request failure, never an uncaught transport detail or fabricated report. |
| Analytics response → retry turn | Only HTTP 429 can schedule a bounded retry. Its body remains unread. |
| Analytics identity and selected set → limiter state | Every documented bucket stays bound to the private Analytics authority, closed operation, method, range, and canonical selected set. |
| repair candidate → PR evidence | Validation and review must apply to one replacement SHA, not the rejected candidate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5-002-07-01 | Denial of service | multi-window limiter and retry loop | mitigate | Enforce every documented bucket atomically and use capped, jittered, cancellation-aware 429 retries with a local retry budget. |
| T5-002-07-02 | Information disclosure | lifecycle failures and 429 responses | mitigate | Map only safe status, provenance, and completeness data; do not retain response bodies, raw errors, credentials, or tokens. |
| T5-002-07-03 | Tampering | mutation evidence | mitigate | Require compiling semantic mutants and one named behavioral test failure per guard; reject compile-break evidence and restoration drift. |
| T5-002-07-04 | Elevation of privilege | Analytics operation surface | mitigate | Preserve D-05 and D-06 closed request behavior, six operations, identity assertion, G01-G05, and D-09 no-MCP ownership. |
| T5-002-07-05 | Repudiation | candidate evidence | mitigate | Mark the rejected SHA invalid and record before/after SHA values for every supported-runtime command and independent review. |
| T5-002-07-SC | Tampering | dependency supply chain | mitigate | Use authentic registry-backed clean installs with the locked dependencies on both supported Node versions. |
</threat_model>

<verification>
Run the focused compiled lifecycle suite after every RED and GREEN change. Run the mutation harness only after each listed mutation compiles and fails its named test. Do not select a candidate until all source, test, harness, and matrix diffs are committed. After candidate selection, rerun the full exact-head Node 20.20.2 and Node 22.22.2 validation sequences and request a separate review on that SHA.
</verification>

<success_criteria>
- Every valid Analytics lifecycle call returns a typed, immutable non-success envelope for capability denial and source-request failure, without raw upstream content.
- The six-operation adapter applies all documented simultaneous rate budgets and safe finite 429 retries.
- Every guard has a compiling semantic mutant that fails only its named behavioral test and restores a clean source tree.
- The earlier PR #50 candidate evidence is invalidated and a separate reviewer records a verdict for the new exact candidate SHA.
</success_criteria>

<source_audit>
SOURCE | ID | Feature or requirement | Plan | Status | Notes
GOAL | — | Capability-gated Analytics job transport and source-distinct reporting without guest-payment data or accounting claims | 05-07 | COVERED | Repairs the T5-002 internal lifecycle only; report presentation remains T5-003.
REQ | T5-002 | POST/GET job lifecycle, bounded polling, invalid-or-expired handling, replacement, and endpoint/time-range limiter policy | 05-07 | COVERED | Restores denied/request envelopes, full documented budgets, finite retries, and evidence.
RESEARCH | six-operation catalog | Closed non-guest operation pairs | 05-07 | COVERED | Task 1 and Task 2 preserve the catalog and test it.
RESEARCH | lifecycle failures | Fail closed without source bodies or fabricated results | 05-07 | COVERED | Task 1 makes denial and request mapping total after valid lifecycle entry.
RESEARCH | rate limits | Retrieval dual windows and operation/range-specific create windows | 05-07 | COVERED | Task 1 implements and proves every applicable simultaneous bucket.
RESEARCH | 429 handling | Header-aware bounded retry with no retry for authorization or validation | 05-07 | COVERED | Task 1 adds deterministic retry and cancellation proof.
RESEARCH | evidence | Candidate-specific Node 20/22, package, semantic mutations, and independent review | 05-07 | COVERED | Task 2 replaces invalid proof; Task 3 replaces invalid candidate evidence.
RESEARCH | G01-G05 | Five unresolved vendor-contract gates | 05-07 | COVERED | Task 2 keeps each gate mapped and task 3 records it as open.
CONTEXT | D-01 | Separate Analytics credentials and token manager | 05-07 | COVERED | Task 1 preserves separate identity and token ownership.
CONTEXT | D-02 | Scope preflight before every Analytics request | 05-07 | COVERED | Task 1 repeats preflight for create, poll, replacement, and retry turns.
CONTEXT | D-03 | Analytics authority separate from Standard location state | 05-07 | COVERED | Task 1 retains Analytics-only limiter and authority state.
CONTEXT | D-04 | Literal management-group discovery GET | 05-07 | COVERED | Task 1 keeps discovery unchanged and its limiter separate.
CONTEXT | D-05 | Exclude guest-linked data | 05-07 | COVERED | Tasks 1 and 2 preserve the request-layer exclusion.
CONTEXT | D-06 | Closed Analytics operation type | 05-07 | COVERED | Tasks 1 and 2 retain six literal operation pairs.
CONTEXT | D-07 | Immutable registry and canonical selection validation | 05-07 | COVERED | Task 1 repeats current-identity selection assertion for each turn.
CONTEXT | D-08 | T5-002 owns job lifecycle and limiter policy | 05-07 | COVERED | All repair implementation and evidence remain inside T5-002.
CONTEXT | D-09 | T5-003 owns MCP tools, reports, and stdio wiring | 05-07 | COVERED | Every task prohibits MCP presentation and Task 3 verifies its absence.
</source_audit>

<output>
Create `.planning/phases/05-source-distinct-analytics-adapter-and-tools/05-07-SUMMARY.md` when done.
</output>
