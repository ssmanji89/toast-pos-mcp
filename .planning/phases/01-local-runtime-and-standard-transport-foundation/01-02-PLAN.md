---
phase: 01-local-runtime-and-standard-transport-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - LOOP.md
autonomous: true
requirements:
  - GH-4
  - GH-32
  - PH1-PUB
external_preconditions:
  - "Plan 01-01 records the complete successful PR #37 and issue #32 preflight."
  - "Plan 01-01 implementation and mutation proof are committed on the owning Phase 1 branch."
must_haves:
  truths:
    - "The current exact head contains the CLEAN merged PR #37 rate-limit contract and the complete local stdio protocol proof."
    - "Authentic locked dependencies pass all checks on Node 20.20.2 and Node 22.22.2."
    - "Evidence records the exact SHA, commands, Node versions, discovered test count, package count, and mutations."
    - "GitHub, LOOP.md, ROADMAP.md, STATE.md, and Phase 1 validation agree without making STATE.md authoritative."
    - "Phase 1 evidence does not claim report-tool wiring, live Standard compatibility, publication readiness, or GH-4 closure."
    - "The candidate proof keeps issue #4 open because T6-003 owns the first-request cancellation release gate."
    - "Every tracked source, test, documentation, and planning edit is committed before final Node gates start."
  artifacts:
    - path: .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
      provides: "The immutable validation contract and five real Phase 1 task mappings."
    - path: LOOP.md
      provides: "Canonical campaign evidence and the dependency frontier after the merged prerequisites."
    - path: .planning/ROADMAP.md
      provides: "Outcome-level Phase 1 status with residual gates in their owning phases."
    - path: .planning/STATE.md
      provides: "A non-authoritative GitHub-reconciled snapshot for the next autonomous session."
  key_links:
    - from: .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
      to: package-lock.json
      via: "Both runtime gates restore the authentic reviewed dependency graph."
      pattern: "npm ci --no-audit --no-fund"
    - from: LOOP.md
      to: "GitHub issues #4 and #32"
      via: "The ledger records each exact disposition and remaining gate."
      pattern: "#4|#32"
    - from: .planning/STATE.md
      to: LOOP.md
      via: "STATE identifies itself as a snapshot and does not override the canonical ledger."
      pattern: "snapshot"
    - from: "Owning Phase 1 PR evidence comment"
      to: "the immutable candidate SHA"
      via: "Runtime and mutation results name the candidate without claiming an independent review result."
      pattern: "exact head|T6-003"
---

<objective>
Validate and reconcile the Phase 1 exact head after PR #37 and the local stdio protocol proof land.

Purpose: Produce authentic cross-runtime evidence without a public-release or issue-closure claim.
Output: Exact-head validation evidence, a builder evidence comment, and reconciled GSD and campaign state.

Execution precondition: Consume the successful complete preflight in `01-01-SUMMARY.md`. Stop if that record is absent. Do not run a second partial preflight or rebuild PR #37.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-CONTEXT.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-RESEARCH.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-PATTERNS.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- The inherited complete PR #37 and issue #32 preflight record from Plan 01-01.
- One committed immutable candidate containing every tracked source, test, documentation, and planning edit.
- Authentic exact-head Node 20.20.2 and Node 22.22.2 check results.
- Exact discovered test-file, total-test, mutation, and package-file counts.
- A builder evidence comment that records the open T6-003 limit and requests fresh independent review.

<tasks>

<task type="auto">
  <name>Task 1: Finalize and commit the immutable reviewed candidate</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>LOOP.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <files>LOOP.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</files>
  <action>
Read the Plan 01-01 summary. Require its shared preflight record to contain the PR #37 current head, matching exact reviewed head, CLEAN disposition URL, issue #32 evidence URL, merge SHA, and both ancestry results. Stop if any field is absent. Do not repeat or weaken the shared preflight.

Finish every tracked source, test, documentation, and planning edit before this task ends. Reconcile `LOOP.md`, the Phase 1 ROADMAP section, `STATE.md`, and `01-VALIDATION.md` with the implemented local stdio proof and inherited GH-32 prerequisite. Keep final Node gates and independent review pending. Preserve Phase 3 ownership of production report cancellation. Preserve Phase 6 ownership of live compatibility and publication gates. Keep issue #4 open because T6-003 owns first-request cancellation. Record `DOX: updated` or `DOX: no durable change`.

Run focused tests for the implemented paths. Run the ROADMAP parser and `git diff --check`. Confirm that `package.json` and `package-lock.json` are byte-unchanged. Let the executor create the normal atomic task commit containing all tracked edits. Push that commit to the owning Phase 1 PR. Task 2 must start from that clean committed `HEAD` and treat its SHA as immutable. No tracked edit or commit may follow the final Node gates on this candidate.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/test/server.test.js dist-test/test/protocol-cancellation.test.js &amp;&amp; node /Users/sully/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.analyze &amp;&amp; git diff --check &amp;&amp; git diff --exit-code -- package.json package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 01-01 summary contains the complete successful shared preflight.
    - All Phase 1 tracked edits are final before the candidate commit.
    - Planning state distinguishes implemented work from pending validation and review.
    - The atomic task commit is pushed to the owning Phase 1 PR before Task 2 starts.
    - Dependency manifests remain byte-unchanged.
  </acceptance_criteria>
  <done>One clean committed SHA contains every tracked edit that the final gates and reviewer will assess.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the candidate and request fresh independent review</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>package.json</file>
    <file>package-lock.json</file>
    <file>scripts/run-tests.mjs</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md</file>
    <file>LOOP.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
  </read_first>
  <files>none on the reviewed candidate</files>
  <action>
Require a clean worktree and capture the exact committed candidate SHA. Apply the standalone JSON schemas in `01-VALIDATION.md`. Use the installed Node 20.20.2 binary and its paired npm. Run `npm ci --no-audit --no-fund`, `npm run check`, and `npm pack --dry-run --json`. Repeat with Node 22.22.2 and its paired npm. Use a PATH that starts with the matching runtime directory. Keep complete command output and complete pack JSON outside the repository.

Require `HEAD` to equal the captured candidate after every command. Require a clean worktree after each runtime. Record each exact Node version, command, PASS result, exit status, discovered test-file count, total test count, package-file count, complete parsed pack JSON, both cancellation mutation results, and DOX disposition in one `toast-pos-mcp/phase1-candidate-builder@1` comment. State `issue4_disposition: "OPEN_T6-003"`. The builder comment must not claim CLEAN, merge, closure, report wiring, live Standard proof, or publication readiness.

Request one fresh independent `toast-pos-mcp/exact-head-review@1` review of the immutable candidate. Require the reviewer to assess the exact candidate SHA, the ordered server-side first-request probe, the repeated mutations, and the retained-process proof. Stop for that independent review. If the reviewer reports a defect, start a new candidate cycle. Do not add a post-gate fix to the reviewed candidate without restarting validation.
  </action>
  <verify>
    <automated>Run the exact Node 20.20.2 and Node 22.22.2 install, check, and package commands. Require a clean unchanged candidate SHA after each command. Run both named cancellation mutations, require each focused test to fail, restore the source, and require the focused test to pass. Post the builder evidence comment, then request fresh independent review.</automated>
  </verify>
  <acceptance_criteria>
    - The same clean candidate passes authentic install, check, and package gates on Node 20.20.2 and Node 22.22.2.
    - Builder evidence names exact commands, versions, counts, package JSON, mutations, DOX, and the open T6-003 gate.
    - Issue #4 remains open.
    - A fresh independent reviewer receives the exact candidate SHA and no builder CLEAN claim exists.
  </acceptance_criteria>
  <done>The immutable candidate has authentic builder evidence and waits for fresh independent review. Issue #4 stays open for T6-003.</done>
</task>

</tasks>

<verification>
Run the dependency gate first. Run both authentic runtime checks on one exact head. Require the two cancellation mutations. Validate the builder evidence schema, the ROADMAP parser, and `git diff --check` before requesting review.
</verification>

<success_criteria>
- PR #37 and issue #32 are stable before Phase 1 evidence work starts.
- The complete local stdio proof passes under Node 20.20.2 and Node 22.22.2 at one exact SHA.
- Durable builder evidence names commands, versions, test counts, package counts, mutations, DOX, and the open T6-003 gate.
- The independent reviewer can review one exact candidate without local terminal history.
- The repository states that Phase 1 alone does not make the MCP publish-ready or close issue #4.
</success_criteria>

<output>
Create `.planning/phases/01-local-runtime-and-standard-transport-foundation/01-02-SUMMARY.md` after the independent review completes in a continuation.
</output>
