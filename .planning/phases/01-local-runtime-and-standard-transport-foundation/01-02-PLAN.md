---
phase: 01-local-runtime-and-standard-transport-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - LOOP.md
autonomous: true
requirements:
  - GH-4
  - GH-32
  - PH1-PUB
external_preconditions:
  - "Plan 01-01 Task 1 records the complete successful PR #37 and issue #32 preflight."
  - "Plan 01-01 implementation and mutation proof are committed on this owning Phase 1 branch."
must_haves:
  truths:
    - "The current exact head contains the CLEAN merged PR #37 rate-limit contract and the complete GH-4 protocol proof."
    - "Authentic locked dependencies pass all checks on Node 20.20.2 and Node 22.22.2."
    - "Evidence records the exact SHA, commands, Node versions, discovered test count, package count, mutations, review state, and empty CLEAN-to-evidence comment intervals."
    - "GitHub, LOOP.md, ROADMAP.md, STATE.md, and Phase 1 validation agree without turning STATE.md into authority."
    - "Phase 1 evidence does not claim report-tool wiring, live Standard compatibility, or publication readiness."
    - "Every tracked source, test, documentation, and planning edit is committed before final Node gates start."
    - "The reviewed candidate receives no later commit; post-merge GSD reconciliation uses a separate control-plane branch from merged main."
  artifacts:
    - path: .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
      provides: "The immutable validation contract, three PR comment-interval invariants, and five real Phase 1 task mappings."
    - path: LOOP.md
      provides: "Canonical campaign evidence and the next dependency frontier after GH-4 and GH-32."
    - path: .planning/ROADMAP.md
      provides: "Outcome-level Phase 1 status with residual gates carried to their owning phases."
    - path: .planning/STATE.md
      provides: "Non-authoritative GitHub-reconciled snapshot for the next autonomous session."
  key_links:
    - from: .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
      to: package-lock.json
      via: "Both exact runtime gates restore the authentic reviewed dependency graph."
      pattern: "npm ci --no-audit --no-fund"
    - from: LOOP.md
      to: "GitHub issues #4 and #32"
      via: "The ledger records exact issue disposition, validation, and reviewer state."
      pattern: "#4|#32"
    - from: .planning/STATE.md
      to: LOOP.md
      via: "STATE identifies itself as a snapshot and does not override the canonical ledger."
      pattern: "snapshot"
    - from: "Owning Phase 1 PR evidence comment"
      to: "the immutable reviewed candidate SHA"
      via: "Node 20/22 results and independent CLEAN disposition are external evidence, so they do not alter the candidate."
      pattern: "exact head|CLEAN"
---

<objective>
Validate and reconcile the complete Phase 1 exact head after PR #37 and the GH-4 protocol proof land.

Purpose: Produce authentic cross-runtime evidence without converting Phase 1 into a public-release claim.
Output: Exact-head validation evidence, GitHub evidence comments, and reconciled GSD and campaign state.

Execution precondition: Consume the successful complete preflight in `01-01-SUMMARY.md`. Stop if that record is absent. Do not run a second partial preflight or rebuild PR #37.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-CONTEXT.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-RESEARCH.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-PATTERNS.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- The inherited complete PR #37 and issue #32 preflight record from Plan 01-01.
- One committed immutable candidate containing every tracked source, test, documentation, and planning edit.
- Authentic exact-head Node 20.20.2 and Node 22.22.2 check results.
- Exact discovered test-file, total-test, mutation, and package-file counts.
- Exact-head independent review with empty CLEAN-to-evidence intervals, owning issue evidence, merged-main proof, and post-merge GSD reconciliation.

<tasks>

<task type="auto">
  <name>Task 1: Finalize and commit the immutable reviewed candidate</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>LOOP.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <files>LOOP.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</files>
  <action>
Read the Plan 01-01 summary. Require its shared preflight record to contain the PR #37 current head, matching exact reviewed head, CLEAN disposition URL, issue #32 evidence URL, merge SHA, and both ancestry results. Stop if any field is absent. Do not repeat or weaken the shared preflight.

Finish every tracked source, test, documentation, and planning edit before this task ends. Reconcile `LOOP.md`, the Phase 1 ROADMAP section, `STATE.md`, and `01-VALIDATION.md` with the implemented GH-4 proof and inherited GH-32 prerequisite. Keep the final Node gates and independent review marked pending. Preserve Phase 3 ownership of production report cancellation. Preserve Phase 6 ownership of live compatibility and publication gates. Record `DOX: updated` or `DOX: no durable change`.

Run focused tests for the implemented GH-4 paths. Run the ROADMAP parser and `git diff --check`. Confirm that `package.json` and `package-lock.json` are byte-unchanged. Let the executor create the normal atomic task commit containing all tracked edits. Push that commit to the owning Phase 1 PR. Task 2 must start from that clean committed `HEAD` and treat its SHA as immutable. No tracked edit or commit may follow the final Node gates on this candidate.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/test/server.test.js dist-test/test/protocol-cancellation.test.js &amp;&amp; node /Users/sully/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.analyze &amp;&amp; git diff --check &amp;&amp; git diff --exit-code -- package.json package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 01-01 summary contains the complete successful shared preflight.
    - All Phase 1 tracked edits are final before the candidate commit.
    - Planning state distinguishes implemented work from pending validation and review.
    - The atomic task commit is pushed to the owning Phase 1 PR before Task 2 starts.
    - Dependency manifests remain byte-unchanged.
  </acceptance_criteria>
  <done>One clean committed SHA now contains every tracked edit that the final gates and reviewer will assess.</done>
</task>

<task type="auto">
  <name>Task 2: Validate, review, merge, close GH-4, and reconcile after merge</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>package.json</file>
    <file>package-lock.json</file>
    <file>scripts/run-tests.mjs</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md</file>
    <file>LOOP.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
  </read_first>
  <files>none on the reviewed candidate; a separate post-merge control-plane branch may update LOOP.md, .planning/ROADMAP.md, .planning/STATE.md, and 01-VALIDATION.md</files>
  <action>
Require a clean worktree and capture the exact committed candidate SHA. Apply the standalone JSON schemas in `01-VALIDATION.md`. Use the installed Node 20.20.2 binary and its paired npm. Run `npm ci --no-audit --no-fund`, `npm run check`, and `npm pack --dry-run --json`. Repeat with Node 22.22.2 and its paired npm. Use a PATH that starts with the matching runtime directory. Keep command output in an untracked temporary directory outside the repository. Preserve each complete pack JSON output. Do not redirect or reduce it to a count.

Require `HEAD` to equal the captured candidate after every command. Require a clean worktree after each runtime. Record each exact Node version, command, PASS result, exit status, discovered test-file count, total test count, package-file count, complete parsed pack JSON, both GH-4 mutation results, and DOX disposition in the `toast-pos-mcp/phase1-candidate@1` gate object. Never use a copied cache, alternate lockfile, validation double, GitHub Actions, or reconstructed output. Do not edit `01-VALIDATION.md` or any other tracked file after these gates.

Request a fresh independent review of that exact candidate. Require one `toast-pos-mcp/exact-head-review@1` top-level comment for `phase1-candidate` scope. Require independent CLEAN, an empty finding list, distinct reviewer and builder identifiers, and the exact candidate SHA. Require no unresolved review thread. If review finds a defect, stop. Apply the fix only in a new candidate cycle that repeats Task 1 and both complete runtime gates. Never add a post-gate fix to the reviewed candidate without restarting validation.

After CLEAN, merge the owning PR only while its head still equals the reviewed candidate. Create the separate control-plane worktree and open its PR so the issue marker can name that PR number. Post one final `toast-pos-mcp/phase1-candidate@1` JSON comment on the merged owning PR. Include the merge SHA, complete gate object, CLEAN URL, and disposition. Require this marker to be the latest top-level PR comment. Resolve the CLEAN URL to its GitHub comment ID. Capture the final evidence comment ID. Require `clean_comment_id < evidence_comment_id` and zero other top-level comment IDs in that open interval. Post one `toast-pos-mcp/issue4-closure@1` JSON comment on issue #4. Include the same complete gate object, candidate PR evidence URL, CLEAN URL, merge SHA, and control-plane PR number. Require this marker to be the latest issue comment. Close issue #4. Fetch `origin`. Verify that the candidate merge commit is on `origin/main`. The PR head-to-merge relation is authoritative for a squash merge; do not require the candidate commit to be a Git ancestor of the squash commit.

Use the separate post-merge control-plane worktree and branch from the verified `origin/main`. Reconcile `LOOP.md`, `01-VALIDATION.md`, the Phase 1 ROADMAP section, and `STATE.md` with the merged candidate, CLEAN review, issue #4 closure, and next dependency frontier. Mark `STATE.md` non-authoritative. Run the ROADMAP parser and `git diff --check`. Commit the documentation-only reconciliation separately. Obtain one `toast-pos-mcp/exact-head-review@1` independent CLEAN comment for `phase1-control-plane` scope before its separate merge. After merge, post one final `toast-pos-mcp/phase1-control-plane@1` JSON comment. Include the exact control head and merge SHAs, candidate and candidate merge SHAs, issue evidence URL, PASS parser and diff results, DOX, and CLEAN review URL. Require this marker to be the latest top-level control-plane comment. Capture its evidence comment ID and the resolved CLEAN comment ID. Require `clean_comment_id < evidence_comment_id` and zero other top-level comment IDs in that open interval. Reject any later native `COMMENTED` review on either reviewed head. Do not amend, rebase, or add commits to the reviewed code candidate branch. Do not claim report wiring, live Standard proof, or publication readiness.
  </action>
  <verify>
    <automated>EVIDENCE_DIR=$(mktemp -d) &amp;&amp; trap 'rm -rf "$EVIDENCE_DIR"' EXIT &amp;&amp; CANDIDATE=$(git rev-parse HEAD) &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test "$(/Users/sully/.nvm/versions/node/v20.20.2/bin/node --version)" = "v20.20.2" &amp;&amp; env PATH="/Users/sully/.nvm/versions/node/v20.20.2/bin:/usr/bin:/bin:/opt/homebrew/bin" /Users/sully/.nvm/versions/node/v20.20.2/bin/npm ci --no-audit --no-fund &amp;&amp; env PATH="/Users/sully/.nvm/versions/node/v20.20.2/bin:/usr/bin:/bin:/opt/homebrew/bin" /Users/sully/.nvm/versions/node/v20.20.2/bin/npm run check &gt;"$EVIDENCE_DIR/node20-check.log" &amp;&amp; env PATH="/Users/sully/.nvm/versions/node/v20.20.2/bin:/usr/bin:/bin:/opt/homebrew/bin" /Users/sully/.nvm/versions/node/v20.20.2/bin/npm pack --dry-run --json &gt;"$EVIDENCE_DIR/node20-pack.json" &amp;&amp; jq -e 'type == "array" and length &gt; 0 and (map(.files | length) | add) &gt; 0' "$EVIDENCE_DIR/node20-pack.json" &gt;/dev/null &amp;&amp; test "$(git rev-parse HEAD)" = "$CANDIDATE" &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test "$(/Users/sully/.nvm/versions/node/v22.22.2/bin/node --version)" = "v22.22.2" &amp;&amp; env PATH="/Users/sully/.nvm/versions/node/v22.22.2/bin:/usr/bin:/bin:/opt/homebrew/bin" /Users/sully/.nvm/versions/node/v22.22.2/bin/npm ci --no-audit --no-fund &amp;&amp; env PATH="/Users/sully/.nvm/versions/node/v22.22.2/bin:/usr/bin:/bin:/opt/homebrew/bin" /Users/sully/.nvm/versions/node/v22.22.2/bin/npm run check &gt;"$EVIDENCE_DIR/node22-check.log" &amp;&amp; env PATH="/Users/sully/.nvm/versions/node/v22.22.2/bin:/usr/bin:/bin:/opt/homebrew/bin" /Users/sully/.nvm/versions/node/v22.22.2/bin/npm pack --dry-run --json &gt;"$EVIDENCE_DIR/node22-pack.json" &amp;&amp; jq -e 'type == "array" and length &gt; 0 and (map(.files | length) | add) &gt; 0' "$EVIDENCE_DIR/node22-pack.json" &gt;/dev/null &amp;&amp; test "$(git rev-parse HEAD)" = "$CANDIDATE" &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; BRANCH=$(git branch --show-current) &amp;&amp; PR_NUMBER=$(gh pr list --repo ssmanji89/toast-pos-mcp --head "$BRANCH" --state merged --json number --jq '.[0].number') &amp;&amp; gh pr view "$PR_NUMBER" --repo ssmanji89/toast-pos-mcp --json state,headRefOid,mergeCommit &gt;"$EVIDENCE_DIR/candidate-pr.json" &amp;&amp; gh api --paginate "repos/ssmanji89/toast-pos-mcp/issues/$PR_NUMBER/comments" &gt;"$EVIDENCE_DIR/candidate-comments.json" &amp;&amp; gh api --paginate "repos/ssmanji89/toast-pos-mcp/pulls/$PR_NUMBER/reviews" &gt;"$EVIDENCE_DIR/candidate-reviews.json" &amp;&amp; MERGE=$(jq -er '.mergeCommit.oid | select(test("^[0-9a-f]{40}$"))' "$EVIDENCE_DIR/candidate-pr.json") &amp;&amp; jq -e --arg candidate "$CANDIDATE" --arg merge "$MERGE" '.state == "MERGED" and .headRefOid == $candidate and .mergeCommit.oid == $merge' "$EVIDENCE_DIR/candidate-pr.json" &gt;/dev/null &amp;&amp; jq -ce --arg candidate "$CANDIDATE" '[.[] | . as $c | ($c.body | fromjson? // empty) as $b | select($b.schema == "toast-pos-mcp/phase1-candidate@1" and $b.candidate_sha == $candidate) | {id:$c.id,url:$c.html_url,created_at:$c.created_at,body:$b}] | sort_by(.id) | last' "$EVIDENCE_DIR/candidate-comments.json" &gt;"$EVIDENCE_DIR/candidate-evidence.json" &amp;&amp; CANDIDATE_EVIDENCE_ID=$(jq -er '.id' "$EVIDENCE_DIR/candidate-evidence.json") &amp;&amp; jq -e --argjson id "$CANDIDATE_EVIDENCE_ID" 'max_by(.id).id == $id' "$EVIDENCE_DIR/candidate-comments.json" &gt;/dev/null &amp;&amp; jq -e --argjson pr "$PR_NUMBER" --arg candidate "$CANDIDATE" --arg merge "$MERGE" '.body as $e | $e.pr_number == $pr and $e.issue_number == 4 and $e.candidate_sha == $candidate and $e.merge_sha == $merge and $e.review.disposition == "CLEAN" and ($e.review.comment_url | test("^https://github.com/ssmanji89/toast-pos-mcp/pull/[0-9]+#issuecomment-[0-9]+$")) and ($e.gate.runtime_gates | keys | sort) == ["node20","node22"] and $e.gate.runtime_gates.node20.node_version == "v20.20.2" and $e.gate.runtime_gates.node22.node_version == "v22.22.2" and all($e.gate.runtime_gates[] as $r; $r.install.command == "npm ci --no-audit --no-fund" and $r.install.result == "PASS" and $r.install.exit_code == 0 and $r.check.command == "npm run check" and $r.check.result == "PASS" and $r.check.exit_code == 0 and $r.check.test_files &gt; 0 and $r.check.tests_total &gt; 0 and $r.check.tests_passed == $r.check.tests_total and $r.check.tests_failed == 0 and $r.pack.command == "npm pack --dry-run --json" and $r.pack.result == "PASS" and $r.pack.exit_code == 0 and $r.pack.package_files &gt; 0 and ($r.pack.raw_json | type) == "array" and ($r.pack.raw_json | map(.files | length) | add) == $r.pack.package_files) and (($e.gate.mutations | sort_by(.id)) == ([{"id":"ignore-handler-signal","focused_test_result":"FAIL","result":"CAUGHT","restored":true},{"id":"terminate-process-on-cancel","focused_test_result":"FAIL","result":"CAUGHT","restored":true}] | sort_by(.id))) and ($e.gate.dox == "updated" or $e.gate.dox == "no durable change")' "$EVIDENCE_DIR/candidate-evidence.json" &gt;/dev/null &amp;&amp; CANDIDATE_CLEAN_URL=$(jq -er '.body.review.comment_url' "$EVIDENCE_DIR/candidate-evidence.json") &amp;&amp; jq -ce --arg url "$CANDIDATE_CLEAN_URL" '[.[] | . as $c | select($c.html_url == $url) | ($c.body | fromjson? // empty) as $b | {id:$c.id,created_at:$c.created_at,body:$b}] | select(length == 1) | .[0]' "$EVIDENCE_DIR/candidate-comments.json" &gt;"$EVIDENCE_DIR/candidate-clean.json" &amp;&amp; jq -e --argjson pr "$PR_NUMBER" --arg candidate "$CANDIDATE" '.body.schema == "toast-pos-mcp/exact-head-review@1" and .body.subject.repository == "ssmanji89/toast-pos-mcp" and .body.subject.kind == "pull_request" and .body.subject.number == $pr and .body.subject.head_sha == $candidate and .body.subject.scope == "phase1-candidate" and .body.review.independent == true and .body.review.disposition == "CLEAN" and .body.review.finding_ids == [] and (.body.review.reviewer_id | type == "string" and length &gt; 0) and (.body.review.builder_id | type == "string" and length &gt; 0) and .body.review.reviewer_id != .body.review.builder_id' "$EVIDENCE_DIR/candidate-clean.json" &gt;/dev/null &amp;&amp; CANDIDATE_CLEAN_COMMENT_ID=$(jq -er '.id' "$EVIDENCE_DIR/candidate-clean.json") &amp;&amp; jq -e --argjson clean_comment_id "$CANDIDATE_CLEAN_COMMENT_ID" --argjson evidence_comment_id "$CANDIDATE_EVIDENCE_ID" '($clean_comment_id &lt; $evidence_comment_id) and ([.[] | select(.id &gt; $clean_comment_id and .id &lt; $evidence_comment_id)] | length == 0)' "$EVIDENCE_DIR/candidate-comments.json" &gt;/dev/null &amp;&amp; CANDIDATE_CLEAN_TIME=$(jq -er '.created_at' "$EVIDENCE_DIR/candidate-clean.json") &amp;&amp; jq -e --arg candidate "$CANDIDATE" --arg clean_time "$CANDIDATE_CLEAN_TIME" '[.[] | select(.state == "COMMENTED" and .commit_id == $candidate and .submitted_at &gt; $clean_time)] | length == 0' "$EVIDENCE_DIR/candidate-reviews.json" &gt;/dev/null &amp;&amp; gh api graphql -F owner=ssmanji89 -F repo=toast-pos-mcp -F number="$PR_NUMBER" -f query='query($owner:String!,$repo:String!,$number:Int!){repository(owner:$owner,name:$repo){pullRequest(number:$number){reviewThreads(first:100){nodes{isResolved}}}}}' --jq '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved == false)] | length' | rg '^0$' &amp;&amp; gh issue view 4 --repo ssmanji89/toast-pos-mcp --json state &gt;"$EVIDENCE_DIR/issue.json" &amp;&amp; gh api --paginate repos/ssmanji89/toast-pos-mcp/issues/4/comments &gt;"$EVIDENCE_DIR/issue-comments.json" &amp;&amp; jq -e '.state == "CLOSED"' "$EVIDENCE_DIR/issue.json" &gt;/dev/null &amp;&amp; jq -ce --arg candidate "$CANDIDATE" '[.[] | . as $c | ($c.body | fromjson? // empty) as $b | select($b.schema == "toast-pos-mcp/issue4-closure@1" and $b.candidate_sha == $candidate) | {id:$c.id,url:$c.html_url,body:$b}] | sort_by(.id) | last' "$EVIDENCE_DIR/issue-comments.json" &gt;"$EVIDENCE_DIR/issue-evidence.json" &amp;&amp; ISSUE_EVIDENCE_ID=$(jq -er '.id' "$EVIDENCE_DIR/issue-evidence.json") &amp;&amp; jq -e --argjson id "$ISSUE_EVIDENCE_ID" 'max_by(.id).id == $id' "$EVIDENCE_DIR/issue-comments.json" &gt;/dev/null &amp;&amp; CANDIDATE_EVIDENCE_URL=$(jq -er '.url' "$EVIDENCE_DIR/candidate-evidence.json") &amp;&amp; CANDIDATE_GATE=$(jq -c '.body.gate' "$EVIDENCE_DIR/candidate-evidence.json") &amp;&amp; jq -e --argjson pr "$PR_NUMBER" --arg candidate "$CANDIDATE" --arg merge "$MERGE" --arg pr_url "$CANDIDATE_EVIDENCE_URL" --arg clean_url "$CANDIDATE_CLEAN_URL" --argjson gate "$CANDIDATE_GATE" '.body.issue_number == 4 and .body.pr_number == $pr and .body.candidate_sha == $candidate and .body.merge_sha == $merge and .body.pr_evidence_url == $pr_url and .body.review_comment_url == $clean_url and .body.gate == $gate and (.body.control_plane_pr_number | type == "number")' "$EVIDENCE_DIR/issue-evidence.json" &gt;/dev/null &amp;&amp; CONTROL_PR=$(jq -er '.body.control_plane_pr_number' "$EVIDENCE_DIR/issue-evidence.json") &amp;&amp; gh pr view "$CONTROL_PR" --repo ssmanji89/toast-pos-mcp --json state,headRefOid,mergeCommit &gt;"$EVIDENCE_DIR/control-pr.json" &amp;&amp; gh api --paginate "repos/ssmanji89/toast-pos-mcp/issues/$CONTROL_PR/comments" &gt;"$EVIDENCE_DIR/control-comments.json" &amp;&amp; gh api --paginate "repos/ssmanji89/toast-pos-mcp/pulls/$CONTROL_PR/reviews" &gt;"$EVIDENCE_DIR/control-reviews.json" &amp;&amp; CONTROL_HEAD=$(jq -er '.headRefOid | select(test("^[0-9a-f]{40}$"))' "$EVIDENCE_DIR/control-pr.json") &amp;&amp; CONTROL_MERGE=$(jq -er '.mergeCommit.oid | select(test("^[0-9a-f]{40}$"))' "$EVIDENCE_DIR/control-pr.json") &amp;&amp; jq -e --arg head "$CONTROL_HEAD" --arg merge "$CONTROL_MERGE" '.state == "MERGED" and .headRefOid == $head and .mergeCommit.oid == $merge' "$EVIDENCE_DIR/control-pr.json" &gt;/dev/null &amp;&amp; jq -ce --arg head "$CONTROL_HEAD" '[.[] | . as $c | ($c.body | fromjson? // empty) as $b | select($b.schema == "toast-pos-mcp/phase1-control-plane@1" and $b.head_sha == $head) | {id:$c.id,url:$c.html_url,created_at:$c.created_at,body:$b}] | sort_by(.id) | last' "$EVIDENCE_DIR/control-comments.json" &gt;"$EVIDENCE_DIR/control-evidence.json" &amp;&amp; CONTROL_EVIDENCE_ID=$(jq -er '.id' "$EVIDENCE_DIR/control-evidence.json") &amp;&amp; jq -e --argjson id "$CONTROL_EVIDENCE_ID" 'max_by(.id).id == $id' "$EVIDENCE_DIR/control-comments.json" &gt;/dev/null &amp;&amp; ISSUE_EVIDENCE_URL=$(jq -er '.url' "$EVIDENCE_DIR/issue-evidence.json") &amp;&amp; jq -e --argjson pr "$CONTROL_PR" --arg head "$CONTROL_HEAD" --arg merge "$CONTROL_MERGE" --arg candidate "$CANDIDATE" --arg candidate_merge "$MERGE" --arg issue_url "$ISSUE_EVIDENCE_URL" '.body.pr_number == $pr and .body.head_sha == $head and .body.merge_sha == $merge and .body.candidate_sha == $candidate and .body.candidate_merge_sha == $candidate_merge and .body.issue_number == 4 and .body.issue_evidence_url == $issue_url and .body.roadmap_analyze == "PASS" and .body.git_diff_check == "PASS" and .body.review.disposition == "CLEAN" and (.body.dox == "updated" or .body.dox == "no durable change")' "$EVIDENCE_DIR/control-evidence.json" &gt;/dev/null &amp;&amp; CONTROL_CLEAN_URL=$(jq -er '.body.review.comment_url' "$EVIDENCE_DIR/control-evidence.json") &amp;&amp; jq -ce --arg url "$CONTROL_CLEAN_URL" '[.[] | . as $c | select($c.html_url == $url) | ($c.body | fromjson? // empty) as $b | {id:$c.id,created_at:$c.created_at,body:$b}] | select(length == 1) | .[0]' "$EVIDENCE_DIR/control-comments.json" &gt;"$EVIDENCE_DIR/control-clean.json" &amp;&amp; jq -e --argjson pr "$CONTROL_PR" --arg head "$CONTROL_HEAD" '.body.schema == "toast-pos-mcp/exact-head-review@1" and .body.subject.repository == "ssmanji89/toast-pos-mcp" and .body.subject.kind == "pull_request" and .body.subject.number == $pr and .body.subject.head_sha == $head and .body.subject.scope == "phase1-control-plane" and .body.review.independent == true and .body.review.disposition == "CLEAN" and .body.review.finding_ids == [] and (.body.review.reviewer_id | type == "string" and length &gt; 0) and (.body.review.builder_id | type == "string" and length &gt; 0) and .body.review.reviewer_id != .body.review.builder_id' "$EVIDENCE_DIR/control-clean.json" &gt;/dev/null &amp;&amp; CONTROL_CLEAN_COMMENT_ID=$(jq -er '.id' "$EVIDENCE_DIR/control-clean.json") &amp;&amp; jq -e --argjson clean_comment_id "$CONTROL_CLEAN_COMMENT_ID" --argjson evidence_comment_id "$CONTROL_EVIDENCE_ID" '($clean_comment_id &lt; $evidence_comment_id) and ([.[] | select(.id &gt; $clean_comment_id and .id &lt; $evidence_comment_id)] | length == 0)' "$EVIDENCE_DIR/control-comments.json" &gt;/dev/null &amp;&amp; CONTROL_CLEAN_TIME=$(jq -er '.created_at' "$EVIDENCE_DIR/control-clean.json") &amp;&amp; jq -e --arg head "$CONTROL_HEAD" --arg clean_time "$CONTROL_CLEAN_TIME" '[.[] | select(.state == "COMMENTED" and .commit_id == $head and .submitted_at &gt; $clean_time)] | length == 0' "$EVIDENCE_DIR/control-reviews.json" &gt;/dev/null &amp;&amp; gh api graphql -F owner=ssmanji89 -F repo=toast-pos-mcp -F number="$CONTROL_PR" -f query='query($owner:String!,$repo:String!,$number:Int!){repository(owner:$owner,name:$repo){pullRequest(number:$number){reviewThreads(first:100){nodes{isResolved}}}}}' --jq '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved == false)] | length' | rg '^0$' &amp;&amp; git fetch origin &amp;&amp; git merge-base --is-ancestor "$MERGE" origin/main &amp;&amp; git merge-base --is-ancestor "$CONTROL_MERGE" origin/main &amp;&amp; git show origin/main:LOOP.md | rg -F "$CANDIDATE" &amp;&amp; git show origin/main:.planning/STATE.md | rg -F "$CANDIDATE" &amp;&amp; git show origin/main:.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md | rg -F "$CANDIDATE"</automated>
  </verify>
  <acceptance_criteria>
    - Both supported runtime gates pass at one unchanged clean candidate SHA.
    - Candidate and control PR evidence contain complete gates and empty CLEAN-to-evidence comment intervals.
    - The exact reviewed candidate merges, issue #4 closes with complete evidence, and origin/main contains the merge.
    - A separate post-merge control-plane commit reconciles LOOP and GSD state without changing the reviewed candidate.
    - Phase 1 keeps production report cancellation in Phase 3 and publication proof in Phase 6.
  </acceptance_criteria>
  <done>Issue #4 is closed from immutable exact-head evidence, main contains the CLEAN candidate, and separate GSD state records the next frontier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub → local campaign state | Remote PR, issue, and reviewer facts control local evidence claims. |
| npm registry → local install | External package bytes restore the reviewed exact dependency graph. |
| Runtime output → durable evidence | Test output can be incomplete, misattributed, or contain unsafe data. |
| Builder evidence → reviewer disposition | A builder cannot convert its own passing gates into independent CLEAN approval. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing | PR #37 dependency | mitigate | Query GitHub at execution time and require the merge in current ancestry. |
| T-02-02 | Tampering | Exact-head evidence | mitigate | Capture one SHA before both gates and reject any source or lockfile change between runs. |
| T-02-03 | Repudiation | Reviewer state | mitigate | Keep builder validation separate and require empty CLEAN-to-evidence intervals for every reviewed PR. |
| T-02-04 | Information disclosure | Validation logs and comments | mitigate | Record commands, counts, and synthetic markers only; never record credentials or Merchant Data. |
| T-02-05 | Denial of service | Registry or runtime gate | accept | Stop with the gate open if authentic restoration or either supported runtime fails. |
| T-02-06 | Elevation of privilege | Publication claim | mitigate | Preserve explicit Phase 3 and Phase 6 gates and prohibit release claims from Phase 1 tests. |
| T-02-SC | Tampering | npm package restoration | mitigate | Use only `npm ci` against the reviewed lockfile and reject manifest or lock changes. |

Any new HIGH severity finding blocks this plan. The executor must stop and record the exact unresolved gate.
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Prove local MCP v2 stdio and read-only Standard transport across supported Nodes and unresolved external semantics. | 01-01, 01-02 | COVERED | Protocol proof plus post-PR #37 exact-head evidence. |
| REQ | GH-4 | Initialization, negotiation, independent requests, restart, reconnect, cancellation, and missing-state failure. | 01-01, 01-02 | COVERED | Authentic child-process tests and evidence. |
| REQ | GH-32 | Authoritative reset semantics and official header contract. | 01-01, 01-02 | COVERED | The shared Plan 01-01 preflight proves the PR #37 CLEAN merge and issue closure; Plan 01-02 consumes it without duplicate implementation. |
| REQ | PH1-PUB | Keep publication claims separate from Phase 1 proof. | 01-02 | COVERED | Evidence-state separation is an acceptance gate. |
| RESEARCH | GH-4-REQ | Sequential and concurrent era-correct requests use one retained PID. | 01-01 | COVERED | `ping` for legacy and `discover` for modern. |
| RESEARCH | GH-4-CANCEL | Official client abort reaches the handler signal and preserves process use. | 01-01 | COVERED | Test-only fixture with mutation verification. |
| RESEARCH | GH-32-HEADERS | Official `X-Toast-*` fields drive bounded location-scoped state. | 01-01, 01-02 | COVERED | Owned only by prerequisite PR #37 and checked by the shared preflight. |
| RESEARCH | GH-32-BOUND | Absolute seconds-or-milliseconds reset parsing stays bounded. | 01-01, 01-02 | COVERED | Owned only by prerequisite PR #37 and checked through its exact evidence. |
| RESEARCH | VALIDATION | Run authentic locked Node 20 and Node 22 gates with discovered counts. | 01-02 | COVERED | Exact candidate SHA and package counts required. |
| CONTEXT | Existing implementation | Treat T1-001 through T1-006 and PR #24 as merged evidence without rebuilding. | 01-01, 01-02 | COVERED | Plans add proof only. |
| CONTEXT | Authority | Use GitHub and LOOP.md as atomic authority. | 01-02 | COVERED | Fresh GitHub queries precede state edits. |
| CONTEXT | Authenticity | Never replace Node 20 or Node 22 execution with validation doubles. | 01-02 | COVERED | Exact local runtime commands are mandatory. |
| CONTEXT | Protocol scope | Keep issue #4 local stdio-only and cover every named behavior. | 01-01 | COVERED | No remote transport or production report tool. |
| CONTEXT | Rate-limit ownership | PR #37 owns the complete repair and issue #32 closure. | 01-02 | COVERED | Hard precondition prevents a race or rebuild. |
| CONTEXT | Publication | Do not mark the package publish-ready from Phase 1 evidence. | 01-02 | COVERED | ROADMAP and issue evidence retain later gates. |
| CONTEXT | Deferred | User-facing tools, Streamable HTTP, and live Toast evidence. | none | EXCLUDED | Explicitly deferred to later owned phases. |

<verification>
Run the dependency gate first. Run both authentic runtime checks on one exact head. Validate all three PR comment intervals and the ROADMAP parser. Require `git diff --check` and unchanged dependency manifests.
</verification>

<success_criteria>
- PR #37 and issue #32 are stable before any Phase 1 evidence work starts.
- The complete GH-4 proof passes under Node 20.20.2 and Node 22.22.2 at one exact SHA.
- Durable evidence names commands, versions, test counts, package counts, mutations, DOX, and reviewer state.
- The independent reviewer can review one exact candidate without local terminal history.
- The repository still says that Phase 1 alone does not make the MCP publish-ready.
</success_criteria>

<output>
Create `.planning/phases/01-local-runtime-and-standard-transport-foundation/01-02-SUMMARY.md` when done.
</output>
