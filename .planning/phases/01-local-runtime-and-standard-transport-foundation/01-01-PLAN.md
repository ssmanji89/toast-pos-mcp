---
phase: 01-local-runtime-and-standard-transport-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/server.test.ts
  - test/protocol-cancellation.test.ts
  - test/fixtures/protocol-cancellation-server.ts
autonomous: true
requirements:
  - GH-4
  - GH-32
external_preconditions:
  - "The shared Task 1 preflight must prove the complete PR #37 and issue #32 gate before any tracked edit."
must_haves:
  truths:
    - "One fail-closed preflight proves the PR #37 head, exact reviewed head, CLEAN disposition, an empty CLEAN-to-evidence comment interval, issue #32 evidence, merge state, and merge ancestry before either plan runs."
    - "Legacy and modern official MCP clients each complete sequential and concurrent requests on one retained child process."
    - "A closed connection can restart as a new process and complete another era-correct request without hidden session state."
    - "An official client cancellation reaches the real server handler signal and does not terminate the retained process."
    - "Missing consent or configuration still fails before protocol output and exposes no synthetic secret marker."
    - "The production server remains tool-free during Phase 1."
  artifacts:
    - path: test/server.test.ts
      provides: "Retained-process, restart, negotiation, and missing-state child-process proof."
    - path: test/protocol-cancellation.test.ts
      provides: "Official-client cancellation and post-cancellation process-reuse proof."
    - path: test/fixtures/protocol-cancellation-server.ts
      provides: "Synthetic test-only handler that observes the real MCP request abort signal."
  key_links:
    - from: test/server.test.ts
      to: dist/index.js
      via: "StdioClientTransport starts the compiled production executable with synthetic runtime configuration."
      pattern: "StdioClientTransport"
    - from: test/protocol-cancellation.test.ts
      to: test/fixtures/protocol-cancellation-server.ts
      via: "The official client starts the compiled fixture and cancels one active tool request."
      pattern: "callTool"
    - from: test/fixtures/protocol-cancellation-server.ts
      to: "@modelcontextprotocol/server/stdio"
      via: "The fixture uses the official serveStdio factory boundary."
      pattern: "serveStdio"
---

<objective>
Close the remaining local stdio protocol-proof gap from GitHub issue #4.

Purpose: Prove request independence, restart behavior, and cancellation through authentic MCP v2 child-process paths.
Output: Focused synthetic integration tests and one test-only cancellable stdio fixture.

Execution precondition: Stop before edits unless PR #37 is CLEAN and merged, and issue #32 is closed. Do not rebuild PR #37.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-CONTEXT.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-RESEARCH.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-PATTERNS.md
@.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md
</context>

## Artifacts this phase produces

- A fail-closed PR #37 and issue #32 preflight record with the CLEAN-to-evidence comment interval that Plan 01-02 inherits.
- A retained-process proof for legacy 2025 and pinned modern 2026-07-28 clients.
- A handler-observed cancellation proof through the official stdio client and server packages.
- A post-cancellation process-reuse proof without a production MCP tool.
- A mutation record that shows the cancellation assertions fail when handler behavior regresses.

<tasks>

<task type="auto">
  <name>Task 1: Enforce the shared PR #37 and issue #32 dependency preflight</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>LOOP.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-CONTEXT.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-VALIDATION.md</file>
  </read_first>
  <files>none</files>
  <action>
Fetch `origin` before any tracked edit. Apply the standalone JSON schemas in `01-VALIDATION.md`. Query PR #37 metadata, top-level issue comments, native reviews, and review threads through GitHub. Select the latest `toast-pos-mcp/pr37-gate@1` marker for the current head. Require that marker to be the latest top-level PR comment. Resolve its CLEAN review URL to exactly one `toast-pos-mcp/exact-head-review@1` marker for PR #37, the current head, and `rate-limit-prerequisite` scope. Require independent CLEAN with no findings and distinct reviewer and builder identifiers.

Capture the resolved CLEAN comment ID and the final PR evidence comment ID from GitHub. Require `clean_comment_id < evidence_comment_id`. Require zero other top-level comment IDs in that open interval. Reject every native `COMMENTED` review on the current head that is later than the CLEAN marker. Reject every unresolved review thread. These checks make a later finding invalidate the prior CLEAN marker without relying on GitHub `CHANGES_REQUESTED` counts or text search.

Validate the complete PR #37 gate object. Require exact Node `v20.20.2` and `v22.22.2` entries. Each runtime must record exact install, check, and pack commands, PASS, zero exit, positive discovered file and test counts, zero failed tests, and complete raw pack JSON whose file count matches `package_files`. Require the exact official Toast source, current header names, bounded absolute reset contract, non-official-prefix negative PASS, a caught mutation gate with zero survivors, restored source, and DOX disposition.

Query issue #32 metadata and top-level comments. Require the closed issue's latest comment to be one `toast-pos-mcp/issue32-closure@1` marker for the same PR head and merge SHA. Require its PR evidence URL and CLEAN review URL to match PR #37. Require its complete gate object to equal the validated PR gate object exactly. Require PR #37 to be `MERGED`. Require its merge commit to be a forty-character SHA and an ancestor of both `origin/main` and the execution `HEAD`.

Stop with a dependency blocker when any check fails. The current open PR #37 or open issue #32 is an expected execution block, not permission to change this plan. Do not edit, rebase, cherry-pick, recreate, or self-approve PR #37. Write the verified head, review disposition URL, issue evidence URL, and merge SHA to `01-01-SUMMARY.md` only after all checks pass. Plan 01-02 must consume that summary and must not run a second partial preflight.
  </action>
  <verify>
    <automated>TMP_DIR=$(mktemp -d) &amp;&amp; trap 'rm -rf "$TMP_DIR"' EXIT &amp;&amp; git fetch origin &amp;&amp; gh pr view 37 --repo ssmanji89/toast-pos-mcp --json state,headRefOid,mergeCommit &gt;"$TMP_DIR/pr.json" &amp;&amp; gh api --paginate repos/ssmanji89/toast-pos-mcp/issues/37/comments &gt;"$TMP_DIR/pr-comments.json" &amp;&amp; gh api --paginate repos/ssmanji89/toast-pos-mcp/pulls/37/reviews &gt;"$TMP_DIR/pr-reviews.json" &amp;&amp; gh issue view 32 --repo ssmanji89/toast-pos-mcp --json state &gt;"$TMP_DIR/issue.json" &amp;&amp; gh api --paginate repos/ssmanji89/toast-pos-mcp/issues/32/comments &gt;"$TMP_DIR/issue-comments.json" &amp;&amp; HEAD37=$(jq -er '.headRefOid | select(test("^[0-9a-f]{40}$"))' "$TMP_DIR/pr.json") &amp;&amp; MERGE37=$(jq -er '.mergeCommit.oid | select(test("^[0-9a-f]{40}$"))' "$TMP_DIR/pr.json") &amp;&amp; jq -e --arg head "$HEAD37" --arg merge "$MERGE37" '.state == "MERGED" and .headRefOid == $head and .mergeCommit.oid == $merge' "$TMP_DIR/pr.json" &gt;/dev/null &amp;&amp; jq -ce --arg head "$HEAD37" '[.[] | . as $c | ($c.body | fromjson? // empty) as $b | select($b.schema == "toast-pos-mcp/pr37-gate@1" and $b.head_sha == $head) | {id:$c.id,url:$c.html_url,created_at:$c.created_at,body:$b}] | sort_by(.id) | last' "$TMP_DIR/pr-comments.json" &gt;"$TMP_DIR/pr-evidence.json" &amp;&amp; PR_EVIDENCE_ID=$(jq -er '.id' "$TMP_DIR/pr-evidence.json") &amp;&amp; jq -e --argjson id "$PR_EVIDENCE_ID" 'max_by(.id).id == $id' "$TMP_DIR/pr-comments.json" &gt;/dev/null &amp;&amp; jq -e --arg head "$HEAD37" --arg merge "$MERGE37" '.body as $e | $e.pr_number == 37 and $e.issue_number == 32 and $e.head_sha == $head and $e.merge_sha == $merge and $e.review.disposition == "CLEAN" and ($e.review.comment_url | test("^https://github.com/ssmanji89/toast-pos-mcp/pull/37#issuecomment-[0-9]+$")) and $e.gate.contract.source_url == "https://doc.toasttab.com/doc/devguide/apiRateLimiting.html" and ($e.gate.contract.retrieved_on | test("^[0-9]{4}-[0-9]{2}-[0-9]{2}$")) and $e.gate.contract.reset_semantics == "absolute-unix-epoch-seconds-or-milliseconds-bounded" and (($e.gate.contract.header_names | sort) == (["Retry-After","X-Toast-RateLimit-By","X-Toast-RateLimit-Limit","X-Toast-RateLimit-Remaining","X-Toast-RateLimit-Reset"] | sort)) and $e.gate.contract.non_official_prefix_negative.result == "PASS" and ($e.gate.runtime_gates | keys | sort) == ["node20","node22"] and $e.gate.runtime_gates.node20.node_version == "v20.20.2" and $e.gate.runtime_gates.node22.node_version == "v22.22.2" and all($e.gate.runtime_gates[] as $r; $r.install.command == "npm ci --no-audit --no-fund" and $r.install.result == "PASS" and $r.install.exit_code == 0 and $r.check.command == "npm run check" and $r.check.result == "PASS" and $r.check.exit_code == 0 and $r.check.test_files &gt; 0 and $r.check.tests_total &gt; 0 and $r.check.tests_passed == $r.check.tests_total and $r.check.tests_failed == 0 and $r.pack.command == "npm pack --dry-run --json" and $r.pack.result == "PASS" and $r.pack.exit_code == 0 and $r.pack.package_files &gt; 0 and ($r.pack.raw_json | type) == "array" and ($r.pack.raw_json | map(.files | length) | add) == $r.pack.package_files) and $e.gate.mutation_gate.result == "PASS" and $e.gate.mutation_gate.total &gt; 0 and $e.gate.mutation_gate.caught == $e.gate.mutation_gate.total and $e.gate.mutation_gate.survivors == 0 and $e.gate.mutation_gate.restored == true and ($e.gate.dox == "updated" or $e.gate.dox == "no durable change")' "$TMP_DIR/pr-evidence.json" &gt;/dev/null &amp;&amp; CLEAN_URL=$(jq -er '.body.review.comment_url' "$TMP_DIR/pr-evidence.json") &amp;&amp; jq -ce --arg url "$CLEAN_URL" '[.[] | . as $c | select($c.html_url == $url) | ($c.body | fromjson? // empty) as $b | {id:$c.id,url:$c.html_url,created_at:$c.created_at,body:$b}] | select(length == 1) | .[0]' "$TMP_DIR/pr-comments.json" &gt;"$TMP_DIR/clean.json" &amp;&amp; jq -e --arg head "$HEAD37" '.body.schema == "toast-pos-mcp/exact-head-review@1" and .body.subject.repository == "ssmanji89/toast-pos-mcp" and .body.subject.kind == "pull_request" and .body.subject.number == 37 and .body.subject.head_sha == $head and .body.subject.scope == "rate-limit-prerequisite" and .body.review.independent == true and .body.review.disposition == "CLEAN" and .body.review.finding_ids == [] and (.body.review.reviewer_id | type == "string" and length &gt; 0) and (.body.review.builder_id | type == "string" and length &gt; 0) and .body.review.reviewer_id != .body.review.builder_id' "$TMP_DIR/clean.json" &gt;/dev/null &amp;&amp; CLEAN_COMMENT_ID=$(jq -er '.id' "$TMP_DIR/clean.json") &amp;&amp; jq -e --argjson clean_comment_id "$CLEAN_COMMENT_ID" --argjson evidence_comment_id "$PR_EVIDENCE_ID" '($clean_comment_id &lt; $evidence_comment_id) and ([.[] | select(.id &gt; $clean_comment_id and .id &lt; $evidence_comment_id)] | length == 0)' "$TMP_DIR/pr-comments.json" &gt;/dev/null &amp;&amp; CLEAN_TIME=$(jq -er '.created_at' "$TMP_DIR/clean.json") &amp;&amp; jq -e --arg head "$HEAD37" --arg clean_time "$CLEAN_TIME" '[.[] | select(.state == "COMMENTED" and .commit_id == $head and .submitted_at &gt; $clean_time)] | length == 0' "$TMP_DIR/pr-reviews.json" &gt;/dev/null &amp;&amp; gh api graphql -F owner=ssmanji89 -F repo=toast-pos-mcp -F number=37 -f query='query($owner:String!,$repo:String!,$number:Int!){repository(owner:$owner,name:$repo){pullRequest(number:$number){reviewThreads(first:100){nodes{isResolved}}}}}' --jq '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved == false)] | length' | rg '^0$' &amp;&amp; jq -e '.state == "CLOSED"' "$TMP_DIR/issue.json" &gt;/dev/null &amp;&amp; jq -ce --arg head "$HEAD37" '[.[] | . as $c | ($c.body | fromjson? // empty) as $b | select($b.schema == "toast-pos-mcp/issue32-closure@1" and $b.head_sha == $head) | {id:$c.id,url:$c.html_url,body:$b}] | sort_by(.id) | last' "$TMP_DIR/issue-comments.json" &gt;"$TMP_DIR/issue-evidence.json" &amp;&amp; ISSUE_EVIDENCE_ID=$(jq -er '.id' "$TMP_DIR/issue-evidence.json") &amp;&amp; jq -e --argjson id "$ISSUE_EVIDENCE_ID" 'max_by(.id).id == $id' "$TMP_DIR/issue-comments.json" &gt;/dev/null &amp;&amp; PR_EVIDENCE_URL=$(jq -er '.url' "$TMP_DIR/pr-evidence.json") &amp;&amp; PR_GATE=$(jq -c '.body.gate' "$TMP_DIR/pr-evidence.json") &amp;&amp; jq -e --arg head "$HEAD37" --arg merge "$MERGE37" --arg pr_url "$PR_EVIDENCE_URL" --arg clean_url "$CLEAN_URL" --argjson gate "$PR_GATE" '.body.issue_number == 32 and .body.pr_number == 37 and .body.head_sha == $head and .body.merge_sha == $merge and .body.pr_evidence_url == $pr_url and .body.review_comment_url == $clean_url and .body.gate == $gate' "$TMP_DIR/issue-evidence.json" &gt;/dev/null &amp;&amp; git merge-base --is-ancestor "$MERGE37" origin/main &amp;&amp; git merge-base --is-ancestor "$MERGE37" HEAD</automated>
  </verify>
  <acceptance_criteria>
    - The GitHub PR head equals the exact independently reviewed CLEAN head.
    - The CLEAN comment ID precedes the evidence ID with no other top-level comment ID between them.
    - Issue #32 contains exact-head validation and reviewer evidence and is closed.
    - PR #37 is merged, and its merge commit exists in both required ancestries.
    - No tracked file changes before the complete preflight passes.
  </acceptance_criteria>
  <done>Both Phase 1 plans have one complete, inherited, fail-closed PR #37 dependency gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove independent retained-process requests and clean restart</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>LOOP.md</file>
    <file>test/server.test.ts</file>
    <file>src/index.ts</file>
    <file>src/stdio.ts</file>
    <file>src/server.ts</file>
  </read_first>
  <files>test/server.test.ts</files>
  <behavior>
    - The legacy client sends two sequential pings and two concurrent pings while its child PID stays constant.
    - The pinned modern client sends two sequential discover requests and two concurrent discover requests while its child PID stays constant.
    - Closing and reconnecting starts a different non-null PID, and the new process completes an era-correct request.
    - Existing missing-consent and missing-configuration cases keep a nonzero exit, empty stdout, static stderr, and no secret marker.
  </behavior>
  <action>
Extend the existing child-process test helpers in `test/server.test.ts`. Use the official `Client` and `StdioClientTransport` against compiled `dist/index.js`. Capture each retained child PID after connection. Use `ping()` only for the legacy client. Use `discover()` only for the pinned modern client. Send two sequential operations and one concurrent pair for each era. Require the PID to remain unchanged during those operations.

Strengthen the existing restart case. Close the first client in `finally`, start a new connection, require a different non-null PID, and complete one supported request on the new process. Keep every connect, request, and close bounded by the existing timeout helper. Preserve all fail-closed missing-state assertions and synthetic-only environment data.

Do not use `listTools()` as modern wire proof. Do not use an in-memory transport. Do not change `src/server.ts`, `package.json`, or `package-lock.json`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/test/server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Both protocol eras complete four retained-process operations on one stable PID.
    - Restart creates a new PID and completes a supported request.
    - All child-process paths have deterministic cleanup and bounded waits.
    - Missing state remains fail-closed and stdout remains protocol-only.
    - `git diff -- src/server.ts package.json package-lock.json` is empty.
  </acceptance_criteria>
  <done>The production executable proves negotiation, independent requests, restart, and missing-state failure without a capability shortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove handler-observed cancellation and retained-process reuse</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>test/server.test.ts</file>
    <file>test/stdio.test.ts</file>
    <file>src/server.ts</file>
    <file>src/stdio.ts</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-RESEARCH.md</file>
    <file>.planning/phases/01-local-runtime-and-standard-transport-foundation/01-PATTERNS.md</file>
  </read_first>
  <files>test/protocol-cancellation.test.ts, test/fixtures/protocol-cancellation-server.ts</files>
  <behavior>
    - The official client aborts one active synthetic tool request.
    - The fixture observes `ctx.mcpReq.signal` abort for that exact request.
    - The cancelled request yields no successful result.
    - The same child PID completes a supported request after cancellation.
    - Ignoring the handler signal or terminating the process on cancellation makes the focused test fail.
  </behavior>
  <action>
Create `test/fixtures/protocol-cancellation-server.ts` as a compiled test-only executable. Use the official MCP server and `serveStdio(factory)` packages. Register exactly one synthetic wait tool. Observe cancellation through the handler's request signal. Write one fixed observation marker to stderr so stdout remains protocol-only. Never load Toast credentials, Merchant Data, or the production runtime configuration.

Create `test/protocol-cancellation.test.ts`. Start the compiled fixture with `process.execPath` and official `StdioClientTransport`. Use the pinned modern official client. Capture the retained PID. Start the synthetic wait request with an `AbortController`, abort it, require client rejection, require explicit handler-side abort observation, and then send `discover()` on the same PID. Bound all operations and close the client in `finally`.

Run two scoped mutation checks. First, patch the fixture so the handler ignores its request signal and confirm the focused test fails. Restore that patch. Second, patch the fixture so cancellation ends the process and confirm the post-cancel request fails. Restore that patch. Record both caught mutations in the plan summary. Do not use `git checkout`, an in-memory transport, or `fakeServe` to restore or satisfy the proof.

Keep the synthetic tool outside `src/server.ts`. Do not add a production capability. Do not change dependency manifests or lockfiles.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/test/protocol-cancellation.test.js dist-test/test/server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The real SDK cancellation reaches the handler signal.
    - The cancelled call cannot return a successful tool result.
    - The retained child process remains usable after cancellation.
    - Both required temporary mutations fail the focused test and leave a clean restored worktree.
    - Production source exports and registered tools remain unchanged.
  </acceptance_criteria>
  <done>Issue #4 has authentic local protocol cancellation evidence, while production report cancellation remains a Phase 3 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub PR and issue state → Phase 1 execution | Remote exact-head review and merge facts control whether either plan may edit tracked files. |
| Official MCP client → child-process stdio | Untrusted protocol messages cross stdin and stdout into the test server. |
| Client abort → handler request signal | Cancellation must bind to the correct in-flight request without terminating unrelated process work. |
| Child stdout/stderr → test runner | Protocol output and diagnostic output can expose secrets or corrupt the stdio stream. |
| Test fixture → production package | Test-only capabilities must not become part of the shipped MCP surface. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Spoofing | Protocol-era negotiation | mitigate | Use official pinned legacy and modern clients against the compiled executable. |
| T-01-02 | Tampering | Cancellation request binding | mitigate | Observe the real handler signal and mutation-test ignored or process-ending cancellation. |
| T-01-03 | Repudiation | Protocol proof | mitigate | Record the exact PID assertions, commands, discovered test counts, and exact Git SHA. |
| T-01-04 | Information disclosure | Child output | mitigate | Use synthetic markers only, require empty stdout on startup failure, and assert no secret marker appears. |
| T-01-05 | Denial of service | Active handler and child lifecycle | mitigate | Bound every operation, close clients in `finally`, and prove post-cancel process reuse. |
| T-01-06 | Elevation of privilege | Production tool surface | mitigate | Keep the synthetic tool in `test/fixtures` and require no diff in `src/server.ts`. |
| T-01-07 | Spoofing / Repudiation | PR #37 prerequisite | mitigate | Match the current PR head, require an empty CLEAN-to-evidence comment interval, reject unresolved findings, verify issue evidence, and prove merge ancestry. |
| T-01-SC | Tampering | Package supply chain | mitigate | Add no package and preserve the PR #24 reviewed lockfile exactly. |

Any new HIGH severity finding blocks this plan. The executor must stop, record the finding, and preserve fail-closed behavior.
</threat_model>

<verification>
Run the shared preflight before tracked edits. Run the focused compiled tests after each implementation task. Confirm `git diff -- package.json package-lock.json src/server.ts` is empty. Confirm the test runner discovers the new cancellation test file.
</verification>

<success_criteria>
- The shared preflight proves PR #37 and issue #32 authority before either plan changes tracked state.
- GitHub issue #4 requirements for initialization, negotiation, independent requests, restart, reconnect, local cancellation, and missing-state failure have direct authentic tests.
- The official stdio packages and compiled child processes provide the proof.
- No production MCP tool, remote listener, dependency change, or Toast data access exists.
- The plan summary records both mutation results and the focused test count.
</success_criteria>

<output>
Create `.planning/phases/01-local-runtime-and-standard-transport-foundation/01-01-SUMMARY.md` when done.
</output>
