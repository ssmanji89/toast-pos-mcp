---
phase: 06-release-hardening-and-public-compatibility-proof
plan: "04"
type: tdd
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/index.ts
  - src/server.ts
  - src/report-tools.ts
  - test/server.test.ts
  - test/report-tools-e2e.test.ts
  - test/fixtures/installed-artifact-fetch-preload.ts
  - scripts/verify-t6-public-wiring-mutations.mjs
autonomous: false
requirements: [T6-003]
must_haves:
  truths:
    - "A legacy 2025 MCP client and a pinned modern MCP client list the same six registered tools from the compiled production executable."
    - "Both retained MCP eras call a Standard report through the same process-owned ApplicationRuntime and receive its deterministic Standard envelope."
    - "Every Standard tool advertises an output schema that accepts every handler status it can emit and rejects mismatched report/status envelopes."
    - "Standard report cancellation and provenance remain on the real request-to-response chain."
    - "The repair passes isolated mutations, Node 20.20.2 and Node 22.22.2 exact-head gates, and a fresh independent exact-head review without changing external release gates."
  artifacts:
    - path: "src/index.ts"
      provides: "One process-owned ApplicationRuntime captured by both stdio protocol-era server factories"
    - path: "src/report-tools.ts"
      provides: "Per-Standard-tool complete/incomplete/denied output-schema unions"
    - path: "test/server.test.ts"
      provides: "Compiled production-index legacy and modern list/call regression proof"
    - path: "scripts/verify-t6-public-wiring-mutations.mjs"
      provides: "Isolated behavioral mutation proof for public runtime wiring and output contracts"
  key_links:
    - from: "src/index.ts"
      to: "src/server.ts"
      via: "Both legacy and modern factory calls pass the same startup ApplicationRuntime to createServer"
      pattern: "createServer\\(\\{ runtime"
    - from: "src/server.ts"
      to: "src/report-tools.ts"
      via: "The runtime-present branch registers Standard report tools for both eras"
      pattern: "registerStandardReportTools\\(server, options\\.runtime\\)"
    - from: "src/report-tools.ts"
      to: "src/runtime.ts"
      via: "Every registered handler forwards ctx.mcpReq.signal to the shared-runtime report builder"
      pattern: "signal: ctx\\.mcpReq\\.signal"
---

<objective>
Repair the legacy public MCP runtime wiring and the Standard report output-schema contract.

Purpose: Both supported local stdio clients must reach one process-owned
reporting runtime, while MCP output schemas accurately describe all explicit
fail-closed report states.
Output: Production wiring and per-tool schema unions with authentic compiled
stdio evidence, mutation coverage, and exact-head PR review gates.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-CONTEXT.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-03-SUMMARY.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-04-RESEARCH.md
@src/index.ts
@src/server.ts
@src/stdio.ts
@src/report-tools.ts
@test/server.test.ts
@test/report-tools-e2e.test.ts
@test/fixtures/installed-artifact-fetch-preload.ts
@scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs
</context>

<source_audit>

| Source | ID | Required fact or decision | Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 6 | Public compatibility evidence must prove the real stdio chain without a release-authority claim. | Tasks 1-3 | COVERED |
| REQ | T6-003 | Retained stdio clients and package-facing public contracts stay production-wired and independently reviewed. | Tasks 1-3 | COVERED |
| RESEARCH | PW-01 | Legacy and modern factories must capture one process-owned ApplicationRuntime. | Tasks 1-3 | COVERED |
| RESEARCH | PW-02 | Per-tool Standard output schemas must cover actual complete, denied, and labor incomplete handler results. | Tasks 1-3 | COVERED |
| RESEARCH | Cancellation and provenance | Preserve the request signal and report provenance while changing registration/schema code. | Tasks 1-3 | COVERED |
| RESEARCH | Node and mutation gates | Run authentic Node 20.20.2/22.22.2 restores plus isolated behavioral mutations. | Task 3 | COVERED |
| CONTEXT | D-01 | Separate implemented repair evidence from synthetic tests and external gates. | Task 3 | COVERED |
| CONTEXT | D-02, D-03 | Make no approval, endorsement, publication, or legal-sufficiency claim. | Task 3 | COVERED |
| CONTEXT | D-04, D-05, D-06 | Preserve consent, credential, AI-processing, and excluded-data boundaries while using invented fixtures only. | Tasks 1-3 | COVERED |
| CONTEXT | D-07, D-08 | Retain the actual Standard tool set and the body-free Analytics boundary. | Tasks 1-2 | COVERED |
| CONTEXT | D-09 | Keep first-request cancellation, G01, live compatibility, signing, and publication open. | Task 3 | COVERED |

</source_audit>

<dependency_graph>

| Task | Needs | Creates | Wave dependency |
| --- | --- | --- | --- |
| 1 | Current production index/server/report registrations and invented preload fixture | Failing compiled legacy/modern and schema-contract tests | None |
| 2 | Task 1 failing contracts | Shared runtime wiring and sound per-tool schema unions | Task 1 |
| 3 | Task 2 green candidate | Isolated mutation proof and exact-head Node/PR evidence | Task 2 |

</dependency_graph>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add failing public-runtime and output-schema contracts</name>
  <files>test/server.test.ts, test/report-tools-e2e.test.ts, test/fixtures/installed-artifact-fetch-preload.ts</files>
  <behavior>
    - Test 1: A raw legacy `2025-06-18` client starts the compiled `dist/index.js` under an external invented-fetch preload, lists exactly the five Standard tools and `toast_analytics_metrics_day`, and calls `toast_sales_summary` successfully.
    - Test 2: A pinned modern `2026-07-28` client starts a separate compiled production process with the same fixture contract, lists the same tools, and receives the same complete Standard report identity, source, schema version, and provenance fields.
    - Test 3: An inaccessible selected restaurant returns the existing Standard `denied` envelope with a denial code and no fabricated aggregate field in both eras.
    - Test 4: Existing nonzero active-request cancellation asserts the handler-observed abort marker, and successful reports retain source, location-context, and request provenance fields.
    - Test 5: The `tools/list` output schemas for sales, payment, item, cash, and labor expose only their real report literal and status branches: complete plus denied for the first four, and complete/incomplete/denied for labor. A missing denial branch, an added irrelevant status, a missing denial object, or a missing item dimension fails.
  </behavior>
  <action>Replace the legacy empty-list/denied-call expectation in `test/server.test.ts` with an authentic compiled-index proof. Start `dist/index.js`, not a fixture server, under the compiled external preload from `test/fixtures/installed-artifact-fetch-preload.ts`; pass only invented configuration and the preload import through the child environment. Keep raw JSON-RPC initialization for legacy and the official pinned client for modern. For each era, assert the six exact registered names, invoke `toast_sales_summary` for the invented business date, and require the existing schema-version-one complete Standard envelope with `source: standard_api`, report literal, restaurant GUID, context provenance, and source request provenance. Add the inaccessible restaurant invocation that proves the existing denied envelope has `isError`, a structured denial, and no report total. Extend the external preload only with an opt-in invented abortable Orders response and non-secret start/abort markers so the existing active-request cancellation proof can exercise the production index without a live fallback. Keep the first request cancellation issue outside these tests per D-09. In `test/report-tools-e2e.test.ts`, inspect the real `tools/list` schema emitted through stdio and assert each named Standard tool's discriminated JSON-schema branches and required structural fields against the exact handler-result matrix in `06-04-RESEARCH.md`; use the current fixture-denial and labor-incomplete calls to prove those schemas match real responses. Do not modify Analytics input/output behavior, add live credentials, process Merchant Data, or alter public documentation in this task. Run the focused contracts before production edits and record the expected RED result without changing their assertions.</action>
  <verify>
    <automated>npm run build:test &amp;&amp; ! node --test --enable-source-maps dist-test/test/server.test.js dist-test/test/report-tools-e2e.test.js</automated>
  </verify>
  <done>The focused compiled tests fail on the current legacy runtime omission and incomplete advertised output schemas. The tests already prove the production executable path, both client eras, deterministic denial/success envelopes, active cancellation, provenance, and exact per-tool schema branches.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire both eras to one runtime and publish sound Standard unions</name>
  <files>src/index.ts, src/server.ts, src/report-tools.ts, scripts/verify-t6-public-wiring-mutations.mjs</files>
  <behavior>
    - Test 1: The factory passes the one startup ApplicationRuntime into `createServer()` for legacy and modern protocol eras; legacy retains only its required capability metadata difference.
    - Test 2: Every registered Standard handler continues to forward `ctx.mcpReq.signal` to its existing report builder and returns the result's unchanged `structuredContent` and denied error flag behavior.
    - Test 3: Sales, payment, item, and cash advertise complete-or-denied unions with their own report literal. Labor advertises complete-or-incomplete-or-denied branches with the labor report literal.
    - Test 4: Complete/incomplete schema branches retain the mandatory source, business-date, context, provenance, formula, warnings, and report-specific fields. Denied branches require the failure metadata and scope arrays while permitting unresolved location context only where current builders omit it.
  </behavior>
  <action>In `src/index.ts`, construct `ApplicationRuntime` once before `startStdioServer()` and pass that exact object to every `createServer()` factory result. Preserve the legacy tool-list capability metadata through an explicit era-derived option; do not use protocol era to select a runtime-less server. In `src/server.ts`, correct the runtime and legacy comments so the no-runtime constructor remains a side-effect-free test seam only, while both production eras register Standard and existing Analytics tools against the captured process runtime. In `src/report-tools.ts`, replace the shared complete-only advertised schema with named per-tool Zod unions derived from the current `SalesSummaryResult`, `PaymentSummaryResult`, `ItemSalesSummaryResult`, `CashSummaryResult`, and `LaborSummaryResult` interfaces. Use a strict common Standard envelope plus report-specific complete/incomplete fields, and a report-specific denied branch that requires schema version, source, report, business dates, generation time, denial object, scope arrays, formula notes, and warnings. Keep optional only the current pre-resolution location-context fields. Preserve open-string report data through existing report-field validation; do not add closed Toast enums, broad `unknown` output branches, or a permissive catch-all branch. Preserve every builder invocation and its `ctx.mcpReq.signal` forwarding exactly. Create an isolated-worktree mutation harness following `scripts/verify-t5-003-analytics-mcp-guard-mutations.mjs`. The harness must restore the committed lockfile in its temporary worktree, mutate one unique source marker at a time, compile, run the named focused behavioral test, restore the file, and remove the worktree. Cover at least: removing the legacy runtime argument in `src/index.ts`; replacing the captured runtime with a fresh runtime expression; suppressing Standard registration when legacy metadata is present; replacing a handler request signal; removing complete-report provenance; removing the denied schema branch from each four two-state tools; removing the labor incomplete or denied branch; widening a report literal; and making a required denial or item-dimension field optional. Require each compiling mutation to fail its named behavior test, then require the restored focused suite and a clean temporary diff. After all tests are green, commit one candidate head without changing `package.json`, `package-lock.json`, `private: true`, signing/publish metadata, `LOOP.md`, `ROADMAP.md`, `STATE.md`, release status, external gates, Analytics G01, first-request cancellation, or live-credential policy. Push a non-main branch and create a PR that links the two integration-audit defects. Under Node 20.20.2 and then Node 22.22.2, confirm the candidate SHA before and after authentic `npm ci --no-audit --no-fund`, `npm run check`, the focused compiled public-wiring tests, and the mutation harness. Record Node/npm versions, discovered test count, total tests, caught/surviving mutation count, exact head, changed paths, and `DOX: updated` in the PR evidence. Request a fresh independent review of that exact head. Do not merge or describe the candidate as release-ready. Record that #4/T6-003 first-tool-request cancellation, T5-003-G01, #28, live Standard/Analytics compatibility, signing, publication, consent, Terms, and brand approval remain open.</action>
  <verify>
    <automated>candidate_sha="$(git rev-parse HEAD)" &amp;&amp; test "$(git branch --show-current)" != main &amp;&amp; nvm exec 20.20.2 sh -lc 'npm ci --no-audit --no-fund &amp;&amp; npm run check &amp;&amp; node --test --enable-source-maps dist-test/test/server.test.js dist-test/test/report-tools-e2e.test.js &amp;&amp; node scripts/verify-t6-public-wiring-mutations.mjs' &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; nvm exec 22.22.2 sh -lc 'npm ci --no-audit --no-fund &amp;&amp; npm run check &amp;&amp; node --test --enable-source-maps dist-test/test/server.test.js dist-test/test/report-tools-e2e.test.js &amp;&amp; node scripts/verify-t6-public-wiring-mutations.mjs' &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; git diff --check &amp;&amp; gh pr view --json url,headRefName,baseRefName</automated>
  </verify>
  <done>Legacy and modern production stdio processes list and call the same runtime-backed tools. Each Standard output schema is a sound per-tool union for every real handler outcome, successful/denied paths retain cancellation and provenance behavior, and the candidate PR contains exact Node/mutation evidence plus a fresh-review request.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Obtain independent exact-head review</name>
  <action>Pause after the PR evidence is complete. A reviewer, not the builder, must inspect the immutable candidate head before any merge decision.</action>
  <what-built>The PR has one exact candidate SHA with compiled legacy/modern production-index proof, Standard output-schema unions, isolated mutations, and Node 20.20.2/22.22.2 evidence.</what-built>
  <how-to-verify>
    1. Review the exact PR head, not a later branch state.
    2. Confirm the PR evidence contains both Node gates, focused tests, mutation count, changed paths, and `DOX: updated`.
    3. Confirm the change does not alter package metadata, Analytics G01, first-request cancellation, live credentials, or release-control-plane records.
    4. Record a CLEAN review or list required findings. Do not treat the review as Toast, Merchant, consent, signing, publication, Terms, or brand approval.
  </how-to-verify>
  <verify>
    <automated>gh pr view --json headRefOid,url,reviewDecision</automated>
  </verify>
  <done>An independent reviewer records CLEAN for the exact candidate SHA, or reports findings for a new repair cycle. The review does not close an external authorization or release gate.</done>
  <resume-signal>Type "CLEAN <candidate SHA>" or list required findings.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| MCP protocol era to process runtime | Legacy and modern session state may vary, but credentials, location state, transport, and rate limits must remain process-owned. |
| Handler result to declared schema | An MCP client relies on tool metadata to validate complete, incomplete, and denied structured output. |
| Synthetic stdio test to release claim | Invented fixture proof cannot establish live Toast compatibility, authorization, consent, or publication authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-04-01 | Spoofing | Legacy factory | mitigate | Prove raw legacy list/call behavior from the compiled production index and require the same captured runtime identity as modern. |
| T-06-04-02 | Tampering | Standard output declarations | mitigate | Use per-tool discriminated Zod unions and isolated mutations for every real status branch and required failure field. |
| T-06-04-03 | Information disclosure | Synthetic subprocess environment | mitigate | Use invented preload values and routes only; retain generic startup errors and never persist raw child stderr, credentials, or Merchant Data. |
| T-06-04-04 | Denial of service | Report cancellation | mitigate | Retain request-signal forwarding and prove handler-observed active cancellation; leave the owned request-zero limitation open. |
| T-06-04-05 | Repudiation | Candidate evidence | mitigate | Record exact head, runtime matrix, mutation count, commands, changed paths, and fresh-review result in the PR. |
| T-06-04-SC | Tampering | npm dependency restoration | accept | Restore only the committed lockfile with authentic `npm ci --no-audit --no-fund`; add or upgrade no package. |
</threat_model>

<verification>
1. Run the focused RED contracts before production edits.
2. Run the focused GREEN tests after runtime/schema changes.
3. Run the isolated mutation harness and confirm every named mutation is caught.
4. Validate the unchanged candidate through committed-lockfile restores on Node 20.20.2 and Node 22.22.2.
5. Confirm the PR references one candidate SHA and request independent review of that exact head.
6. Inspect the final changed paths and diff. Confirm no package metadata, release-control-plane, live-data, credential, Analytics-completion, or external-gate change exists.
</verification>

<success_criteria>
- Both retained MCP protocol eras use one runtime-backed public tool surface from the production executable.
- Standard output schemas accurately validate actual complete, denied, and labor incomplete envelopes without permissive fallback branches.
- Request cancellation and report provenance remain connected to the runtime-backed handler chain.
- Node 20.20.2, Node 22.22.2, and isolated mutation gates pass on one immutable candidate.
- A fresh independent exact-head review is requested before merge, and external gates remain open.
</success_criteria>

<output>
Create `.planning/phases/06-release-hardening-and-public-compatibility-proof/06-04-SUMMARY.md` when done.
</output>
