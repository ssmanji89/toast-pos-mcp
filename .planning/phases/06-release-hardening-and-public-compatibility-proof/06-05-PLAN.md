---
phase: 06-release-hardening-and-public-compatibility-proof
plan: "05"
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - src/report-tools.ts
  - test/report-tools-e2e.test.ts
  - scripts/verify-t6-public-wiring-mutations.mjs
  - .planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md
autonomous: true
requirements: [T6-003]
must_haves:
  truths:
    - "Phase 06 validation states only evidence that an exact command and named test support."
    - "The validation map records existing 06-04 runtime, output-branch, and mutation evidence only when tests directly prove it."
    - "Published Standard nested schemas become stricter only for nested shapes proved by real handler fixtures and tests."
    - "Unknown Toast-derived enum values remain accepted string data in successful Standard reports."
    - "PR #55 final-review, first-request cancellation, T5-003-G01, live compatibility, signing, publication, consent, Terms, and brand gates remain open."
    - "D-01: The validation record separates implemented local evidence from every pending external gate."
    - "D-02: and D-03: No validation or test result claims approval, publication, or legal sufficiency."
    - "D-04:, D-05:, and D-06: The work preserves consent, credentials, no-training, and guest-data exclusions."
    - "D-07: and D-08: The five Standard tools remain distinct from the incomplete-only Analytics lifecycle tool."
    - "D-09: The named live, package, and authority gates remain open."
  artifacts:
    - path: "test/report-tools-e2e.test.ts"
      provides: "Nested Standard output-schema and open-enum regression evidence"
    - path: "src/report-tools.ts"
      provides: "Specific nested output schemas only where fixture-proved"
    - path: "scripts/verify-t6-public-wiring-mutations.mjs"
      provides: "Mutation evidence for each newly strict nested schema contract"
    - path: ".planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md"
      provides: "Reconciled Phase 06 validation map with explicit limitations"
  key_links:
    - from: "test/report-tools-e2e.test.ts"
      to: "src/report-tools.ts"
      via: "tools/list emitted outputSchema and invoked invented-fixture report results"
      pattern: "listTools|outputSchema"
    - from: "scripts/verify-t6-public-wiring-mutations.mjs"
      to: "test/report-tools-e2e.test.ts"
      via: "each nested-schema mutation runs its named behavioral test"
      pattern: "focusedReportToolsTest"
    - from: ".planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md"
      to: "test/server.test.ts"
      via: "06-04 runtime evidence cites the named compiled production test"
      pattern: "serves retained legacy 2025 requests"
---

<objective>
Reconcile Phase 06 validation records and make nested Standard output schemas
specific only where exact fixture-based evidence supports that contract.

Purpose: Release evidence must distinguish tested local behavior from pending
review and external authority. Published schemas must reject unproved shape
drift without treating evolving Toast enum values as closed enums.
Output: A tested nested-schema decision, mutation evidence when code changes,
and a truthful Phase 06 validation map.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-CONTEXT.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-04-SUMMARY.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-05-RESEARCH.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md
@src/report-tools.ts
@test/report-tools-e2e.test.ts
@test/server.test.ts
@test/public-operator-docs.test.ts
@scripts/verify-t6-public-wiring-mutations.mjs
</context>

<source_audit>

| Source | ID | Required fact or decision | Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 6 | Evidence must not make unresolved production compatibility appear complete. | Tasks 1-3 | COVERED |
| REQ | T6-003 | Retain package-facing runtime and public contract evidence without a release-authority claim. | Tasks 1-3 | COVERED |
| REQ | Formal phase requirement set | `.planning/REQUIREMENTS.md` and Phase 06 requirement IDs are absent. | Task 3 records this limitation; no formal coverage claim | BLOCKED BY MISSING SOURCE |
| RESEARCH | VA-01 to VA-05 | Use exact current PR/main facts, repair the validation contradiction, and tie 06-04 rows to named tests only. | Task 3 | COVERED |
| RESEARCH | Nested-schema finding | Test emitted nested schemas and fixture results before any strict nested shape change. | Tasks 1-2 | COVERED |
| RESEARCH | Open enum guardrail | Preserve unknown Toast values as strings. | Tasks 1-2 | COVERED |
| CONTEXT | D-01: | Separate implemented local proof from pending external gates. | Task 3 | COVERED |
| CONTEXT | D-02:, D-03: | Do not claim approval, publication, or legal sufficiency. | Task 3 | COVERED |
| CONTEXT | D-04:, D-05:, D-06: | Preserve credential, consent, provider, no-training, and guest-data boundaries. | Tasks 1-3 | COVERED |
| CONTEXT | D-07:, D-08: | Preserve the five Standard tools and the body-free Analytics boundary. | Tasks 1-2 | COVERED |
| CONTEXT | D-09: | Keep all named release and live-proof gates open. | Task 3 | COVERED |

</source_audit>

<dependency_graph>

| Task | Needs | Creates | Wave dependency |
| --- | --- | --- | --- |
| 1 | Current emitted Standard schemas, invented fixture results, and 06-04 test seams | Failing nested-contract tests and a recorded shape decision | None |
| 2 | Task 1 exact failing contracts | Fixture-proved strict schemas and matching mutation guards, if evidence supports them | Task 1 |
| 3 | Task 2 exact command outcomes and current GitHub review state | Reconciled validation map with faithful 06-04 rows and retained limitations | Task 2 |

</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove or reject each proposed nested Standard contract</name>
  <files>test/report-tools-e2e.test.ts</files>
  <behavior>
    - Test 1: Each candidate nested output field is checked in the `tools/list` JSON Schema and in an invoked invented-fixture complete response before it is treated as a strict contract.
    - Test 2: A required nested field missing from a candidate strict shape fails the focused schema test.
    - Test 3: The existing invented unknown item-tag value remains a successful string value and is not validated as a closed enum.
    - Test 4: A nested field that the evidence identifies as extensible is asserted as intentionally open, with no false strictness claim.
  </behavior>
  <action>Read every `z.object({}).passthrough()` complete-report field in `src/report-tools.ts` and the exact complete result interfaces/builders that supply it. For each field, classify it as fixed-shape, open-key map, open Toast-derived string data, or intentionally extensible. Add failing focused cases to `test/report-tools-e2e.test.ts` that call the current stdio fixture and inspect the emitted `tools/list` JSON Schema plus the matching structured report output. Require `additionalProperties: false` and exact nested required fields only for fixed-shape candidates that the invoked result proves. For maps, require the correct value schema without requiring a closed key set. Retain the existing `NEW_ENUM_TAG` fixture behavior as an explicit open-string regression. Do not change production code in this task. Use only invented fixtures. Preserve the Standard five-tool set and the body-free Analytics behavior per D-07 and D-08. Preserve consent, credential, no-training, guest-data, and no-live-data boundaries per D-04 through D-06.</action>
  <verify>
    <automated>npm run build:test && ! node --test --enable-source-maps --test-name-pattern='nested Standard output schemas' dist-test/test/report-tools-e2e.test.js</automated>
  </verify>
  <done>The focused test fails against each proposed strict nested contract that lacks an exact implementation, and it records why each non-strict nested field must remain open. The test proves unknown Toast-derived values stay strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement only fixture-proved nested schemas and mutation guards</name>
  <files>src/report-tools.ts, test/report-tools-e2e.test.ts, scripts/verify-t6-public-wiring-mutations.mjs</files>
  <behavior>
    - Test 1: Every fixed-shape nested field identified by Task 1 emits the exact strict JSON Schema and accepts the real invented complete response.
    - Test 2: Open-key maps and Toast-derived values preserve their documented open-string behavior.
    - Test 3: Replacing each newly strict nested schema with an open catch-all causes its named focused schema test to fail.
    - Test 4: The existing complete, incomplete, denied, provenance, cancellation, and item-dimension contracts remain unchanged.
  </behavior>
  <action>Replace only the fixture-proved fixed-shape `z.object({}).passthrough()` fields in `src/report-tools.ts` with named Zod 4 nested schemas. Use `z.strictObject()` for fixed objects, typed record schemas for open-key maps, and `z.string().min(1)` for open Toast-derived string values. Do not close evolving Toast values with `z.enum`, do not add broad `unknown` output branches, and do not alter report formulas, handler signal forwarding, status unions, Analytics behavior, package metadata, or any release gate. If Task 1 finds no fixed-shape evidence for a field, leave that field open and retain its documented reason in the test and validation record. Extend `scripts/verify-t6-public-wiring-mutations.mjs` only for schemas made strict: each mutation must be unique, compile in its isolated worktree, run its named schema test, fail that test, restore the original file, and leave the temporary worktree clean. Run the full existing three mutation batches after the focused green test. Preserve D-01 separation of local evidence from external gates and D-09 open gates.</action>
  <verify>
    <automated>npm run build:test && node --test --enable-source-maps --test-name-pattern='nested Standard output schemas|tools/list advertises only the real Standard result branches|item tags use the menu group selected by the Orders itemGroup' dist-test/test/report-tools-e2e.test.js && T6_PUBLIC_WIRING_GUARD_BATCH=first node scripts/verify-t6-public-wiring-mutations.mjs && T6_PUBLIC_WIRING_GUARD_BATCH=second node scripts/verify-t6-public-wiring-mutations.mjs && T6_PUBLIC_WIRING_GUARD_BATCH=third node scripts/verify-t6-public-wiring-mutations.mjs</automated>
  </verify>
  <done>Every newly strict nested schema is supported by an invoked fixture result and a failing mutation. Any field without that evidence remains explicitly open. Open Toast enum values still pass as strings.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the Phase 06 validation map without closing gates</name>
  <files>.planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md</files>
  <action>Run the focused public-documentation test, the compiled 06-04 runtime test, the 06-04 output-branch test, the nested-schema test, and the applicable mutation batches on one unchanged candidate SHA. Update `06-VALIDATION.md` from those exact outcomes only. Replace the contradictory phase-wide complete/Nyquist/Wave-0 header claims with conservative incomplete values until a formal phase requirement source and every required gate are present. Reconcile each T6-002 row to green only if the fresh named documentation command passes; otherwise keep its failure or pending state with the command output reference. Add 06-04 rows only for the compiled legacy/modern runtime test, the existing top-level output-branch test, and the 14-guard mutation harness. Do not extend those rows to nested schema claims unless Task 2's named test and mutation evidence exists. Add separate limitation rows for the absent global requirements source and the PR #55 final metadata-head independent review, marked pending rather than complete. Record exact SHA, command, result, test count where available, and `DOX: updated`. Keep first-request cancellation, T5-003-G01, #28, live Standard/Analytics compatibility, signing, publication, consent, Terms, and brand gates open per D-01 through D-03 and D-09. Do not modify `LOOP.md`, `ROADMAP.md`, `STATE.md`, public documentation, package metadata, or GitHub gate state.</action>
  <verify>
    <automated>npm run build:test && node --test --enable-source-maps dist-test/test/public-operator-docs.test.js dist-test/test/server.test.js dist-test/test/report-tools-e2e.test.js && git diff --check && rg -n 'T6-002-DOCS-01|T6-004-PUBLIC-01|formal phase requirement source|db1270e963850aef3fb5bbb5c6fad402fdb212e2|DOX: updated' .planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md</automated>
  </verify>
  <done>The validation front matter and all rows agree with observed commands. New 06-04 entries claim only tested behavior. The missing formal requirements source and PR #55 review remain explicit blockers, and no external release gate is marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toast-derived normalized report data → MCP output schema | Evolving vendor data crosses into a public machine-readable contract. |
| Candidate test evidence → Phase validation record | Local test outcomes become durable release evidence. |
| GitHub PR metadata → validation record | External review status is read-only evidence and cannot be self-certified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | `src/report-tools.ts` nested schemas | mitigate | Task 1 maps each shape to invoked fixture output; Task 2 adds strict schemas and mutation tests only where evidence proves them. |
| T-06-05-02 | Information disclosure | Standard output contract | mitigate | Preserve report aggregation, guest-data exclusions, and open-string enum handling; do not use permissive nested catch-alls for fixture-proved fixed records. |
| T-06-05-03 | Repudiation | `06-VALIDATION.md` | mitigate | Task 3 records candidate SHA, commands, named tests, and pending external review rather than inferred completion. |
| T-06-05-04 | Elevation of privilege | Release/evidence gates | mitigate | Task 3 cannot mark independent review, consent, Terms, signing, publication, or live compatibility complete. |
| T-06-05-SC | Tampering | npm installs | accept | No package-manager change occurs. Validation restores the committed lockfile only. |
</threat_model>

<verification>
Run the focused RED contract before changing production schemas. Run the focused
green contract and every applicable isolated mutation batch after the change.
Run the reconciled validation commands on one unchanged candidate SHA. Confirm
that no claimed 06-04 row lacks a named direct test. Confirm GitHub still shows
the PR #55 final-review gate as pending before recording it.
</verification>

<success_criteria>
- Phase validation makes no contradictory completion claim.
- Every strict nested schema has direct fixture and mutation evidence.
- Unknown Toast-derived values remain open strings.
- 06-04 rows and T6-002 rows identify only real test evidence.
- All external release gates remain visibly open.
</success_criteria>

<output>
Create `.planning/phases/06-release-hardening-and-public-compatibility-proof/06-05-SUMMARY.md` when done.
</output>
