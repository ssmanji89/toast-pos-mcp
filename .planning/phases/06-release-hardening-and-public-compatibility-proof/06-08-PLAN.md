---
phase: 06-release-hardening-and-public-compatibility-proof
plan: "08"
type: execute
wave: 8
depends_on: ["06-07"]
files_modified:
  - scripts/verify-gate60-first-tool-cancellation-mutations.mjs
  - .planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md
  - LOOP.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [T6-003]
must_haves:
  truths:
    - "Every mutation that removes request-zero recognition, exact-ID matching, Standard signal propagation, callback cleanup, or production-process proof fails its named test."
    - "Node 20.20.2 and Node 22.22.2 validate one unchanged candidate through authentic committed-lockfile restores and the executable request-zero test."
    - "The ledger, state snapshot, roadmap, and validation map report only exact local implementation and evidence facts after validation."
    - "All unrelated external gates remain open, and an independent reviewer examines the immutable candidate before merge."
  artifacts:
    - path: "scripts/verify-gate60-first-tool-cancellation-mutations.mjs"
      provides: "Isolated mutation proof for the request-zero cancellation bridge"
    - path: ".planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md"
      provides: "Nyquist evidence map for issue #60"
    - path: "LOOP.md"
      provides: "Authoritative record of the exact issue #60 candidate evidence and remaining gates"
  key_links:
    - from: "scripts/verify-gate60-first-tool-cancellation-mutations.mjs"
      to: "test/first-tool-cancellation-e2e.test.ts"
      via: "Each compiling source mutation runs the real executable-process conformance test"
      pattern: "first-tool-cancellation-e2e"
    - from: ".planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md"
      to: "LOOP.md"
      via: "Both name candidate SHA, Node 20/22 results, independent review status, and remaining gates"
      pattern: "#60"
---

<objective>
Mutation-test, exact-head validate, and independently review the request-zero
cancellation correction.

Purpose: Prove the local correction survives targeted regressions and does not
turn local synthetic execution into a release-authority claim.
Output: Mutation evidence, dual-runtime immutable-candidate evidence, precise
control-plane records, and an independent-review gate.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-CONTEXT.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-07-RESEARCH.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-07-SUMMARY.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md
@src/mcp-request-cancellation.ts
@test/first-tool-cancellation-e2e.test.ts
@scripts/verify-t6-public-wiring-mutations.mjs
</context>

<source_audit>

SOURCE | ID | Required fact or decision | Coverage | Status
--- | --- | --- | --- | ---
GOAL | Phase 06 | Exact-head validation and review must distinguish local evidence from release authority. | Tasks 1-3 | COVERED
REQ | T6-003 | The package-facing process behavior must pass supported Node runtime gates. | Tasks 1-2 | COVERED
REQ | #60 | The correction must cover request-zero and preserve the complete production cancellation chain. | Tasks 1-2 | COVERED
RESEARCH | Mutation resistance | Source changes need isolated compiling mutations, not only a green aggregate suite. | Task 1 | COVERED
RESEARCH | Node 20/22 baseline | The same committed-lockfile candidate requires both clean-worktree gates. | Task 2 | COVERED
CONTEXT | D-01 | Record implementation, synthetic validation, and external gates separately. | Task 2 | COVERED
CONTEXT | D-02, D-03 | Do not claim Toast approval, publication, consent, or legal sufficiency. | Task 2 | COVERED
CONTEXT | D-04, D-05, D-06 | Retain the consent acknowledgment boundary and excluded-data policy. | Tasks 1-2 | COVERED
CONTEXT | D-07, D-08 | Preserve Standard tools and body-free Analytics behavior. | Task 1 | COVERED
CONTEXT | D-09 | Keep G01, live compatibility, signing, and publication explicit until independently owned proof exists. | Tasks 2-3 | COVERED

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isolated cancellation-bridge mutation proof</name>
  <files>scripts/verify-gate60-first-tool-cancellation-mutations.mjs</files>
  <behavior>
    - Test 1: A mutation that excludes numeric ID zero causes the first-tool process test to fail.
    - Test 2: A mutation that ignores the cancellation request ID or skips the combined report signal causes the named process test to fail.
    - Test 3: A mutation that omits active-callback cleanup causes the retained-process reuse or shutdown assertion to fail.
    - Test 4: A mutation that bypasses a registered Standard or Analytics callback causes its existing focused stdio test to fail.
  </behavior>
  <action>Create `scripts/verify-gate60-first-tool-cancellation-mutations.mjs` using the existing isolated-worktree mutation-harness pattern. For each one-marker mutation, create a temporary detached worktree from the candidate, restore only the committed lockfile, apply one compiling mutation, build, and run the exact named focused test. Require that test to fail, restore the source, remove the worktree, then run the restored focused suite and require clean tracked and index diffs. Mutate exactly these bridge properties: numeric-zero eligibility, exact request-ID map lookup, bridge-to-runtime signal composition, callback-finally cleanup, Standard callback wrapping, and Analytics callback wrapping. Run the actual executable first-tool test for the first four mutations. Run the existing Standard and Analytics stdio tests for registration mutations. Do not alter test assertions, use a source fixture as proof, add dependencies, edit package metadata, or use mutation survivors as acceptable evidence.</action>
  <verify>
    <automated>node scripts/verify-gate60-first-tool-cancellation-mutations.mjs</automated>
  </verify>
  <done>Every compiling mutation fails its named behavior test. The restored focused suite passes, and no temporary worktree or source change remains.</done>
</task>

<task type="auto">
  <name>Task 2: Validate one immutable candidate and record bounded evidence</name>
  <files>.planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md, LOOP.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>After Task 1 and every tracked implementation change are committed, set `CANDIDATE_SHA` once. Create separate detached clean worktrees for Node 20.20.2 and Node 22.22.2 at that exact SHA. In each worktree, record SHA before validation, restore the committed lockfile with `npm ci --no-audit --no-fund`, run `npm run check`, run the compiled first-tool executable test, run the protocol-cancellation and report-tool process tests, run the gate-60 mutation harness, run `git diff --check`, require empty tracked and index diffs, record SHA after validation, and require equality with `CANDIDATE_SHA`. Record only candidate SHA, Node/npm versions, commands, exit states, discovered-test counts, test counts, mutation counts, bounded test markers, and clean-diff results. After both gates pass, update `06-VALIDATION.md` first, then authoritative `LOOP.md`, then the snapshot and roadmap projection. State that issue #60 has local exact-head implementation, synthetic executable-process validation, and independent review pending. Do not call it closed, publishable, live-compatible, consented, Toast-approved, signed, or legally sufficient before Task 3. Preserve #28, T5-003-G01, live Analytics, Merchant consent, Terms or brand, signing, publication, and the PR #55 and PR #58 GitHub-review gaps. Record `DOX: updated`. Do not capture raw stderr, environment values, tokens, Merchant Data, or copied Toast materials.</action>
  <verify>
    <automated>candidate_sha="$(git rev-parse HEAD)" &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; gate60_parent="$(mktemp -d)" &amp;&amp; gate60_node20="$gate60_parent/node20" &amp;&amp; gate60_node22="$gate60_parent/node22" &amp;&amp; git worktree add --detach "$gate60_node20" "$candidate_sha" &amp;&amp; git worktree add --detach "$gate60_node22" "$candidate_sha" &amp;&amp; (cd "$gate60_node20" &amp;&amp; source /Users/sully/.nvm/nvm.sh &amp;&amp; nvm use 20.20.2 &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; npm ci --no-audit --no-fund &amp;&amp; npm run check &amp;&amp; node --test --enable-source-maps dist-test/test/first-tool-cancellation-e2e.test.js dist-test/test/protocol-cancellation.test.js dist-test/test/report-tools-e2e.test.js &amp;&amp; node scripts/verify-gate60-first-tool-cancellation-mutations.mjs &amp;&amp; git diff --check &amp;&amp; git diff --quiet &amp;&amp; git diff --cached --quiet &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha") &amp;&amp; (cd "$gate60_node22" &amp;&amp; source /Users/sully/.nvm/nvm.sh &amp;&amp; nvm use 22.22.2 &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; npm ci --no-audit --no-fund &amp;&amp; npm run check &amp;&amp; node --test --enable-source-maps dist-test/test/first-tool-cancellation-e2e.test.js dist-test/test/protocol-cancellation.test.js dist-test/test/report-tools-e2e.test.js &amp;&amp; node scripts/verify-gate60-first-tool-cancellation-mutations.mjs &amp;&amp; git diff --check &amp;&amp; git diff --quiet &amp;&amp; git diff --cached --quiet &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha") &amp;&amp; git worktree remove "$gate60_node20" &amp;&amp; git worktree remove "$gate60_node22" &amp;&amp; rmdir "$gate60_parent" &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; git diff --check</automated>
  </verify>
  <done>Both supported Node versions validate the same clean candidate with authentic dependencies, executable stdio proof, and mutation proof. The control plane records only those observed local facts and retains every external gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Obtain independent exact-head cancellation review</name>
  <action>Request a findings-only review from a reviewer who did not build this candidate. Give the reviewer the immutable SHA, the two Node-version evidence records, the local bridge source, and the executable-process test. The reviewer must inspect the production chain and report findings without approving their own work.</action>
  <what-built>A request-zero local bridge, real production-process cancellation test, mutation proof, and Node 20/22 immutable-candidate evidence.</what-built>
  <how-to-verify>
    1. Review the immutable candidate SHA named in the pull request.
    2. Confirm the first tools/call is observed as numeric ID zero through the official client transport.
    3. Confirm cancellation reaches the compiled production executable, runtime, Standard source fetch, structured result boundary, process reuse, and shutdown.
    4. Confirm the local bridge uses only public SDK APIs and no SDK, package, transport, or lockfile substitution.
    5. Confirm the control plane leaves #28, G01, consent, Terms or brand, signing, publication, live compatibility, and pending legacy PR reviews open.
  </how-to-verify>
  <resume-signal>Record `CLEAN <candidate SHA>` or list required findings.</resume-signal>
  <verify>
    <automated>candidate_sha="$(git rev-parse HEAD)" &amp;&amp; git diff --check &amp;&amp; git diff --quiet &amp;&amp; git diff --cached --quiet &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; gh pr view --json headRefOid,url,reviewDecision</automated>
  </verify>
  <done>An independent reviewer records findings-only CLEAN for the exact candidate, or reports findings for a new candidate cycle. The builder does not self-approve and no external authority gate changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Candidate source to mutation worktree | A mutation must not contaminate the candidate or create a false green result. |
| Candidate to Node runtime evidence | Each supported runtime must restore real committed dependencies at the same SHA. |
| Local evidence to ledger language | Exact synthetic process evidence cannot grant external approval or live compatibility. |
| Builder to reviewer | The builder cannot approve its own correction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-08-01 | Tampering | Mutation harness | mitigate | Use one isolated compiling mutation, named behavior test, restoration, and clean-worktree assertion per case. |
| T-06-08-02 | Repudiation | Candidate validation | mitigate | Record before/after candidate SHAs, Node/npm versions, commands, results, and diffs for both runtimes. |
| T-06-08-03 | Spoofing | Release-control records | mitigate | Keep implementation, local synthetic validation, review, and external gates in separate fields. |
| T-06-08-04 | Information disclosure | Test and evidence output | mitigate | Record bounded markers and counts only; never persist stderr, environment, credentials, or Merchant Data. |
| T-06-08-05 | Elevation of privilege | Review gate | mitigate | Require independent exact-head review and preserve owner-controlled external gates. |
| T-06-08-SC | Tampering | npm supply chain | accept | Use only the committed lockfile and authentic `npm ci`; add, upgrade, or reconstruct no package. |
</threat_model>

<verification>
1. Run the isolated mutation harness and require zero survivors.
2. Run Node 20.20.2 and Node 22.22.2 clean-worktree gates at one immutable SHA.
3. Confirm every process test starts the actual executable through official stdio transport.
4. Confirm the final diff contains only planned implementation, test, script, and evidence-control paths.
5. Obtain independent findings-only review before merge.
</verification>

<success_criteria>
- Mutations of request-zero handling, ID isolation, signal propagation, cleanup, and registration cannot survive.
- Both supported Node runtimes prove the same candidate through real dependency restoration and executable stdio cancellation.
- The evidence records state only observed local implementation and validation facts.
- The reviewer evaluates the same immutable candidate.
- All unowned legal, consent, vendor, live-compatibility, signing, publication, and prior-review gates remain explicit and open.
</success_criteria>

<output>
Create `.planning/phases/06-release-hardening-and-public-compatibility-proof/06-08-SUMMARY.md` when done.
</output>
