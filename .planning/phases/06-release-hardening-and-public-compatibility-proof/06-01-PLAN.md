---
phase: 06-release-hardening-and-public-compatibility-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/operator-guide.md
  - docs/architecture/report-contract.md
  - docs/architecture/public-use-boundary.md
  - docs/architecture/threat-model.md
  - test/public-operator-docs.test.ts
autonomous: true
requirements: [T6-002]
must_haves:
  truths:
    - "An operator can identify each registered Standard tool, its Standard API source, and its documented completeness boundary."
    - "An operator can identify the Analytics lifecycle tool as body-free denied or incomplete output, not a complete Analytics report."
    - "An operator sees credential, Merchant-consent, AI-provider, logging, retention, subprocessor, no-training, and guest-data duties before configuration guidance."
    - "Public documentation separates implemented behavior, synthetic-test evidence, and still-open human or external gates."
    - "The documentation does not claim Toast approval, endorsement, certification, partnership, publication, installed-artifact proof, or live compatibility."
  artifacts:
    - path: "docs/operator-guide.md"
      provides: "Evidence-bound operator checklist and local-stdio safety guidance"
    - path: "docs/architecture/report-contract.md"
      provides: "Source-derived public tool and report-contract catalog"
    - path: "test/public-operator-docs.test.ts"
      provides: "Regression test connecting registered tool names to public documentation"
  key_links:
    - from: "src/report-tools.ts"
      to: "docs/architecture/report-contract.md"
      via: "The documentation contract test checks all five registered Standard tool names against the catalog."
      pattern: "toast_(sales|payment|item_sales|cash|labor)_summary"
    - from: "src/analytics-report-tools.ts"
      to: "README.md"
      via: "The documentation contract test checks the Analytics tool name, denied/incomplete states, and no-complete-schema limitation."
      pattern: "toast_analytics_metrics_day"
---

<objective>
Create the T6-002 public operator documentation contract without changing the release state.

Purpose: Give operators accurate, safety-first guidance while keeping implementation evidence, synthetic evidence, and external approval or live-compatibility gates separate.
Output: An operator guide, a source-derived report catalog, repaired public entry points, and a regression test for their durable claims.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-CONTEXT.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-RESEARCH.md
@README.md
@docs/architecture/public-use-boundary.md
@docs/architecture/threat-model.md
@docs/architecture/standard-report-tools.md
@src/server.ts
@src/report-tools.ts
@src/analytics-report-tools.ts
@src/report-contract.ts
@package.json
@scripts/run-tests.mjs
</context>

<mutable_ownership>

| Owner | Mutable paths or state | Rule |
|---|---|---|
| T6-002 | The six frontmatter-listed documentation and test paths | This plan is the only writer for these documentation contracts and their regression test. |
| T6-003 | Package bytes, tarball/install smoke, executable stdio proof, signing, and publication evidence | Do not create package artifacts, publish instructions, signatures, or release-ready claims. |
| T5-003-G01 | Verified complete Analytics result schema | Do not add a parser, result schema, formula, or complete-result statement for Analytics. |
| Human or Toast reviewers | Terms/brand approval, legal sufficiency, Merchant consent, third-party-provider approval, and live credentials | Record these only as gates. Do not create approval records or infer approval. |
| Campaign control plane | `LOOP.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` | Do not edit these files in this slice. Do not reconcile the rate-limit-reset record discrepancy. |
</mutable_ownership>

<tasks>

<task type="auto">
  <name>Task 1: Create the operator safety and Terms checkpoint</name>
  <files>docs/operator-guide.md, docs/architecture/public-use-boundary.md</files>
  <action>Create `docs/operator-guide.md` and update the durable public-use boundary. Put the safety checklist before every configuration reference, per D-04: authorized restaurant credentials; documented Merchant consent before AI processing; MCP host, model provider, prompts, tool logs, traces, human review, retention, and subprocessor review; no training, fine-tuning, evaluation for model improvement, or other model improvement without Toast prior written approval; and exclusions for guest-linked data, Analytics guest-payment data, delivery addresses, and payment identifiers. State per D-05 that `TOAST_MERCHANT_AI_CONSENT_ACKNOWLEDGED=true` records an operator acknowledgment and is not legal-sufficiency proof. Link directly to the Toast API Terms and record the observed 2026-06-23 Terms date per D-03; do not copy Terms text. State per D-02 and D-03 that brand-feature and public-use approval remain a human or Toast gate, without saying the Terms review grants approval. Use invented placeholders only and no credential values, Merchant Data, or API-derived fixtures. Add cross-links to the report catalog and README. Keep local stdio explicitly separate from authorization, consent, provider, logging, retention, and third-party-processing permission.</action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test --enable-source-maps dist-test/test/public-operator-docs.test.js</automated>
  </verify>
  <done>The operator guide places all safety duties before configuration guidance, records the Terms link/date, preserves exclusions, and identifies approvals as external gates without an endorsement, consent, or publication assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the evidence-bound tool and current-state documentation</name>
  <files>README.md, docs/architecture/report-contract.md, docs/architecture/threat-model.md</files>
  <action>Create `docs/architecture/report-contract.md`, update the README, and replace only the stale current-state narrative in the threat model. Catalog exactly the registered `toast_sales_summary`, `toast_payment_summary`, `toast_item_sales_summary`, `toast_cash_summary`, and `toast_labor_summary` Standard API tools, plus `toast_analytics_metrics_day`, from the source registrations per D-07. For every catalog entry, document source family, inputs, output schema/status behavior, freshness or completeness treatment, exclusions, formula/provenance boundary, and informational/non-GAAP limit where applicable. State per D-08 that the Analytics tool is a body-free lifecycle envelope that returns only `denied` or `incomplete`; retain `analytics_result_schema_unverified` and do not describe a completed Analytics body, formula, or report. Use an explicit three-state evidence model in README and catalog: implemented behavior, synthetic-test evidence, and remaining external gates, per D-01. Keep T5-003-G01, first-tool-request cancellation, live Standard compatibility, live Analytics compatibility, installed-artifact smoke, signing, and publication open per D-09. State no position for the rate-limit-reset control-plane discrepancy. Repair stale README and threat-model statements about absent report tools while preserving read-only, location-isolated, non-accounting, and no-endorsement boundaries. Do not add installation-from-tarball, signing, publishing, or live-credential procedures; those belong to T6-003 or an external gate.</action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test --enable-source-maps dist-test/test/public-operator-docs.test.js</automated>
  </verify>
  <done>Public entry points list the six current tools accurately, distinguish Standard calculations from constrained Analytics output, and separate code facts from synthetic evidence and unresolved release or approval gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add a documentation contract test and reviewable PR evidence model</name>
  <files>test/public-operator-docs.test.ts</files>
  <behavior>
    - Test 1: Every current registration name in `src/report-tools.ts` and `src/analytics-report-tools.ts` appears in the public report catalog and README.
    - Test 2: The public documents label Standard API and Analytics API as distinct source families, and Analytics permits only denied or incomplete envelopes while the schema gate remains unresolved.
    - Test 3: The operator-facing documents contain the Terms URL/date, non-endorsement boundary, acknowledgment-not-proof boundary, excluded guest-data boundary, and all three evidence-state labels.
    - Test 4: The public documents do not present the package as published, live-compatible, Toast-approved, or a complete Analytics-report provider.
  </behavior>
  <action>Write the failing TypeScript Node built-in test before implementation, then make the documentation pass it. Read only repository source and Markdown; the test must not start the server, read environment credentials, contact Toast, or load Merchant Data. Assert the registered names in source and the required documentation statements together, so a tool-registration or wording drift fails deterministically. Scope negative assertions to public-document content and preserve neutral Terms references. In the task summary and pull-request evidence, use this separate evidence model: (1) implementation — exact documentation and test paths changed; (2) synthetic validation — focused test and full repository-gate commands with discovered-test counts and exact results; (3) external gates — T5-003-G01, #4/T6-003, #28, live Analytics, installed-artifact smoke, signing, publication, and human brand/Terms approvals remain open. Record `DOX: updated`, issue #22, the exact commit head, the observed Terms date/link, source-registration files reviewed, and a diff scan result showing no secret, Merchant Data, copied Toast documentation, approval record, package artifact, or control-plane file change. Do not represent reviewer acceptance as legal approval, publication authorization, live proof, or G01 resolution.</action>
  <verify>
    <automated>npm ci --no-audit --no-fund &amp;&amp; npm run check</automated>
  </verify>
  <done>The new test is discovered by the existing compiled-test runner and catches tool-catalog, source-separation, Analytics-limitation, safety-wording, and evidence-state regressions. The PR evidence model distinguishes local documentation work from every remaining external gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source registration to public documentation | Tool names and result states can change independently from Markdown. |
| Operator guide to external AI processing | Operators may mistake local stdio or an environment acknowledgment for legal authorization. |
| Repository documentation to public distribution | Wording can imply Toast approval, brand permission, publication, or live proof without any such evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-01 | Spoofing | README and operator guide | mitigate | Per D-02/D-03, use neutral identification, link the observed Terms, and name brand approval as a human or Toast gate. |
| T-06-02 | Tampering | Report catalog and tool registrations | mitigate | Add `test/public-operator-docs.test.ts` to compare the six source registrations with public documentation and the Analytics result-state boundary. |
| T-06-03 | Information disclosure | Configuration examples and test evidence | mitigate | Per D-04/D-06, use invented placeholders only; never read environment values or place credentials, Merchant Data, or guest-linked data in docs or tests. |
| T-06-04 | Repudiation | PR evidence | mitigate | Require exact head, issue #22, commands, discovered-test counts, source files reviewed, Terms date/link, DOX result, and explicit external-gate status. |
| T-06-05 | Elevation of privilege | Operator guidance | mitigate | State that local stdio and consent acknowledgment do not grant credential, Merchant, provider, legal, or Toast approval; retain capability denials and location isolation. |
| T-06-SC | Tampering | npm dependency restoration for verification | accept | No package is added or changed. Use only the committed lockfile with `npm ci --no-audit --no-fund`; never substitute cached, copied, or reconstructed package evidence. |
</threat_model>

<evidence_model>

| Evidence class | T6-002 may record | T6-002 must not record as achieved |
|---|---|---|
| Implemented documentation | Exact documentation and test paths, source registrations reviewed, and operator-contract wording | Package publication, signing, live Toast access, or a complete Analytics result parser |
| Synthetic validation | Focused documentation test, full `npm run check`, exact head, Node/npm versions, and test discovery/counts | Live compatibility, installed-artifact smoke, or legal approval |
| External or human gate | Direct Terms link/date and the remaining gate identifiers | Consent proof, Toast brand approval, third-party-provider approval, G01 resolution, or release authorization |

### Required PR evidence

1. Link issue #22 and state `DOX: updated`.
2. Record the immutable candidate head before every command and confirm it did not change.
3. Record focused-test and `npm run check` commands, exit status, discovered file count, and test count.
4. List the reviewed registration files and all modified documentation/test paths.
5. State that no live credentials, Merchant Data, publication, signing, tarball install, or control-plane update occurred.

</evidence_model>

<verification>
1. Restore dependencies only from the committed lockfile with `npm ci --no-audit --no-fund`.
2. Run `npm run build:test && node --test --enable-source-maps dist-test/test/public-operator-docs.test.js`.
3. Run `npm run check` and record discovered test files and test count.
4. Inspect `git diff --check` and the changed-path list. Confirm only the six owned paths changed.
5. Inspect the documentation diff for Terms-copying, secret-like values, Merchant Data, endorsement/approval claims, package publication claims, or a complete Analytics-result claim. Record the result in PR evidence.
</verification>

<success_criteria>
- The docs describe every current tool from its registration contract and keep Standard and Analytics sources distinct.
- The Analytics documentation permits no result state beyond `denied` or `incomplete` and preserves T5-003-G01.
- Operators see safety and external-approval responsibilities before configuration guidance.
- The compiled-test runner discovers and passes the documentation regression test.
- The PR evidence separates implementation, synthetic validation, and external gates, with `DOX: updated`.
- No release action, live request, Merchant Data processing, approval claim, or control-plane mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/06-release-hardening-and-public-compatibility-proof/06-01-SUMMARY.md` when done.
</output>
