---
phase: 06-release-hardening-and-public-compatibility-proof
plan: "07"
type: tdd
wave: 7
depends_on: ["06-06"]
files_modified:
  - src/mcp-request-cancellation.ts
  - src/index.ts
  - src/server.ts
  - src/report-tools.ts
  - src/analytics-report-tools.ts
  - test/first-tool-cancellation-e2e.test.ts
  - test/fixtures/installed-artifact-fetch-preload.ts
autonomous: true
requirements: [T6-003]
must_haves:
  truths:
    - "The pinned modern 2026-07-28 client sends its first post-connect Standard tools/call with JSON-RPC ID 0, while the retained legacy client sends initialize with ID 0 and its first tools/call with ID 1; client abort reaches the request signal used by the production runtime in both eras."
    - "In both eras, a later nonzero report request also aborts through the local bridge and stops its selected Standard upstream fetch with the existing fail-closed cancellation result shape."
    - "Resolve, source rejection, and each cancellation leave zero active cancellation controllers and zero local abort-signal relay listeners before process reuse and shutdown."
    - "The explicit registration matrix covers toast_sales_summary, toast_payment_summary, toast_item_sales_summary, toast_cash_summary, toast_labor_summary, and toast_analytics_metrics_day, with each callback using the same bridge path."
  artifacts:
    - path: "src/mcp-request-cancellation.ts"
      provides: "Public-SDK request-ID cancellation bridge and deterministic active-controller and relay-listener observer"
    - path: "test/first-tool-cancellation-e2e.test.ts"
      provides: "Official-client, production-executable request-zero cancellation proof"
    - path: "test/fixtures/installed-artifact-fetch-preload.ts"
      provides: "Invented abortable upstream source for the executable proof"
  key_links:
    - from: "test/first-tool-cancellation-e2e.test.ts"
      to: "src/index.ts"
      via: "Official legacy and modern Client configurations start the compiled production executable without a fixture server and preserve their actual request-ID allocation"
      pattern: "dist/index.js"
    - from: "src/mcp-request-cancellation.ts"
      to: "src/report-tools.ts"
      via: "Each registered report callback receives a combined SDK and bridge abort signal"
      pattern: "ctx.mcpReq.id"
    - from: "src/report-tools.ts"
      to: "src/rate-limited-client.ts"
      via: "The combined request signal reaches existing runtime builders and cancellable Standard source calls"
      pattern: "signal"
    - from: "src/mcp-request-cancellation.ts"
      to: "test/first-tool-cancellation-e2e.test.ts"
      via: "A bounded opt-in observer reports zero active entries and zero local relay listeners at each terminal callback state"
      pattern: "activeControllers.*0"
---

<objective>
Correct the request-zero cancellation gap through a reviewed local bridge that
uses only public MCP SDK APIs.

Purpose: Make the first real stdio report-tool request abort the same runtime
and Standard source chain already proven for later request IDs.
Output: A failing-then-green executable conformance test and a production
bridge for all registered report callbacks.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-CONTEXT.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-07-RESEARCH.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md
@src/index.ts
@src/server.ts
@src/stdio.ts
@src/report-tools.ts
@src/analytics-report-tools.ts
@src/runtime.ts
@src/rate-limited-client.ts
@test/protocol-cancellation.test.ts
@test/package-artifact-e2e.test.ts
@test/fixtures/installed-artifact-fetch-preload.ts
</context>

<interfaces>
From the installed `@modelcontextprotocol/server@2.0.0` public types:

- `ctx.mcpReq.id` is the JSON-RPC ID for the active handler.
- `ctx.mcpReq.signal` is the SDK-provided request abort signal.
- `McpServer.server` exposes the official underlying server and its typed
  notification-handler registration API.
- `serveStdio(factory)` owns stdio and pins one factory instance for a
  connection lifetime.

From `test/support/report-tools-e2e-support.ts`:

- The retained legacy era uses the official client `versionNegotiation` legacy
  mode.
- The retained modern era uses the official client pinned to `2026-07-28`.
- Both eras use the installed official `StdioClientTransport`.

From `src/report-tools.ts` and `src/analytics-report-tools.ts`:

- Standard report builders accept `{ signal?: AbortSignal }` and return their
  current complete, incomplete, or denied envelopes.
- Analytics remains an existing denied-or-incomplete-only lifecycle tool.
- `createServer()` supplies one process-owned `ApplicationRuntime` to every
  tool registration.
</interfaces>

<source_audit>

SOURCE | ID | Required fact or decision | Coverage | Status
--- | --- | --- | --- | ---
GOAL | Phase 06 | A release gate requires an actual production path, not a fixture server or direct function test. | Tasks 1-2 | COVERED
REQ | T6-003 | Package-facing stdio behavior needs exact production-chain evidence. | Tasks 1-2 | COVERED
REQ | #60 | Request-zero cancellation must reach handler, runtime, and Standard upstream abort. | Tasks 1-2 | COVERED
RESEARCH | SDK 2.0.0 | SDK cancellation treats numeric zero as absent. | Task 1 | COVERED
RESEARCH | Public bridge seams | Request context IDs and notification registration permit a local correction without SDK internals. | Task 2 | COVERED
RESEARCH | Existing cancellation chain | Runtime and Standard transport already propagate a supplied signal. | Tasks 1-2 | COVERED
RESEARCH | Retained protocol eras | Legacy and modern official-client paths need independent executable cancellation proof. | Task 1 | COVERED
RESEARCH | Lifecycle cleanup | Controllers and abort relays must be observable as zero after every terminal callback state. | Tasks 1-2 | COVERED
CONTEXT | D-01 | Separate implementation from synthetic and external evidence. | Task 2 | COVERED
CONTEXT | D-02, D-03 | Preserve no-approval and no-publication claims. | Task 2 | COVERED
CONTEXT | D-04, D-05, D-06 | Preserve consent, credential, no-training, and excluded-data boundaries. | Tasks 1-2 | COVERED
CONTEXT | D-07, D-08 | Preserve the actual Standard tools and constrained Analytics tool. | Task 2 | COVERED
CONTEXT | D-09 | Retain all named external gates until the next plan records exact-head evidence. | Task 2 | COVERED

</source_audit>

<dependency_graph>

| Task | Needs | Creates | Wave dependency |
| --- | --- | --- | --- |
| 1 | Current compiled executable, official SDK client/transport, existing test-only preload | Failing legacy-and-modern executable cancellation and lifecycle-cleanup contracts | None |
| 2 | Task 1 RED contracts | Public-SDK cancellation bridge, observer, and explicit six-tool registration matrix | Task 1 |

</dependency_graph>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write failing legacy-and-modern production-path contracts</name>
  <files>test/first-tool-cancellation-e2e.test.ts, test/fixtures/installed-artifact-fetch-preload.ts</files>
  <behavior>
    - Test 1: The retained legacy official client connects through an official `StdioClientTransport` to compiled `dist/index.js`, sends initialize with numeric ID `0`, then sends its first `tools/call` with numeric ID `1`.
    - Test 2: The official client pinned to `2026-07-28` connects through the same path, performs discovery, then sends its first `tools/call` with numeric ID `0`.
    - Test 3: In each era, that actual first `toast_sales_summary` call starts the invented abortable Orders fetch. Aborting its client signal rejects the caller, makes the source observe abort, and observes the existing structured denied envelope for the matching request ID.
    - Test 4: In each era, a later `toast_payment_summary` call has a nonzero wire ID, starts its invented abortable source path, and has the same cancellation result, source-abort, and retained-process behavior after the bridge.
    - Test 5: In each era, a successful report resolution and an invented source rejection both produce their existing result boundaries and then report zero active controllers and zero bridge relay listeners.
    - Test 6: After modern ID-zero, legacy ID-one, and later nonzero cancellation, each process reports zero active controllers and zero bridge relay listeners before a later report succeeds and `client.close()` completes.
  </behavior>
  <action>Create `test/first-tool-cancellation-e2e.test.ts` as a parameterized process-level conformance test for both retained eras. Use the existing official client configuration: legacy `versionNegotiation` mode and modern `2026-07-28` pinned mode. Compile and start `dist/index.js` from the current candidate for each era. Do not start `test/fixtures/protocol-cancellation-server.ts`, `test/fixtures/stdio-report-server.ts`, or a direct report-builder test. Instantiate the installed official `Client` and `StdioClientTransport`. Use a passive test subclass of that transport only to record outbound JSON-RPC messages before delegating unchanged to the official transport. The subclass must neither serialize frames nor allocate IDs. For legacy mode, assert numeric ID `0` for initialize, numeric ID `1` for the first `tools/call`, and cancellation notification request ID `1`. Label this as legacy regression proof. For modern pinned `2026-07-28` mode, assert discovery precedes the first `tools/call`, assert that first tool ID is numeric `0`, and assert cancellation notification request ID `0`. Label this as the numeric-zero defect proof. In each era, abort `toast_sales_summary` only after the invented Orders fetch starts. Then call `toast_payment_summary`, assert its observed ID is nonzero, and cancel it after its source start marker. For each cancellation, record the matching response before the official client rejects and assert the existing denied result shape, source abort marker, and zero cleanup snapshot. Also run one resolving source path and one rejecting source path. Each terminal path must emit a bounded in-memory-observed snapshot with `activeControllers: 0` and `relayListeners: 0` before reuse. Add an optional typed cancellation-registry observer at production composition. It must default to no observer. Enable it only through an explicit executable-test setting with invented data. The observer must report counts only. It must not replace the bridge, runtime, transport, source, or result path. Extend the existing external preload with opt-in invented Orders routes for start, abort, resolve, and reject markers. Preserve its existing default artifact-test behavior. Start the executable with only invented environment values and that copied compiled preload. Assert a later report succeeds through the same PID after both cancellation cases and `client.close()` finishes. Do not use real credentials, Toast endpoints, Merchant Data, raw stderr persistence, custom JSON-RPC framing, a source-path stand-in, a test double for the server/runtime, or package metadata changes. Run the focused command and retain the RED failure caused by the modern SDK numeric-zero gap.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; ! node --test --enable-source-maps dist-test/test/first-tool-cancellation-e2e.test.js</automated>
  </verify>
  <done>The RED tests prove that the actual modern first request with ID zero reaches the compiled production executable and upstream source but does not yet make the source observe cancellation. The tests also prove legacy first-request ID one cancellation as regression coverage and define the exact nonzero and terminal-cleanup requirements for the correction.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add the public-SDK request-ID cancellation bridge</name>
  <files>src/mcp-request-cancellation.ts, src/index.ts, src/server.ts, src/report-tools.ts, src/analytics-report-tools.ts</files>
  <behavior>
    - Test 1: The bridge recognizes numeric ID zero and nonzero numeric or string IDs without truthiness filtering.
    - Test 2: A cancellation notification aborts only the matching active callback, then callback finalization removes its controller and every bridge-created SDK or local abort relay after resolve, rejection, and cancellation.
    - Test 3: Standard builders receive a signal that represents either SDK cancellation or bridge cancellation and preserve their current result envelopes.
    - Test 4: The registration matrix maps exactly the five Standard tools and `toast_analytics_metrics_day` to the same callback wrapper. Analytics does not gain a completed result body or bypass its existing capability boundary.
  </behavior>
  <action>Create `src/mcp-request-cancellation.ts` as the one local correction point. Give it a narrow typed API that attaches one public cancellation-notification handler to the official underlying server and wraps a registered tool callback. Key active controllers by `ctx.mcpReq.id`. Treat an ID as present when it is not undefined, so numeric zero remains valid. Combine the SDK handler signal with the bridge controller signal. Track each bridge-created abort relay and remove every one during a single idempotent finalization path. Finalization must run after resolve, rejection, and cancellation. Add a typed observer snapshot containing only active-controller and relay-listener counts. It defaults to absent and must never affect cancellation behavior. In `src/index.ts` and `src/server.ts`, pass that observer only for the explicit executable-test configuration; normal local package execution creates no observer. In the test configuration, emit only bounded count markers for test-process observation. Create an explicit registration matrix as one reviewable source of truth. It must name exactly `toast_sales_summary`, `toast_payment_summary`, `toast_item_sales_summary`, `toast_cash_summary`, `toast_labor_summary`, and `toast_analytics_metrics_day`. Use it to require the bridge wrapper at every tool registration. Update every Standard callback in `src/report-tools.ts` and the constrained Analytics callback in `src/analytics-report-tools.ts` to use the matrix-controlled wrapper while preserving current input schema, output schema, `toolResult`, runtime identity, signal forwarding, denial behavior, and body-free Analytics result boundary. This replaces the SDK cancellation dispatch for these callbacks, so it must handle existing nonzero IDs as well as zero. A late or unknown cancellation notification must have no effect. Use only the installed public SDK imports and documented public methods. Do not edit `node_modules`, inspect or mutate SDK private fields, use a deep SDK import, cast around private state, patch a prototype, reimplement a transport or protocol, add an SDK generation, add an alternate transport, introduce a server/runtime test double, or change `package.json` or `package-lock.json`. Per D-04 through D-06, retain invented-only test data, consent limits, and guest-data exclusions. Per D-07 and D-08, preserve all five Standard registrations and the denied-or-incomplete Analytics tool. Per D-01, D-02, D-03, and D-09, do not change release documentation or claim that any external gate is resolved. Run the focused GREEN test for both eras, the existing protocol-cancellation test, and the report-tools process tests after implementation.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test --enable-source-maps dist-test/test/first-tool-cancellation-e2e.test.js dist-test/test/protocol-cancellation.test.js dist-test/test/report-tools-e2e.test.js</automated>
  </verify>
  <done>The compiled production executable accepts official request-zero and nonzero cancellation in both retained eras, propagates each through the runtime and selected Standard source fetch, returns the existing fail-closed result behavior, reports deterministic zero cleanup after every terminal state, supports later work on the retained process, and keeps all six tool contracts unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Official stdio client to production executable | The test must retain real SDK framing and request identity semantics. |
| SDK cancellation notification to local bridge | A malformed, late, or zero-valued request ID must not cancel the wrong request. |
| Bridge signal to runtime and source | Cancellation must terminate queue, wait, and fetch paths without converting failure to a successful report. |
| Invented preload to process test | Synthetic upstream data may isolate HTTP only and must not become the server/runtime under test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-07-01 | Tampering | SDK cancellation handling | mitigate | Use an owned active-ID map through public SDK APIs; forbid private SDK access and package mutation. |
| T-06-07-02 | Denial of service | Active callback cleanup | mitigate | Remove map entries and signal relays in callback finalization; prove process reuse and shutdown. |
| T-06-07-03 | Spoofing | Test request identity | mitigate | Passively observe official per-era initialize, discovery, tools/call, and cancellation messages; require modern first-tool ID zero and legacy first-tool ID one. |
| T-06-07-04 | Information disclosure | Process fixture | mitigate | Use independently invented preload data and bounded markers; do not persist environment or raw stderr. |
| T-06-07-05 | Elevation of privilege | Report handling | mitigate | Preserve shared runtime, location isolation, capability decisions, read-only tools, and Analytics incomplete-only behavior. |
| T-06-07-SC | Tampering | MCP package supply chain | accept | Add no package and change no lockfile. Restore the committed lockfile only in the next plan's clean-worktree gates. |
</threat_model>

<verification>
1. Record the current RED result from the compiled production-process test.
2. Run the focused GREEN process test, protocol-cancellation test, and report-tool process test.
3. Inspect the changed paths. They must contain only the bridge, server registration, tool wrappers, test, and test-only preload.
4. Confirm no direct test function, fixture server, SDK-private access, package change, live credential, Merchant Data, publication, signing, or control-plane edit appears.
</verification>

<success_criteria>
- The pinned modern first post-connect Standard tool call uses JSON-RPC ID zero, while the retained legacy first Standard tool call uses JSON-RPC ID one after initialize ID zero, through the installed official SDK stdio transport.
- Cancelling that call reaches the actual runtime and the selected abortable Standard source fetch.
- The process remains usable and closes cleanly after cancellation.
- The bridge is a public-API correction for every registered report callback, not an SDK patch or a parallel protocol implementation.
- Existing nonzero cancellation, report schemas, location isolation, capability limits, and Analytics constraints remain intact.
</success_criteria>

<output>
Create `.planning/phases/06-release-hardening-and-public-compatibility-proof/06-07-SUMMARY.md` when done.
</output>
