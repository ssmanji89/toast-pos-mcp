---
phase: 06-release-hardening-and-public-compatibility-proof
plan: "07"
type: tdd
wave: 7
depends_on: ["06-06"]
files_modified:
  - src/mcp-request-cancellation.ts
  - src/server.ts
  - src/report-tools.ts
  - src/analytics-report-tools.ts
  - test/first-tool-cancellation-e2e.test.ts
  - test/fixtures/installed-artifact-fetch-preload.ts
autonomous: true
requirements: [T6-003]
must_haves:
  truths:
    - "The first post-connect Standard tools/call request has JSON-RPC ID 0 and client abort reaches the request signal used by the production runtime."
    - "The cancelled request stops its selected Standard upstream fetch and returns only the existing fail-closed cancellation result shape."
    - "A later call on the retained process succeeds and clean shutdown leaves no active request cancellation state."
    - "Every registered report callback retains official SDK cancellation for nonzero IDs and gains the same request-zero bridge behavior."
  artifacts:
    - path: "src/mcp-request-cancellation.ts"
      provides: "Public-SDK request-ID cancellation bridge for all registered report callbacks"
    - path: "test/first-tool-cancellation-e2e.test.ts"
      provides: "Official-client, production-executable request-zero cancellation proof"
    - path: "test/fixtures/installed-artifact-fetch-preload.ts"
      provides: "Invented abortable upstream source for the executable proof"
  key_links:
    - from: "test/first-tool-cancellation-e2e.test.ts"
      to: "src/index.ts"
      via: "Official StdioClientTransport starts compiled production executable without a fixture server"
      pattern: "dist/index.js"
    - from: "src/mcp-request-cancellation.ts"
      to: "src/report-tools.ts"
      via: "Each registered report callback receives a combined SDK and bridge abort signal"
      pattern: "ctx.mcpReq.id"
    - from: "src/report-tools.ts"
      to: "src/rate-limited-client.ts"
      via: "The combined request signal reaches existing runtime builders and cancellable Standard source calls"
      pattern: "signal"
---

<objective>
Correct the request-zero cancellation gap through a reviewed local bridge that
uses only public MCP SDK APIs.

Purpose: Make the first real stdio report-tool request abort the same runtime
and Standard source chain already proven for later request IDs.
Output: A failing-then-green executable conformance test and a production
bridge for all registered report callbacks.
</objective>

<execution_context>
@/Users/sully/.codex/gsd-core/workflows/execute-plan.md
@/Users/sully/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@LOOP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-CONTEXT.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-07-RESEARCH.md
@.planning/phases/06-release-hardening-and-public-compatibility-proof/06-VALIDATION.md
@src/index.ts
@src/server.ts
@src/stdio.ts
@src/report-tools.ts
@src/analytics-report-tools.ts
@src/runtime.ts
@src/rate-limited-client.ts
@test/protocol-cancellation.test.ts
@test/package-artifact-e2e.test.ts
@test/fixtures/installed-artifact-fetch-preload.ts
</context>

<interfaces>
From the installed `@modelcontextprotocol/server@2.0.0` public types:

- `ctx.mcpReq.id` is the JSON-RPC ID for the active handler.
- `ctx.mcpReq.signal` is the SDK-provided request abort signal.
- `McpServer.server` exposes the official underlying server and its typed
  notification-handler registration API.
- `serveStdio(factory)` owns stdio and pins one factory instance for a
  connection lifetime.

From `src/report-tools.ts` and `src/analytics-report-tools.ts`:

- Standard report builders accept `{ signal?: AbortSignal }` and return their
  current complete, incomplete, or denied envelopes.
- Analytics remains an existing denied-or-incomplete-only lifecycle tool.
- `createServer()` supplies one process-owned `ApplicationRuntime` to every
  tool registration.
</interfaces>

<source_audit>

SOURCE | ID | Required fact or decision | Coverage | Status
--- | --- | --- | --- | ---
GOAL | Phase 06 | A release gate requires an actual production path, not a fixture server or direct function test. | Tasks 1-2 | COVERED
REQ | T6-003 | Package-facing stdio behavior needs exact production-chain evidence. | Tasks 1-2 | COVERED
REQ | #60 | Request-zero cancellation must reach handler, runtime, and Standard upstream abort. | Tasks 1-2 | COVERED
RESEARCH | SDK 2.0.0 | SDK cancellation treats numeric zero as absent. | Task 1 | COVERED
RESEARCH | Public bridge seams | Request context IDs and notification registration permit a local correction without SDK internals. | Task 2 | COVERED
RESEARCH | Existing cancellation chain | Runtime and Standard transport already propagate a supplied signal. | Tasks 1-2 | COVERED
CONTEXT | D-01 | Separate implementation from synthetic and external evidence. | Task 2 | COVERED
CONTEXT | D-02, D-03 | Preserve no-approval and no-publication claims. | Task 2 | COVERED
CONTEXT | D-04, D-05, D-06 | Preserve consent, credential, no-training, and excluded-data boundaries. | Tasks 1-2 | COVERED
CONTEXT | D-07, D-08 | Preserve the actual Standard tools and constrained Analytics tool. | Task 2 | COVERED
CONTEXT | D-09 | Retain all named external gates until the next plan records exact-head evidence. | Task 2 | COVERED

</source_audit>

<dependency_graph>

| Task | Needs | Creates | Wave dependency |
| --- | --- | --- | --- |
| 1 | Current compiled executable, official SDK client/transport, existing test-only preload | A failing ID-zero executable-process contract | None |
| 2 | Task 1 RED contract | Public-SDK cancellation bridge and green production path | Task 1 |

</dependency_graph>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write the failing first-request production-path contract</name>
  <files>test/first-tool-cancellation-e2e.test.ts, test/fixtures/installed-artifact-fetch-preload.ts</files>
  <behavior>
    - Test 1: A pinned official `Client` connects through an official `StdioClientTransport` to compiled `dist/index.js`, and its first `tools/call` message has ID `0`.
    - Test 2: That first `toast_sales_summary` call starts the invented abortable Orders fetch. Aborting its client signal rejects the caller and makes the source observe abort.
    - Test 3: The server's reply for the observed request-zero wire ID is the existing structured denied envelope without a fabricated aggregate result.
    - Test 4: The retained process completes a later report request and closes within the existing bounded test timeout.
  </behavior>
  <action>Create `test/first-tool-cancellation-e2e.test.ts` as a process-level conformance test. Compile and start `dist/index.js` from the current candidate. Do not start `test/fixtures/protocol-cancellation-server.ts`, `test/fixtures/stdio-report-server.ts`, or a direct report-builder test. Instantiate the installed official `Client` and `StdioClientTransport`. Use a passive test subclass of that transport only to record inbound and outbound JSON-RPC messages before delegating unchanged to the official transport. The subclass must neither serialize frames nor allocate IDs. After `client.connect`, call `toast_sales_summary` immediately with the existing invented business date and explicit tool definition. Assert the first observed `tools/call` request ID equals numeric zero. Extend the existing external preload with an opt-in invented Orders route that writes bounded start and abort markers to stderr, waits only for `init.signal`, and rejects after abort. Preserve its existing default artifact-test behavior. Start the executable with only invented environment values and that copied compiled preload. Wait for the fetch-start marker before aborting. Record the matching request-zero response before the official client rejects its cancelled promise. Assert the response remains the current denial shape, the source abort marker occurs, a later report succeeds through the same PID, and `client.close()` finishes. Do not use real credentials, Toast endpoints, Merchant Data, raw stderr persistence, custom JSON-RPC framing, a source-path stand-in, or package metadata changes. Run the focused command and retain the RED failure caused by the known SDK request-zero gap.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; ! node --test --enable-source-maps dist-test/test/first-tool-cancellation-e2e.test.js</automated>
  </verify>
  <done>The RED test proves that an actual official-client stdio request with ID zero reaches the compiled production executable and upstream source, but does not yet make the source observe cancellation.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add the public-SDK request-ID cancellation bridge</name>
  <files>src/mcp-request-cancellation.ts, src/server.ts, src/report-tools.ts, src/analytics-report-tools.ts</files>
  <behavior>
    - Test 1: The bridge recognizes numeric ID zero and nonzero numeric or string IDs without truthiness filtering.
    - Test 2: A cancellation notification aborts only the matching active callback, then the callback cleanup removes its controller and event relays.
    - Test 3: Standard builders receive a signal that represents either SDK cancellation or bridge cancellation and preserve their current result envelopes.
    - Test 4: Analytics uses the same callback registration path without gaining a completed result body or bypassing its existing capability boundary.
  </behavior>
  <action>Create `src/mcp-request-cancellation.ts` as the one local correction point. Give it a narrow typed API that attaches one public cancellation-notification handler to the official underlying server and wraps a registered tool callback. Key active controllers by `ctx.mcpReq.id`. Treat an ID as present when it is not undefined, so numeric zero remains valid. Combine the SDK handler signal with the bridge controller signal. Ensure every callback settlement removes its active entry and all local abort listeners. A late or unknown cancellation notification must have no effect. In `src/server.ts`, construct the bridge beside the existing official `McpServer`, then pass its callback wrapper to both registration modules. Update every Standard tool callback in `src/report-tools.ts` and the one constrained Analytics callback in `src/analytics-report-tools.ts` to use that wrapper while preserving their current input schema, output schema, `toolResult`, runtime identity, signal forwarding, denial behavior, and body-free Analytics result boundary. This replaces the SDK's cancellation notification dispatch for these callbacks, so it must handle existing nonzero request IDs as well as zero. Use only the installed public SDK imports and documented public methods. Do not edit `node_modules`, inspect or mutate SDK private fields, use a deep SDK import, cast around private state, patch a prototype, reimplement a transport or protocol, add an SDK generation, or change `package.json` or `package-lock.json`. Per D-04 through D-06, retain invented-only test data, consent limits, and guest-data exclusions. Per D-07 and D-08, preserve all five Standard registrations and the denied-or-incomplete Analytics tool. Per D-01, D-02, D-03, and D-09, do not change release documentation or claim that any external gate is resolved. Run the focused GREEN test, the existing protocol-cancellation test, and the report-tools process tests after implementation.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test --enable-source-maps dist-test/test/first-tool-cancellation-e2e.test.js dist-test/test/protocol-cancellation.test.js dist-test/test/report-tools-e2e.test.js</automated>
  </verify>
  <done>The compiled production executable accepts official request-zero cancellation, propagates it through the runtime and selected Standard source fetch, returns the existing fail-closed result behavior, supports later work on the retained process, and keeps all existing tool contracts unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Official stdio client to production executable | The test must retain real SDK framing and request identity semantics. |
| SDK cancellation notification to local bridge | A malformed, late, or zero-valued request ID must not cancel the wrong request. |
| Bridge signal to runtime and source | Cancellation must terminate queue, wait, and fetch paths without converting failure to a successful report. |
| Invented preload to process test | Synthetic upstream data may isolate HTTP only and must not become the server/runtime under test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-07-01 | Tampering | SDK cancellation handling | mitigate | Use an owned active-ID map through public SDK APIs; forbid private SDK access and package mutation. |
| T-06-07-02 | Denial of service | Active callback cleanup | mitigate | Remove map entries and signal relays in callback finalization; prove process reuse and shutdown. |
| T-06-07-03 | Spoofing | Test request identity | mitigate | Passively observe the official transport's first outbound tools/call message and require ID zero. |
| T-06-07-04 | Information disclosure | Process fixture | mitigate | Use independently invented preload data and bounded markers; do not persist environment or raw stderr. |
| T-06-07-05 | Elevation of privilege | Report handling | mitigate | Preserve shared runtime, location isolation, capability decisions, read-only tools, and Analytics incomplete-only behavior. |
| T-06-07-SC | Tampering | MCP package supply chain | accept | Add no package and change no lockfile. Restore the committed lockfile only in the next plan's clean-worktree gates. |
</threat_model>

<verification>
1. Record the current RED result from the compiled production-process test.
2. Run the focused GREEN process test, protocol-cancellation test, and report-tool process test.
3. Inspect the changed paths. They must contain only the bridge, server registration, tool wrappers, test, and test-only preload.
4. Confirm no direct test function, fixture server, SDK-private access, package change, live credential, Merchant Data, publication, signing, or control-plane edit appears.
</verification>

<success_criteria>
- The first post-connect Standard tool call uses JSON-RPC ID zero through the installed official SDK stdio transport.
- Cancelling that call reaches the actual runtime and the selected abortable Standard source fetch.
- The process remains usable and closes cleanly after cancellation.
- The bridge is a public-API correction for every registered report callback, not an SDK patch or a parallel protocol implementation.
- Existing nonzero cancellation, report schemas, location isolation, capability limits, and Analytics constraints remain intact.
</success_criteria>

<output>
Create `.planning/phases/06-release-hardening-and-public-compatibility-proof/06-07-SUMMARY.md` when done.
</output>
